---
phase: 02.5-you-re-a-staff-level-engineer-be-sure-this-app-is-fully-productionalized
plan: "02"
subsystem: infra
tags: [inngest, pinecone, openai, cron, tdd]

requires:
  - phase: 02.5-01
    provides: process_message failure handler, HEALTHCHECK, CI workflow, render.yaml

provides:
  - Real sync_pinecone_queue sweep logic — drains pending rows from pinecone_sync_queue and upserts to Pinecone
  - _openai_client module-level var in inngest_client.py injected from main.py lifespan
  - Unit tests for sweep (success, failure, empty queue, mixed rows)

affects:
  - Any phase touching inngest_client.py or the Pinecone sync pipeline

tech-stack:
  added: []
  patterns:
    - Module-level singleton (_openai_client) injected from lifespan, same pattern as _orchestrator
    - Separate async session per DB operation (SELECT + per-row UPDATE) to avoid session reuse after exception
    - Row-level error isolation — warning logged, processing continues for remaining rows

key-files:
  created:
    - tests/inngest/test_sync_pinecone_queue.py
  modified:
    - src/inngest_client.py
    - src/main.py

key-decisions:
  - "_openai_client module-level var in inngest_client.py set by lifespan — same pattern as _orchestrator, avoids circular imports"
  - "write_job_embedding imported at module top of inngest_client.py (not lazy) — no circular import concern since pinecone_client has no dependency on inngest_client"
  - "sqlalchemy text() import added to inngest_client.py for raw SQL sweep query"

patterns-established:
  - "Module-level singleton injection from lifespan: ic._openai_client = openai_client after ic._orchestrator = orchestrator"

requirements-completed: [PROD-04]

duration: 8min
completed: 2026-03-08
---

# Phase 02.5 Plan 02: sync_pinecone_queue Sweep Summary

**Pinecone cron sweep implemented via Inngest TriggerCron — queries pending rows, calls write_job_embedding per row, marks success/failed with retry_count increment on failure, openai_client injected from lifespan.**

## Performance

- **Duration:** 8 min
- **Started:** 2026-03-08T08:22:10Z
- **Completed:** 2026-03-08T08:30:00Z
- **Tasks:** 1
- **Files modified:** 3

## Accomplishments

- Replaced no-op sync_pinecone_queue stub with real sweep: SELECT pending LIMIT 50, per-row upsert via write_job_embedding, UPDATE status='success'|'failed' + retry_count+1
- Added _openai_client = None module-level var to inngest_client.py; injected via ic._openai_client = openai_client in lifespan
- 4 unit tests covering success path, failure path (warning emitted, row marked failed), empty queue (no DB writes), and mixed rows (success + failure coexist)
- All 72 existing tests continue to pass

## Task Commits

1. **Task 1: Implement sync_pinecone_queue sweep and wire openai_client from lifespan** - `4f79e54` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `src/inngest_client.py` - Added _openai_client var, write_job_embedding import, text import; replaced stub with real sweep
- `src/main.py` - Added ic._openai_client = openai_client injection in lifespan
- `tests/inngest/test_sync_pinecone_queue.py` - 4 unit tests for sweep logic

## Decisions Made

- write_job_embedding imported at module top (not lazy) — no circular import because pinecone_client has no dependency on inngest_client
- sqlalchemy text() used for raw SQL sweep query, consistent with existing DB usage in the codebase
- Separate async session opened per UPDATE (not reused from SELECT session) — clean session lifecycle per operation

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed bad test assertion using pytest.approx as expected settings value**
- **Found during:** Task 1 (TDD GREEN phase)
- **Issue:** Test used `settings=pytest.approx` in assert_awaited_once_with which doesn't compare correctly; test was failing despite correct implementation
- **Fix:** Replaced assert_awaited_once_with with assert mock_write.await_count == 1 and manual call_args.kwargs inspection
- **Files modified:** tests/inngest/test_sync_pinecone_queue.py
- **Verification:** All 4 tests pass
- **Committed in:** 4f79e54 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug in test assertion)
**Impact on plan:** Required fix for test correctness; no scope creep.

## Issues Encountered

None beyond the test assertion fix above.

## Next Phase Readiness

- Pinecone sync pipeline is fully operational end-to-end: pipeline writes to pinecone_sync_queue, cron sweeps and upserts to Pinecone
- Phase 02.5 productionalization complete

---
*Phase: 02.5-you-re-a-staff-level-engineer-be-sure-this-app-is-fully-productionalized*
*Completed: 2026-03-08*
