---
phase: 01.1-apply-revised-3nf-schema-and-propagate-throughout-app
plan: "02"
subsystem: api
tags: [sqlmodel, fastapi, pytest, sqlite, aiosqlite, inngest]

requires:
  - phase: 01.1-01
    provides: "User, Message, RateLimit, AuditLog models with integer FK topology"

provides:
  - "service.py: get_or_create_user(session, phone_hash) -> User with integer PK"
  - "service.py: enforce_rate_limit(session, user_id: int)"
  - "service.py: write_inbound_message(session, message_sid, user_id, body) -> Message"
  - "service.py: write_audit_log(session, message_sid, event, detail, message_id)"
  - "router.py: get_or_create_user called before enforce_rate_limit; user.id passed to all downstream calls"
  - "conftest.py: in-memory SQLite URL; make_user fixture"
  - "test_webhook.py: assertions on integer user.id and audit_log.detail JSON payload"

affects: [phase-02, integration-tests]

tech-stack:
  added: []
  patterns:
    - "get_or_create_user upsert: INSERT INTO \"user\" ... ON CONFLICT (phone_hash) DO NOTHING, then SELECT"
    - "rate_limit upsert: ON CONFLICT (user_id, created_at) — column list syntax for SQLite/PG compatibility"
    - "write_inbound_message returns Message object so caller can pass message.id to write_audit_log"
    - "audit_log.detail stores raw Twilio form payload as JSON string (SEC-04)"

key-files:
  created: []
  modified:
    - src/sms/service.py
    - src/sms/router.py
    - tests/conftest.py
    - tests/sms/test_webhook.py

key-decisions:
  - "ON CONFLICT (user_id, created_at) column list syntax used instead of ON CONFLICT ON CONSTRAINT name — SQLite does not support the constraint-name form"
  - "enforce_rate_limit uses datetime.now(UTC).replace(second=0, microsecond=0) — timezone-aware created_at matching sa.DateTime(timezone=True)"

patterns-established:
  - "Service layer returns ORM objects so callers can chain IDs without extra queries"
  - "Router call ordering: idempotency check → get_or_create_user → enforce_rate_limit → write_inbound_message → write_audit_log"

requirements-completed: []

duration: 10min
completed: 2026-03-07
---

# Phase 01.1 Plan 02: Service Layer and Router 3NF Propagation Summary

**Webhook service layer rewritten to use integer user_id FKs via get_or_create_user; all 12 tests pass against in-memory SQLite with raw Twilio payload stored in audit_log.detail**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-03-07T19:23:00Z
- **Completed:** 2026-03-07T19:33:00Z
- **Tasks:** 2
- **Files modified:** 4

## Accomplishments

- Replaced register_phone/InboundMessage references with get_or_create_user and Message; service.py now imports from src.users.models
- Router reordered: get_or_create_user runs before enforce_rate_limit so user_id is available for rate limit key
- write_audit_log gains message_id parameter; detail stores raw Twilio JSON payload (SEC-04 compliance)
- conftest.py switched to sqlite+aiosqlite:///:memory: with make_user factory fixture; test.db removed from disk

## Task Commits

1. **Task 1: Rewrite service.py with 3NF-aligned signatures** - `f0323dd` (feat)
2. **Task 2: Update router, conftest, and tests** - `3c2605f` (feat)

## Files Created/Modified

- `src/sms/service.py` - New get_or_create_user; enforce_rate_limit(user_id); write_inbound_message returns Message; write_audit_log gains message_id
- `src/sms/router.py` - Reordered gates; passes user.id to all service calls; json.dumps audit detail
- `tests/conftest.py` - In-memory SQLite URL; make_user fixture; User import
- `tests/sms/test_webhook.py` - User/Message imports; integer id assertions; audit detail JSON check

## Decisions Made

- Used `ON CONFLICT (user_id, created_at)` column list syntax (not constraint name) for cross-DB compatibility with SQLite test environment
- enforce_rate_limit uses timezone-aware `datetime.now(UTC)` directly, no naive conversion helper

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed ON CONFLICT syntax for SQLite compatibility**
- **Found during:** Task 2 (test run)
- **Issue:** `ON CONFLICT ON CONSTRAINT uq_rate_limit_user_window` is PostgreSQL-only syntax; SQLite raised `OperationalError: near "ON": syntax error`
- **Fix:** Changed to `ON CONFLICT (user_id, created_at)` — standard SQL supported by both SQLite and PostgreSQL
- **Files modified:** src/sms/service.py
- **Verification:** All 12 tests pass
- **Committed in:** `3c2605f` (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - Bug)
**Impact on plan:** Essential for test suite to run; no scope change.

## Issues Encountered

None beyond the SQLite ON CONFLICT syntax difference handled via deviation Rule 1.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Service and router fully aligned with 3NF schema; ready for Phase 2 feature work
- Alembic migration (from Plan 01) should be verified against live PostgreSQL before Phase 2

---
*Phase: 01.1-apply-revised-3nf-schema-and-propagate-throughout-app*
*Completed: 2026-03-07*
