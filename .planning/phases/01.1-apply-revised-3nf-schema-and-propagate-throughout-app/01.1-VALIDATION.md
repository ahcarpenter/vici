---
phase: 1.1
slug: apply-revised-3nf-schema-and-propagate-throughout-app
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-07
---

# Phase 1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest 7.x + pytest-asyncio |
| **Config file** | `pyproject.toml` |
| **Quick run command** | `uv run pytest tests/ -x -q` |
| **Full suite command** | `uv run pytest tests/ -v` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/ -x -q`
- **After every plan wave:** Run `uv run pytest tests/ -v`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 1.1-01-01 | 01 | 1 | schema | unit | `uv run pytest tests/ -x -q` | ✅ | ⬜ pending |
| 1.1-01-02 | 01 | 1 | models | unit | `uv run pytest tests/ -x -q` | ✅ | ⬜ pending |
| 1.1-01-03 | 01 | 2 | services | unit | `uv run pytest tests/ -x -q` | ✅ | ⬜ pending |
| 1.1-01-04 | 01 | 2 | router | integration | `uv run pytest tests/sms/ -x -q` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/conftest.py` — switch to `sqlite+aiosqlite:///:memory:`, add `make_user` fixture
- [ ] `tests/sms/test_webhook.py` — update assertions and fixtures to new model names and FK pattern

*Existing test infrastructure covers the framework — only fixture/assertion updates needed.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Alembic migration applies cleanly against PostgreSQL | schema | Needs live DB | `docker compose up -d db && uv run alembic upgrade head` |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
