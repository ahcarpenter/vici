---
phase: "02.12"
plan: "01"
subsystem: "pipeline, extraction, main"
tags: [refactor, architecture, di, cleanup]
dependency_graph:
  requires: []
  provides: ["src.pipeline.orchestrator.PipelineOrchestrator"]
  affects: ["src/main.py", "src/temporal/activities.py", "tests/test_pipeline_orchestrator.py"]
tech_stack:
  added: []
  patterns: ["handler chain DI pattern"]
key_files:
  created: []
  modified:
    - src/pipeline/orchestrator.py
    - src/main.py
    - src/temporal/activities.py
    - tests/test_pipeline_orchestrator.py
  deleted:
    - tests/inngest/__init__.py
    - tests/inngest/test_process_message.py
    - tests/inngest/test_process_message_spans.py
    - tests/inngest/test_sync_pinecone_queue.py
decisions:
  - "PipelineOrchestrator now lives in src/pipeline/orchestrator.py — canonical domain placement"
  - "DI wiring builds handler instances explicitly; audit_repo shared across handlers"
  - "MessageRepository import removed from main.py (no longer used in lifespan)"
metrics:
  duration: "~10 minutes"
  completed_date: "2026-04-03"
  tasks_completed: 5
  files_modified: 5
  files_deleted: 4
---

# Phase 02.12 Plan 01: Canonicalize Domain Structure Summary

**One-liner:** Moved PipelineOrchestrator to pipeline domain, fixed DI bug by instantiating handler instances, deleted four dead Inngest test stubs.

## What Was Done

### Task 1 — Move orchestrator to pipeline domain
`git mv src/extraction/orchestrator.py src/pipeline/orchestrator.py` — no content changes, pure rename. The file was misplaced in the extraction domain since it coordinates all pipeline handlers, not just extraction logic.

### Task 2 — Update 3 import sites
- `src/main.py`: updated import path + added `JobPostingHandler`, `WorkerGoalHandler`, `UnknownMessageHandler` imports
- `src/temporal/activities.py`: updated `TYPE_CHECKING` import path
- `tests/test_pipeline_orchestrator.py`: updated local import path

### Task 3 — Fix DI bug in lifespan
The original `PipelineOrchestrator(...)` call passed repository classes (not instances) and no `handlers` list. Replaced with explicit instantiation of `AuditLogRepository`, then three handler instances sharing the `audit_repo` instance, then `PipelineOrchestrator(extraction_service=..., audit_repo=..., handlers=handlers)`. Also removed the now-unused `from src.sms.repository import MessageRepository` import.

### Task 4 — Delete tests/inngest/
Removed `tests/inngest/__init__.py`, `test_process_message.py`, `test_process_message_spans.py`, `test_sync_pinecone_queue.py`. These were stubs left over from the Inngest-to-Temporal migration (Phase 02.9) and had already been superseded by `tests/temporal/`.

### Task 5 — Full test suite
92 tests pass, 1 skipped. No new failures introduced.

## Deviations from Plan

None — plan executed exactly as written. Handler constructor signatures matched the documented signatures exactly.

## Self-Check: PASSED

- src/pipeline/orchestrator.py: exists (renamed in git)
- src/main.py: updated import + DI wiring confirmed
- tests/inngest/: deleted
- All commits present: e11f6a6, e263d24, 862c648, ecb2349
