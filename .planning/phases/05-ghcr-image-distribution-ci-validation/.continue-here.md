---
context: phase
phase: 05-ghcr-image-distribution-ci-validation
workflow: /gsd-plan-phase 5
workflow_step: 12 — revision loop iteration 1/3 (next action)
status: paused_during_planning
last_updated: 2026-05-02T09:48:33.548Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These persist across the revision pass. Do NOT regress them.

- [ ] CONSTRAINT: **Attestation jq predicate** — Plans 05-01..05-04 already use the CORRECTED predicate from `05-RESEARCH.md` §Code Examples §Verify job: `jq -e '[.manifests[] | select(.annotations["vnd.docker.reference.type"] == "attestation-manifest")] | length >= 2'`. Do NOT regress to `mediaType == "application/vnd.in-toto+json"` (which appears as a stale hint in CONTEXT.md D-14 / §Specifics).
- [ ] CONSTRAINT: **Action version pins** — Plans pin to the verified-2026-05-01 set: `actions/checkout@v6`, `docker/setup-buildx-action@v4`, `docker/login-action@v4`, `docker/metadata-action@v6`, `docker/build-push-action@v7`, `actions/upload-artifact@v7`, `actions/download-artifact@v8`. Do NOT regress to the v5/v6 stale hint in CONTEXT.md "Claude's Discretion".
- [ ] CONSTRAINT: **Revision is targeted, not from-scratch** — Per the plan-phase workflow §12: "Make targeted updates to address checker issues. Do NOT replan from scratch unless issues are fundamental." All 5 findings are acceptance-criteria fixes confined to specific lines in 05-03 and 05-04. Plans 05-01 and 05-02 should be untouched.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Stale-CONTEXT regression | Planner regressing to CONTEXT.md's incorrect attestation predicate or stale action pins during a revision pass because the revision_context emphasizes the checker findings without re-stating the corrections | advisory | Always include the two CONTEXT.md corrections in the revision prompt's `<files_to_read>` and as explicit "do not regress" notes — the checker findings are about acceptance criteria, NOT about the underlying action body which is correct. |
| Acceptance-criteria-as-code drift | Authoring grep/awk one-liners in plan acceptance criteria without testing them against the action's actual emitted YAML — leads to false-negatives that block executor completion | advisory | When revising acceptance criteria, paste the exact substring from the action body and grep -F it (literal match) rather than re-deriving the substring from memory. The 5 checker findings are all instances of this anti-pattern. |

<current_state>
We're paused inside /gsd-plan-phase 5, immediately after gsd-plan-checker returned `## ISSUES FOUND` with 1 BLOCKER + 4 WARNINGS + 1 INFO. The 4 PLAN.md files (05-01..05-04) are written to disk but UNCOMMITTED (intentionally — the workflow commits at §13d after coverage gates pass). All checker findings are confined to malformed acceptance-criteria shell expressions in 05-03 and 05-04 — the underlying action bodies, frontmatter, threat models, requirement coverage, and decision coverage are all sound.

The next workflow action is §12 revision loop iteration 1/3: spawn gsd-planner in revision mode with the 5 findings, then re-spawn gsd-plan-checker.
</current_state>

<completed_work>

Completed in this session:
- ✅ Loaded init JSON for Phase 5 — confirmed CONTEXT.md present, no RESEARCH.md, no plans, requirements CI-01..CI-04
- ✅ Asked user about research at the research gate; user chose "Research first"
- ✅ Spawned gsd-phase-researcher → wrote `05-RESEARCH.md` (commit `3fcf7fa`). Surfaced TWO CONTEXT.md corrections that the planner later applied: corrected attestation jq predicate and current action version pins
- ✅ Created `05-VALIDATION.md` from template with 12 verification samples mapped to CI-01..CI-04 + folded D-14 + manual operator actions D-08/D-18 (commit `ac4ed81`)
- ✅ Spawned gsd-pattern-mapper → wrote `05-PATTERNS.md` mapping each Phase 5 deliverable to its closest in-repo analog with line-numbered excerpts; confirmed D-17 secret scrub satisfied transitively by D-16 deletions (commit `a18d93d`)
- ✅ Spawned gsd-planner → wrote 4 PLAN.md files (untracked):
  - `05-01-PLAN.md` (Wave 1, 2 tasks): docker-compose.prod.yml stub + local validation. Reqs: CI-02, CI-03
  - `05-02-PLAN.md` (Wave 1, 1 task): delete cd-base/dev/staging/prod.yml. Reqs: CI-04
  - `05-03-PLAN.md` (Wave 2, 3 tasks): extend ci.yml with compose-validate + build matrix + merge + verify jobs. Reqs: CI-01, CI-02, CI-03
  - `05-04-PLAN.md` (Wave 3, 1 auto + 1 checkpoint:human-action): README.md operator runbook for D-08 GHCR public toggle and D-18 branch protection update. Reqs: CI-01, CI-04
- ✅ Spawned gsd-plan-checker → returned `## ISSUES FOUND`:
  - 1 BLOCKER (05-03 line 410: malformed grep substring for matrix ternary)
  - 4 WARNINGS (05-03 lines 242, 424, 542; 05-04 line 215 — all malformed shell expressions in acceptance criteria)
  - 1 INFO (05-03 lines 211-218: minor markdown indentation; non-issue)
  - All structural quality-gate checks PASS: every requirement covered, every D-ID honored, wave graph clean, threat models present, action pins / attestation predicate / no-:latest / no-QEMU / cancel-in-progress / fail-loud GIT_SHA all correct
</completed_work>

<remaining_work>

- [ ] §12 Revision iteration 1/3 — spawn gsd-planner in revision mode with the 5 findings (BLOCKER + 4 WARNINGS) listed verbatim. Pass `iteration_count=1`, `prev_issue_count=Infinity`, `stall_reentry_count=0`. Per workflow: "Make targeted updates to address checker issues. Do NOT replan from scratch."
- [ ] §10 Re-spawn gsd-plan-checker after revision returns
- [ ] §13 Requirements coverage gate — verify CI-01..CI-04 all appear in plan frontmatter `requirements` fields (already true)
- [ ] §13a Decision coverage gate — verify D-01..D-18 all referenced in some plan's `must_haves.truths` or `truths` (already true per checker)
- [ ] §13b Record planning completion in STATE.md via `gsd-sdk query state.planned-phase --phase 5 --name "GHCR Image Distribution & CI Validation" --plans 4`
- [ ] §13c Annotate ROADMAP.md with wave dependencies + cross-cutting constraints via `gsd-sdk query roadmap.annotate-dependencies 5`
- [ ] §13d Commit plans + STATE.md + ROADMAP.md (commit_docs is true)
- [ ] §13e Post-planning gap analysis via `node $HOME/.claude/get-shit-done/bin/gsd-tools.cjs gap-analysis --phase-dir <dir>`
- [ ] §14 Present final status via `<offer_next>` (auto_advance is false)
</remaining_work>

<decisions_made>

- Selected "Research first" at the research gate (CONTEXT.md left Discretion items that needed grounding)
- Two CONTEXT.md corrections must persist across revision: (a) attestation predicate uses annotation, not in-toto mediaType; (b) action pins are v4/v6/v7/v8 set, not stale v5/v6
- UI safety gate skipped per project config (`workflow.ui_phase=false`, `workflow.ui_safety_gate=false`)
- Schema-push gate skipped — no ORM files in scope
- Security threat model gate active (ASVS L1, block on high) — all 4 plans pass; T-05-18 (attestation predicate correctness) and T-05-19/22/23 (branch-protection drift) are the high-severity threats and have explicit mitigations
</decisions_made>

<blockers>
None — the BLOCKER is a checker finding to fix in revision, not an external blocker.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — structured handoff with full revision findings
2. `.planning/phases/05-ghcr-image-distribution-ci-validation/05-RESEARCH.md` §Code Examples §Verify job — corrected attestation jq predicate (the planner must NOT regress to the in-toto mediaType during revision)
3. `.planning/phases/05-ghcr-image-distribution-ci-validation/05-RESEARCH.md` §Standard Stack — verified action version pins
4. `.planning/phases/05-ghcr-image-distribution-ci-validation/05-CONTEXT.md` — 18 locked decisions (note: D-14 attestation predicate hint is INCORRECT; see §Specifics for the same stale value)
5. `.planning/phases/05-ghcr-image-distribution-ci-validation/05-VALIDATION.md` — verification samples per requirement
6. `.planning/phases/05-ghcr-image-distribution-ci-validation/05-PATTERNS.md` — analog file mappings
7. The 4 PLAN.md files (untracked): `05-01-PLAN.md`, `05-02-PLAN.md`, `05-03-PLAN.md`, `05-04-PLAN.md`
8. `$HOME/.claude/get-shit-done/workflows/plan-phase.md` §12 (revision loop) — exact iteration tracking + stall detection logic

## Critical Anti-Patterns (do NOT repeat)

- **ANTI-PATTERN: Replanning from scratch on a checker bounce** — The 5 findings are all confined to specific acceptance-criteria lines in 05-03 and 05-04. The planner's revision prompt must list them with `<plan_id>:<line>` precision and the literal `fix_hint` from each finding, not a vague "fix checker issues" instruction. → Structural mitigation: pass the verbatim `checker_findings_to_fix` array from `.planning/HANDOFF.json` into the revision prompt.
- **ANTI-PATTERN: Regressing CONTEXT.md corrections during revision** — A revision pass that focuses only on the checker findings can accidentally lose the attestation-predicate / action-pin corrections if the planner re-derives a section from CONTEXT.md instead of the existing PLAN.md. → Structural mitigation: include in the revision `<files_to_read>` block the existing PLAN.md files AND `05-RESEARCH.md` §Code Examples + §Standard Stack, with explicit "do not regress" notes.
- **ANTI-PATTERN: Acceptance-criteria-as-code drift** — All 5 findings are grep/awk one-liners written without testing against the action's actual emitted YAML. The fix_hints in HANDOFF.json's `checker_findings_to_fix` array specify the corrected expressions; copy them verbatim.

## Infrastructure State

- Git branch: `main`
- Recent commits (this session): `a18d93d` patterns, `ac4ed81` validation, `3fcf7fa` research
- Untracked (intentional, will be committed at §13d): the 4 PLAN.md files
- No background processes
- Plan-checker iteration counter: starts at 1 on resume (one round of check already complete; max 3 per workflow §12)

## Pre-Execution Critique Required

N/A — we're inside the plan-revision loop, not between design and execution.

<context>
The session is in a clean state to resume. The single non-trivial decision pending is whether to spawn the planner in revision mode (recommended — fast, targeted) or whether the BLOCKER warrants a full replan from step 8 (NOT recommended — the checker explicitly noted "all structural checks pass; only acceptance-criteria expressions are malformed").

The mental model: plans are structurally correct; we're polishing the verification scripts that the executor will run inside /gsd-execute-phase. Each fix is a one-line edit. Total expected revision turnaround: ~2 minutes planner, ~30s checker re-run.
</context>

<next_action>
Resume with: `/gsd-resume-work` (then continue /gsd-plan-phase 5 from step 12).

Concretely: spawn gsd-planner with revision_context. The revision prompt MUST:

1. List `<files_to_read>` including the 4 untracked PLAN.md files, `05-RESEARCH.md` (§Code Examples + §Standard Stack — for the attestation predicate and action pins), `05-CONTEXT.md` (for D-IDs reference), and `$HOME/.claude/get-shit-done/workflows/plan-phase.md` §12 (for the iteration semantics).
2. Inline the 6 entries from `.planning/HANDOFF.json` → `checker_findings_to_fix` verbatim (id, severity, plan, task, line, issue, fix_hint).
3. Explicitly forbid regressing the two CONTEXT.md corrections (attestation predicate, action pins).
4. Instruct: "Make targeted edits to ONLY the specified lines in 05-03-PLAN.md and 05-04-PLAN.md. Do not modify 05-01-PLAN.md, 05-02-PLAN.md, frontmatter, action bodies, threat models, must_haves, or requirements."
5. Return marker: `## REVISION COMPLETE` with a one-line summary per fix.

After planner returns, re-spawn gsd-plan-checker with the same context as the first pass (no need for revision-specific instructions to the checker — it always runs the full quality-gate sweep). If checker returns `## VERIFICATION PASSED`, proceed to step 13 (coverage gates → STATE.md → ROADMAP annotation → commit → post-planning gap analysis → final status). If it returns more issues, increment iteration_count to 2 and continue (max 3).
</next_action>
