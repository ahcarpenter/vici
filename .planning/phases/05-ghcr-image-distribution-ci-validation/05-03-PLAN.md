---
phase: 5
plan: 3
type: execute
wave: 2
depends_on: [1, 2]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
must_haves:
  truths:
    - "Push to `main` triggers `ci.yml` jobs that build linux/amd64 + linux/arm64 images on native runners and push them to `ghcr.io/<owner>/vici:sha-<short>` (no `:latest`, no other tag)"
    - "Every push and pull request runs the `compose-validate` job, which executes `docker compose -f docker-compose.yml config --quiet` AND `docker compose -f docker-compose.yml -f docker-compose.prod.yml config --quiet` (with GIT_SHA exported); either failing returns non-zero and blocks the merge"
    - "After build/merge, the `verify` job runs WITHOUT GHCR push credentials, executes `docker buildx imagetools inspect --raw <ref>`, and asserts both `linux/amd64` and `linux/arm64` platforms are present in the index AND at least 2 attestation manifests are present (using the corrected `vnd.docker.reference.type=attestation-manifest` annotation predicate per RESEARCH.md §Pitfall 3, NOT the stale `mediaType == application/vnd.in-toto+json` predicate from CONTEXT.md D-14)"
    - "SBOM (`sbom: true`) and SLSA provenance (`provenance: mode=max`) are enabled on each per-arch build per D-14"
    - "PRs do not build images (`if: github.event_name != 'pull_request'` on build/merge/verify); PRs DO run `test` and `compose-validate` (the merge gate)"
    - "Concurrency block uses `group: ghcr-${{ github.ref }}` with `cancel-in-progress: false` per D-12 — every `main` SHA gets a published image (rollback invariant)"
    - "GitHub Action versions are pinned to the corrected v4/v6/v7/v8 set per RESEARCH.md §Standard Stack (NOT the stale v5/v6 from CONTEXT.md)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Extended CI workflow with 5 new jobs added as siblings of `test`: `compose-validate`, `build`, `merge`, `verify` (the build job is a 2-row matrix on `[linux/amd64, linux/arm64]`)"
      contains: "docker/build-push-action@v7"
  key_links:
    - from: "build (matrix) → merge"
      to: "actions/upload-artifact@v7 / actions/download-artifact@v8 with pattern: digests-*"
      via: "Per-arch digest passing — each build job writes /tmp/digests/<sha> as `digests-${PLATFORM_PAIR}` artifact; merge downloads all `digests-*` with `merge-multiple: true`"
      pattern: "name: digests-\\$\\{\\{ env.PLATFORM_PAIR \\}\\}"
    - from: "merge → GHCR"
      to: "docker buildx imagetools create"
      via: "Manifest assembly — combines per-arch digests into a multi-arch manifest list under the SHA tag from metadata-action"
      pattern: "docker buildx imagetools create"
    - from: "verify → GHCR (anonymous pull)"
      to: "docker buildx imagetools inspect --raw + jq"
      via: "Anonymous inspection — verify job has NO `docker/login-action` and NO `permissions: packages: write` (proves the public-toggle worked per D-08)"
      pattern: "imagetools inspect --raw"
---

<objective>
Extend `.github/workflows/ci.yml` with 5 new jobs that together implement CI-01 (multi-arch GHCR image build/push on `main`/tags/dispatch), CI-02 (SHA-only tagging via `docker/metadata-action@v6`, no `:latest`), and CI-03 (compose-validate as a hard merge gate on every push and PR). The existing `test` job (lint/format/pip-audit/pytest) is preserved untouched per D-15.

Purpose: Phase 5's central deliverable. After this plan completes, every push to `main` produces an immutable, SHA-pinned, multi-arch GHCR image with attached SBOM + SLSA provenance attestations, and every push/PR validates both compose overlays before merge.

Output: Single edit to `.github/workflows/ci.yml` adding (in order under the same `jobs:` map):
1. `compose-validate` job — runs on push + PR; static-parses both overlays
2. `build` job — matrix over `[linux/amd64, linux/arm64]`, gated on non-PR; per-arch native runner; push-by-digest with sbom + provenance
3. `merge` job — needs: [build]; downloads per-arch digests; assembles multi-arch manifest via `imagetools create`
4. `verify` job — needs: [merge]; anonymous (no GHCR creds); asserts both platforms + ≥2 attestation manifests

Plus: workflow-level `on:` extension (add `tags: ['v*']` and `workflow_dispatch`), workflow-level `concurrency:` block, and workflow-level `permissions:` defaults.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ghcr-image-distribution-ci-validation/05-CONTEXT.md
@.planning/phases/05-ghcr-image-distribution-ci-validation/05-RESEARCH.md
@.planning/phases/05-ghcr-image-distribution-ci-validation/05-PATTERNS.md
@.planning/phases/05-ghcr-image-distribution-ci-validation/05-VALIDATION.md
@.planning/phases/05-ghcr-image-distribution-ci-validation/05-01-SUMMARY.md
@.planning/phases/05-ghcr-image-distribution-ci-validation/05-02-SUMMARY.md
@.github/workflows/ci.yml
@docker-compose.prod.yml
@Dockerfile

<interfaces>
<!-- The five new jobs use these GitHub Actions, version-pinned per RESEARCH.md §Standard Stack -->
<!-- (verified 2026-05-01 via GitHub Releases API). DO NOT use the stale v5/v6 hints in CONTEXT.md. -->

Required action versions (from RESEARCH.md §Standard Stack, lines 100-106):
- actions/checkout@v6                  (v6.0.2, 2026-01-09)
- docker/setup-buildx-action@v4        (v4.0.0, 2026-03-05)
- docker/login-action@v4               (v4.1.0, 2026-04-02)
- docker/metadata-action@v6            (v6.0.0, 2026-03-05)
- docker/build-push-action@v7          (v7.1.0, 2026-04-10)
- actions/upload-artifact@v7           (v7.0.1, 2026-04-10)
- actions/download-artifact@v8         (v8.0.1, 2026-03-11)

Existing `test` job uses `astral-sh/setup-uv@v5` — leave untouched.

<!-- Existing ci.yml structure to preserve verbatim (per D-15 — `test:` job stays exactly as-is): -->
<!-- ci.yml lines 1-7 (current top-level on:):                                                  -->
<!--   on:                                                                                     -->
<!--     push:                                                                                 -->
<!--       branches: [main]                                                                    -->
<!--     pull_request:                                                                         -->
<!--       branches: [main]                                                                    -->
<!--                                                                                          -->
<!-- ci.yml lines 9-47 (current `test` job):                                                  -->
<!--   jobs:                                                                                   -->
<!--     test:                                                                                 -->
<!--       runs-on: ubuntu-latest                                                              -->
<!--       steps: [checkout@v4, setup-uv@v5, install, lint, format check, pip-audit, pytest]   -->
<!--   (full body preserved — Plan 03 only ADDS new jobs as siblings)                          -->

<!-- Corrected jq predicates for verify job (RESEARCH.md §Code Examples §Verify job, lines 502-559): -->

Platform-presence assertion:
```bash
ARCHES=$(jq -r '
  [.manifests[]
    | select(.platform.architecture != "unknown")
    | .platform.architecture] | sort | join(",")
' /tmp/manifest.json)
test "$ARCHES" = "amd64,arm64"
```

Attestation-presence assertion (CORRECTED — CONTEXT.md D-14 has this WRONG):
```bash
ATT_COUNT=$(jq '
  [.manifests[]
    | select(.annotations["vnd.docker.reference.type"] == "attestation-manifest")
  ] | length
' /tmp/manifest.json)
test "$ATT_COUNT" -ge 2
```
The annotation is `vnd.docker.reference.type=attestation-manifest`. The mediaType
is `application/vnd.oci.image.manifest.v1+json` (same as platform manifests).
Filtering on `mediaType == "application/vnd.in-toto+json"` returns zero matches and
masks real failures. See RESEARCH.md §Pitfall 3.

<!-- Compose-validate canonical pattern (RESEARCH.md §Code Examples §Compose validate job, lines 563-579): -->

```yaml
compose-validate:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v6

    - name: Validate base alone
      run: docker compose -f docker-compose.yml config --quiet

    - name: Validate base + prod (with GIT_SHA exported)
      run: |
        export GIT_SHA=$(git rev-parse --short=7 HEAD)
        docker compose -f docker-compose.yml -f docker-compose.prod.yml config --quiet
```

<!-- DO NOT add `continue-on-error: true` — compose-validate is a hard merge gate per CI-03. -->
<!-- The 7-char short SHA via `git rev-parse --short=7 HEAD` matches the `cd-base.yml:49` precedent (now deleted by Plan 02) and D-04. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `compose-validate` job + workflow-level on/permissions/concurrency to ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current state — confirm `test` job at lines 9-47 is preserved verbatim per D-15; current `on:` block at lines 3-7 needs extension)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-RESEARCH.md §Code Examples §Compose validate job (lines 563-579 — verbatim canonical YAML)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-RESEARCH.md §Code Examples §Concurrency block (lines 591-600 — `cancel-in-progress: false` rationale)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-PATTERNS.md §"compose-validate job" — style match against `ci.yml:29-33` shell-step pattern (multi-line `run: |`, NO `continue-on-error: true`)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-CONTEXT.md (D-06 triggers, D-12 concurrency, D-15 single-workflow extension)
    - docker-compose.prod.yml (created by Plan 01 — must exist before this job's second step can succeed in CI)
  </read_first>
  <action>
    Edit `.github/workflows/ci.yml` to make THREE workflow-level changes plus add ONE new job. Preserve the existing `test:` job (lines 9-47) byte-for-byte per D-15.

    **Change 1 — Extend `on:` triggers (per D-06):**
    Replace the current `on:` block (lines 3-7) with:
    ```yaml
    on:
      push:
        branches: [main]
        tags: ['v*']
      pull_request:
        branches: [main]
      workflow_dispatch:
    ```
    Rationale: D-06 mandates push-to-main, push-to-tags-`v*`, manual `workflow_dispatch`, AND PRs (PRs trigger lint/test/compose-validate but NOT image build — gating is per-job via `if: github.event_name != 'pull_request'` for build/merge/verify, see Tasks 2 and 3).

    **Change 2 — Add workflow-level `permissions:` defaults (Pitfall 7):**
    Add immediately after the `on:` block (before `jobs:`):
    ```yaml
    permissions:
      contents: read
    ```
    This sets a least-privilege default. Jobs that need elevated scopes (`packages: write`, `attestations: write`, `id-token: write` on build/merge) declare them at job level, NOT here. The `verify` job inherits the default `contents: read` and adds nothing — it must be anonymous.

    **Change 3 — Add workflow-level `concurrency:` block (per D-12):**
    Add immediately after the `permissions:` block (before `jobs:`):
    ```yaml
    concurrency:
      # D-12: serialize publish pipeline per ref. cancel-in-progress is FALSE
      # because every `main` SHA must publish an image (rollback invariant for
      # the SHA-pinned prod overlay). The `test` job is also serialized here;
      # the ~30s back-to-back-push wait is acceptable to avoid splitting into
      # two workflows.
      group: ghcr-${{ github.ref }}
      cancel-in-progress: false
    ```

    **Change 4 — Add `compose-validate` job:**
    Append the following AS A SIBLING of `test:` under the existing `jobs:` map. Place it immediately after the `test:` job's last line (current line 47), with one blank line separator. Do NOT modify `test:` itself.
    ```yaml
      compose-validate:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v6

          - name: Validate base alone
            run: docker compose -f docker-compose.yml config --quiet

          - name: Validate base + prod (with GIT_SHA exported)
            run: |
              export GIT_SHA=$(git rev-parse --short=7 HEAD)
              docker compose -f docker-compose.yml -f docker-compose.prod.yml config --quiet
      ```
    Notes:
    - The job runs on EVERY trigger (no `if:` gate) — it is the merge gate per CI-03 and must run on PRs too.
    - NO `continue-on-error: true` — failure must fail the workflow (CI-03 mandates non-zero exit blocks merge).
    - The `git rev-parse --short=7 HEAD` invocation matches the byte-for-byte precedent from the now-deleted `cd-base.yml:49` (per D-04).
    - 2-space indentation, 4-space step-key indentation (matches `ci.yml:11-14` style).
  </action>
  <verify>
    <automated>grep -F "tags: ['v*']" .github/workflows/ci.yml && grep -F 'workflow_dispatch:' .github/workflows/ci.yml && grep -F 'group: ghcr-${{ github.ref }}' .github/workflows/ci.yml && grep -F 'cancel-in-progress: false' .github/workflows/ci.yml && grep -F 'compose-validate:' .github/workflows/ci.yml && grep -F 'docker compose -f docker-compose.yml config --quiet' .github/workflows/ci.yml && grep -F 'docker compose -f docker-compose.yml -f docker-compose.prod.yml config --quiet' .github/workflows/ci.yml && grep -F 'actions/checkout@v6' .github/workflows/ci.yml && grep -F 'export GIT_SHA=$(git rev-parse --short=7 HEAD)' .github/workflows/ci.yml && grep -c '^  test:$' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F "tags: ['v*']" .github/workflows/ci.yml` exits 0 (D-06 tag trigger)
    - `grep -F 'workflow_dispatch:' .github/workflows/ci.yml` exits 0 (D-06 manual dispatch)
    - `grep -cE '^on:$' .github/workflows/ci.yml` outputs `1` (single top-level `on:` block)
    - `grep -F 'group: ghcr-${{ github.ref }}' .github/workflows/ci.yml` exits 0 (D-12 concurrency group)
    - `grep -F 'cancel-in-progress: false' .github/workflows/ci.yml` exits 0 (D-12 rollback invariant)
    - `grep -cE '^permissions:$' .github/workflows/ci.yml` outputs `1` (workflow-level permissions block)
    - `grep -F 'compose-validate:' .github/workflows/ci.yml` exits 0 (new job present)
    - `grep -F 'docker compose -f docker-compose.yml config --quiet' .github/workflows/ci.yml` exits 0 (base-alone validation)
    - `grep -F 'docker compose -f docker-compose.yml -f docker-compose.prod.yml config --quiet' .github/workflows/ci.yml` exits 0 (overlay validation)
    - `grep -F 'export GIT_SHA=$(git rev-parse --short=7 HEAD)' .github/workflows/ci.yml` exits 0 (D-04 SHA-extraction precedent)
    - `grep -F 'actions/checkout@v6' .github/workflows/ci.yml` exits 0 (corrected version pin per RESEARCH.md §Standard Stack)
    - `grep -c '^  test:$' .github/workflows/ci.yml` outputs `1` (`test:` job preserved per D-15)
    - `grep -F 'astral-sh/setup-uv@v5' .github/workflows/ci.yml` exits 0 (existing `test` job's dependency unchanged)
    - `grep -F 'continue-on-error: true' .github/workflows/ci.yml | grep -v pip-audit` exits 1 — `continue-on-error: true` appears ONLY on the existing pip-audit step, NOT on compose-validate
    - `grep -E 'docker/build-push-action|docker/metadata-action|docker/setup-buildx-action|docker/login-action|actions/upload-artifact|actions/download-artifact' .github/workflows/ci.yml` exits 1 at this point (Tasks 2 and 3 add those — Task 1 only touches compose-validate which uses none of them)
  </acceptance_criteria>
  <done>
    `ci.yml` `on:` block extended with `tags: ['v*']` and `workflow_dispatch:`. Workflow-level `permissions: { contents: read }` and `concurrency: { group: ghcr-${{ github.ref }}, cancel-in-progress: false }` blocks added. New `compose-validate` job appended as sibling of `test:` runs both `docker compose ... config --quiet` invocations as a hard merge gate (no `continue-on-error`). The existing `test:` job is preserved verbatim per D-15. Action version pin uses `actions/checkout@v6` per RESEARCH.md §Standard Stack.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add `build` matrix job + `merge` job to ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (state after Task 1 — confirm `compose-validate` is present and `test:` is unchanged)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-RESEARCH.md §Code Examples §Per-arch build matrix step (lines 377-450 — verbatim canonical YAML; THIS IS THE SOURCE OF TRUTH)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-RESEARCH.md §Code Examples §Merge job (lines 452-500 — verbatim canonical YAML)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-CONTEXT.md (D-07 SHA-only tagging, D-10 native arm64 runner, D-11 per-arch GHA cache scope, D-13 lowercased owner, D-14 sbom + provenance)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-PATTERNS.md §"build-amd64 / build-arm64 jobs" (diff summary table — what to keep from cd-base.yml patterns, what to replace; permissions block extension)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-RESEARCH.md §Common Pitfalls §Pitfall 1 (GHCR lowercase requirement), §Pitfall 4 (cache mode=max), §Pitfall 5 (per-arch scope), §Pitfall 7 (permissions: packages: write + attestations: write), §Pitfall 8 (`ubuntu-24.04-arm` label spelling)
  </read_first>
  <action>
    Append two new jobs to `.github/workflows/ci.yml` AS SIBLINGS of `test:` and `compose-validate:` under the existing `jobs:` map. Place them after `compose-validate:` in the order: `build:` then `merge:`. Preserve all prior content untouched.

    **Job 2 — `build` matrix job** (verbatim from RESEARCH.md §Code Examples §Per-arch build matrix step, lines 382-449, with the job key renamed from `build:` per the canonical source — keep `build:` as the job name since the merge job's `needs: [build]` references it):

    ```yaml
      build:
        strategy:
          fail-fast: false
          matrix:
            platform: [linux/amd64, linux/arm64]
        runs-on: ${{ matrix.platform == 'linux/amd64' && 'ubuntu-latest' || 'ubuntu-24.04-arm' }}
        if: github.event_name != 'pull_request'
        permissions:
          contents: read
          packages: write
          attestations: write
          id-token: write
        steps:
          - name: Prepare platform pair
            run: |
              platform=${{ matrix.platform }}
              echo "PLATFORM_PAIR=${platform//\//-}" >> $GITHUB_ENV
              echo "ARCH=${platform#linux/}" >> $GITHUB_ENV

          - uses: actions/checkout@v6

          - name: Lowercase owner for GHCR
            run: |
              echo "OWNER_LC=$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV
              echo "GHCR_IMAGE=ghcr.io/$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')/vici" >> $GITHUB_ENV

          - name: Docker meta
            id: meta
            uses: docker/metadata-action@v6
            with:
              images: ${{ env.GHCR_IMAGE }}
              tags: |
                type=sha,prefix=sha-,format=short

          - uses: docker/setup-buildx-action@v4

          - uses: docker/login-action@v4
            with:
              registry: ghcr.io
              username: ${{ github.actor }}
              password: ${{ secrets.GITHUB_TOKEN }}

          - name: Build and push by digest
            id: build
            uses: docker/build-push-action@v7
            with:
              context: .
              platforms: ${{ matrix.platform }}
              labels: ${{ steps.meta.outputs.labels }}
              outputs: type=image,name=${{ env.GHCR_IMAGE }},push-by-digest=true,name-canonical=true,push=true
              cache-from: type=gha,scope=${{ env.ARCH }}
              cache-to: type=gha,mode=max,scope=${{ env.ARCH }}
              provenance: mode=max
              sbom: true

          - name: Export digest
            run: |
              mkdir -p /tmp/digests
              digest="${{ steps.build.outputs.digest }}"
              touch "/tmp/digests/${digest#sha256:}"

          - uses: actions/upload-artifact@v7
            with:
              name: digests-${{ env.PLATFORM_PAIR }}
              path: /tmp/digests/*
              if-no-files-found: error
              retention-days: 1
    ```

    **Job 3 — `merge` job** (verbatim from RESEARCH.md §Code Examples §Merge job, lines 455-499):

    ```yaml
      merge:
        needs: [build]
        runs-on: ubuntu-latest
        if: github.event_name != 'pull_request'
        permissions:
          contents: read
          packages: write
        steps:
          - uses: actions/download-artifact@v8
            with:
              path: /tmp/digests
              pattern: digests-*
              merge-multiple: true

          - name: Lowercase owner
            run: |
              echo "GHCR_IMAGE=ghcr.io/$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')/vici" >> $GITHUB_ENV

          - name: Docker meta (for tag list)
            id: meta
            uses: docker/metadata-action@v6
            with:
              images: ${{ env.GHCR_IMAGE }}
              tags: |
                type=sha,prefix=sha-,format=short

          - uses: docker/setup-buildx-action@v4

          - uses: docker/login-action@v4
            with:
              registry: ghcr.io
              username: ${{ github.actor }}
              password: ${{ secrets.GITHUB_TOKEN }}

          - name: Create multi-arch manifest
            working-directory: /tmp/digests
            run: |
              docker buildx imagetools create \
                $(jq -cr '.tags | map("-t " + .) | join(" ")' <<< "$DOCKER_METADATA_OUTPUT_JSON") \
                $(printf '${{ env.GHCR_IMAGE }}@sha256:%s ' *)

          - name: Inspect (sanity)
            run: |
              docker buildx imagetools inspect '${{ env.GHCR_IMAGE }}:${{ steps.meta.outputs.version }}'
    ```

    **Critical correctness invariants to preserve when copying:**
    - `runs-on:` ternary expression spelling: `'ubuntu-24.04-arm'` (not `ubuntu-22.04-arm`, not `arm64`) — Pitfall 8
    - `cache-from:` and `cache-to:` BOTH have `scope=${{ env.ARCH }}` — Pitfall 5
    - `cache-to:` has `mode=max` (not `mode=min`) — Pitfall 4
    - `outputs:` line has `push-by-digest=true,name-canonical=true,push=true` — required for digest-only push that the merge job depends on
    - `provenance: mode=max` and `sbom: true` are BOTH present — D-14
    - `permissions:` on build job has all four: `contents: read, packages: write, attestations: write, id-token: write` — Pitfall 7
    - `permissions:` on merge job has only `contents: read, packages: write` (no attestations/id-token; merge doesn't build)
    - `needs: [build]` on merge job — wait for ALL matrix legs
    - The `imagetools create` shell command uses `<<<` (here-string) and `printf '...@sha256:%s '` to expand digest filenames — DO NOT modify this incantation; copy verbatim
    - Tag generation is via `docker/metadata-action@v6` with `type=sha,prefix=sha-,format=short` — the action handles `sha-<7char>` tag spec including for `v*` git-tag triggers automatically. NO `tags:` literal anywhere; NO `flavor: latest=true`.
    - No `:latest` literal anywhere — CI-02 invariant
  </action>
  <verify>
    <automated>grep -F 'docker/build-push-action@v7' .github/workflows/ci.yml && grep -F 'docker/metadata-action@v6' .github/workflows/ci.yml && grep -F 'docker/setup-buildx-action@v4' .github/workflows/ci.yml && grep -F 'docker/login-action@v4' .github/workflows/ci.yml && grep -F 'actions/upload-artifact@v7' .github/workflows/ci.yml && grep -F 'actions/download-artifact@v8' .github/workflows/ci.yml && grep -F 'platform: [linux/amd64, linux/arm64]' .github/workflows/ci.yml && grep -F 'ubuntu-24.04-arm' .github/workflows/ci.yml && grep -F 'type=sha,prefix=sha-,format=short' .github/workflows/ci.yml && grep -F 'provenance: mode=max' .github/workflows/ci.yml && grep -F 'sbom: true' .github/workflows/ci.yml && grep -F 'cache-to: type=gha,mode=max,scope=${{ env.ARCH }}' .github/workflows/ci.yml && grep -F 'push-by-digest=true,name-canonical=true,push=true' .github/workflows/ci.yml && grep -F 'docker buildx imagetools create' .github/workflows/ci.yml && grep -F 'needs: [build]' .github/workflows/ci.yml && ! grep -F ':latest' .github/workflows/ci.yml && ! grep -E 'flavor:.*latest=true' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -F 'docker/build-push-action@v7' .github/workflows/ci.yml` exits 0 (corrected pin per RESEARCH §Standard Stack — NOT v5/v6)
    - `grep -F 'docker/metadata-action@v6' .github/workflows/ci.yml` exits 0 (corrected pin)
    - `grep -F 'docker/setup-buildx-action@v4' .github/workflows/ci.yml` exits 0 (corrected pin)
    - `grep -F 'docker/login-action@v4' .github/workflows/ci.yml` exits 0 (corrected pin)
    - `grep -F 'actions/upload-artifact@v7' .github/workflows/ci.yml` exits 0 (corrected pin)
    - `grep -F 'actions/download-artifact@v8' .github/workflows/ci.yml` exits 0 (corrected pin)
    - `grep -F 'platform: [linux/amd64, linux/arm64]' .github/workflows/ci.yml` exits 0 (D-10 matrix)
    - `grep -F "ubuntu-24.04-arm" .github/workflows/ci.yml` exits 0 (D-10 native arm64 runner; correct label spelling per Pitfall 8)
    - `grep -F "ubuntu-latest && 'ubuntu-latest' || 'ubuntu-24.04-arm'" .github/workflows/ci.yml` exits 0 (matrix ternary expression)
    - `grep -F 'type=sha,prefix=sha-,format=short' .github/workflows/ci.yml` exits 0 (D-07 SHA-only tag spec)
    - `grep -c 'type=sha,prefix=sha-,format=short' .github/workflows/ci.yml` outputs `2` (metadata-action used in both build and merge)
    - `grep -F 'provenance: mode=max' .github/workflows/ci.yml` exits 0 (D-14 SLSA provenance)
    - `grep -F 'sbom: true' .github/workflows/ci.yml` exits 0 (D-14 SBOM)
    - `grep -F 'cache-to: type=gha,mode=max,scope=${{ env.ARCH }}' .github/workflows/ci.yml` exits 0 (D-11 per-arch scope + Pitfall 4 mode=max)
    - `grep -F 'cache-from: type=gha,scope=${{ env.ARCH }}' .github/workflows/ci.yml` exits 0 (D-11 per-arch cache read)
    - `grep -F 'push-by-digest=true,name-canonical=true,push=true' .github/workflows/ci.yml` exits 0 (digest-only push for merge consumption)
    - `grep -F 'docker buildx imagetools create' .github/workflows/ci.yml` exits 0 (manifest assembly via buildx, NOT legacy `docker manifest create`)
    - `grep -F 'needs: [build]' .github/workflows/ci.yml` exits 0 (merge waits for all matrix legs)
    - `grep -F "if: github.event_name != 'pull_request'" .github/workflows/ci.yml` exits 0 AND `grep -c "if: github.event_name != 'pull_request'" .github/workflows/ci.yml` outputs at least `2` (build and merge both gated; verify added in Task 3)
    - `grep -F 'packages: write' .github/workflows/ci.yml` exits 0 AND `grep -c 'packages: write' .github/workflows/ci.yml` outputs `2` (build job + merge job; verify in Task 3 must NOT have it)
    - `grep -F 'attestations: write' .github/workflows/ci.yml` exits 0 (Pitfall 7 — required for sbom/provenance on the build job)
    - `grep -F 'id-token: write' .github/workflows/ci.yml` exits 0 (build job — future-proofing for keyless signing)
    - `grep -F 'tr "[:upper:]" "[:lower:]"' .github/workflows/ci.yml` exits 0 OR `grep -F "tr '[:upper:]' '[:lower:]'" .github/workflows/ci.yml` exits 0 (D-13 lowercased owner — Pitfall 1)
    - `grep -F ':latest' .github/workflows/ci.yml` exits 1 (no `:latest` tag — CI-02 invariant)
    - `grep -E 'flavor:.*latest=true' .github/workflows/ci.yml` exits 1 (no metadata-action flavor that would auto-add `:latest`)
    - `grep -F 'docker manifest create' .github/workflows/ci.yml` exits 1 (legacy CLI not used — Anti-Pattern in RESEARCH.md)
    - `grep -F 'qemu' .github/workflows/ci.yml` exits 1 AND `grep -F 'setup-qemu-action' .github/workflows/ci.yml` exits 1 (D-10 forbids QEMU; native runners only)
    - `grep -c '^  test:$' .github/workflows/ci.yml` outputs `1` (still preserved per D-15)
    - `grep -c '^  compose-validate:$' .github/workflows/ci.yml` outputs `1` (Task 1's job preserved)
    - `grep -c '^  build:$' .github/workflows/ci.yml` outputs `1` (new build job)
    - `grep -c '^  merge:$' .github/workflows/ci.yml` outputs `1` (new merge job)
  </acceptance_criteria>
  <done>
    `build` matrix job (linux/amd64 on ubuntu-latest, linux/arm64 on ubuntu-24.04-arm; PR-skipped) and `merge` job (ubuntu-latest; needs: [build]; PR-skipped) appended as siblings of `test`/`compose-validate`. Both use the corrected action version pins (v4/v6/v7/v8 set). Per-arch GHA cache with `mode=max,scope=<arch>`. Push-by-digest pattern feeds digest artifacts into the merge job which assembles the multi-arch manifest via `docker buildx imagetools create`. SBOM + provenance attestations enabled per D-14. No `:latest` tag, no `flavor: latest=true`, no QEMU.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add `verify` job (anonymous, with corrected attestation predicate) to ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (state after Task 2 — confirm `build` and `merge` are present)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-RESEARCH.md §Code Examples §Verify job (lines 502-559 — VERBATIM SOURCE OF TRUTH for the corrected jq predicate; CONTEXT.md D-14's predicate is WRONG)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-RESEARCH.md §Common Pitfalls §Pitfall 2 (first-push visibility = private — verify job intentionally fails on first push so operator can toggle public per D-08)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-RESEARCH.md §Common Pitfalls §Pitfall 3 (attestation manifest mediaType misconception — the corrected predicate uses `vnd.docker.reference.type=attestation-manifest` annotation, NOT `mediaType == application/vnd.in-toto+json`)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-RESEARCH.md §Common Pitfalls §Pitfall 9 (verify job timing — manifest eventual consistency; 3-attempt retry with 10s sleep)
    - .planning/phases/05-ghcr-image-distribution-ci-validation/05-CONTEXT.md (D-09 separate verify job with no GHCR creds, D-14 attestation assertion)
  </read_first>
  <action>
    Append the `verify` job to `.github/workflows/ci.yml` AS A SIBLING of `test:`, `compose-validate:`, `build:`, and `merge:` under the existing `jobs:` map. Place it after `merge:` as the final job (so the file order matches the wave/dependency flow). Preserve all prior content untouched.

    **Job 4 — `verify` job** (verbatim from RESEARCH.md §Code Examples §Verify job, lines 504-559):

    ```yaml
      verify:
        needs: [merge]
        runs-on: ubuntu-latest
        if: github.event_name != 'pull_request'
        timeout-minutes: 10
        # NO permissions: packages: write — verify runs anonymously to prove
        # the public-toggle is in place (after first push + manual D-08 step)
        steps:
          - name: Lowercase owner
            run: |
              echo "GHCR_IMAGE=ghcr.io/$(echo '${{ github.repository_owner }}' | tr '[:upper:]' '[:lower:]')/vici" >> $GITHUB_ENV

          - name: Compute SHA tag
            id: tag
            run: echo "tag=sha-$(git rev-parse --short=7 ${{ github.sha }})" >> "$GITHUB_OUTPUT"

          - name: Inspect with retry (handle GHCR eventual consistency)
            id: inspect
            run: |
              IMAGE_REF="${{ env.GHCR_IMAGE }}:${{ steps.tag.outputs.tag }}"
              for attempt in 1 2 3; do
                if RAW=$(docker buildx imagetools inspect --raw "$IMAGE_REF" 2>&1); then
                  echo "$RAW" > /tmp/manifest.json
                  break
                fi
                echo "attempt $attempt failed, retrying in 10s..."
                sleep 10
              done
              test -s /tmp/manifest.json || { echo "manifest never appeared"; exit 1; }
              cat /tmp/manifest.json | jq .

          - name: Assert both platforms present
            run: |
              ARCHES=$(jq -r '
                [.manifests[]
                  | select(.platform.architecture != "unknown")
                  | .platform.architecture] | sort | join(",")
              ' /tmp/manifest.json)
              echo "Found platforms: $ARCHES"
              test "$ARCHES" = "amd64,arm64" || { echo "Missing platforms"; exit 1; }

          - name: Assert >=2 attestation manifests present
            # CORRECTED predicate (CONTEXT.md D-14's `mediaType == "application/vnd.in-toto+json"`
            # is wrong — attestation manifests share the OCI image manifest mediaType and are
            # identified by the `vnd.docker.reference.type=attestation-manifest` annotation
            # plus platform.architecture=unknown).
            run: |
              ATT_COUNT=$(jq '
                [.manifests[]
                  | select(.annotations["vnd.docker.reference.type"] == "attestation-manifest")
                ] | length
              ' /tmp/manifest.json)
              echo "Found attestation manifests: $ATT_COUNT (expected >= 2 — one SBOM + one provenance per platform)"
              test "$ATT_COUNT" -ge 2 || { echo "Attestation manifests missing"; exit 1; }
    ```

    **Critical correctness invariants:**
    - **NO `permissions:` block on this job** (or only `contents: read` inherited from workflow level). NO `packages: write`. NO `id-token: write`. NO `attestations: write`. The job MUST be anonymous to prove the GHCR public-toggle (D-08) is in place — Pitfall 2.
    - **NO `docker/login-action` step.** This is intentional and load-bearing: anonymous pull is what we are verifying.
    - **Use the CORRECTED jq predicate**: `select(.annotations["vnd.docker.reference.type"] == "attestation-manifest")`. DO NOT use `select(.mediaType == "application/vnd.in-toto+json")` from CONTEXT.md D-14 — it returns zero matches because attestation manifests share the OCI image manifest mediaType. Pitfall 3.
    - **3-attempt retry loop with 10s sleep** between attempts. Pitfall 9 — handles GHCR's eventual consistency between merge-push and verify-pull.
    - **`timeout-minutes: 10`** — covers cold-pull edge cases (CONTEXT.md "Claude's Discretion" recommends this).
    - **`needs: [merge]`** — verify must run AFTER manifest assembly is complete.
    - **`if: github.event_name != 'pull_request'`** — verify only runs when there is something to verify (PRs don't build/push images).
    - The shell uses literal `>=` in the echo string (NOT a unicode `≥`) for grep/portability hygiene; the `test "$ATT_COUNT" -ge 2` invocation is the actual comparison.

    First-push expected behavior: This job WILL fail on the very first push to `main` because the GHCR package is created private by default. The operator follows D-08 (manual public-toggle in GitHub Settings, see Plan 04 documentation), then re-triggers via `workflow_dispatch` to confirm verify passes. This is documented as expected behavior in Plan 04.
  </action>
  <verify>
    <automated>grep -F 'verify:' .github/workflows/ci.yml && grep -F 'needs: [merge]' .github/workflows/ci.yml && grep -F 'timeout-minutes: 10' .github/workflows/ci.yml && grep -F 'docker buildx imagetools inspect --raw' .github/workflows/ci.yml && grep -F 'select(.platform.architecture != "unknown")' .github/workflows/ci.yml && grep -F 'select(.annotations["vnd.docker.reference.type"] == "attestation-manifest")' .github/workflows/ci.yml && ! grep -F 'application/vnd.in-toto+json' .github/workflows/ci.yml && grep -F 'for attempt in 1 2 3' .github/workflows/ci.yml && grep -c 'packages: write' .github/workflows/ci.yml | grep -q '^2$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^  verify:$' .github/workflows/ci.yml` outputs `1` (new verify job present)
    - `grep -F 'needs: [merge]' .github/workflows/ci.yml` exits 0 (verify runs after merge)
    - `grep -F 'timeout-minutes: 10' .github/workflows/ci.yml` exits 0 (cold-pull guard)
    - `grep -F 'docker buildx imagetools inspect --raw' .github/workflows/ci.yml` exits 0 (anonymous inspect)
    - `grep -F 'select(.platform.architecture != "unknown")' .github/workflows/ci.yml` exits 0 (corrected platform-presence predicate)
    - `grep -F 'select(.annotations["vnd.docker.reference.type"] == "attestation-manifest")' .github/workflows/ci.yml` exits 0 (CORRECTED attestation-presence predicate per RESEARCH.md §Pitfall 3)
    - `grep -F 'application/vnd.in-toto+json' .github/workflows/ci.yml` exits 1 (the WRONG predicate from CONTEXT.md D-14 must NOT be present anywhere)
    - `grep -F 'for attempt in 1 2 3' .github/workflows/ci.yml` exits 0 (3-attempt retry loop per Pitfall 9)
    - `grep -F 'sleep 10' .github/workflows/ci.yml` exits 0 (10s retry interval)
    - `grep -F 'test "$ARCHES" = "amd64,arm64"' .github/workflows/ci.yml` exits 0 (platform assertion exit-on-mismatch)
    - `grep -F 'test "$ATT_COUNT" -ge 2' .github/workflows/ci.yml` exits 0 (attestation count assertion)
    - `grep -c 'packages: write' .github/workflows/ci.yml` outputs `2` (still ONLY 2 — build and merge; verify does NOT add a third)
    - `grep -F 'docker/login-action' .github/workflows/ci.yml | wc -l | tr -d ' '` outputs `2` (login appears in build and merge ONLY — verify does NOT log in)
    - `grep -c "if: github.event_name != 'pull_request'" .github/workflows/ci.yml` outputs `3` (build, merge, verify — all gated; compose-validate runs on PRs too as the merge gate)
    - `grep -c '^  test:$\|^  compose-validate:$\|^  build:$\|^  merge:$\|^  verify:$' .github/workflows/ci.yml` outputs `5` (all five jobs present)
    - End-to-end YAML parse sanity: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (the workflow YAML is well-formed)
  </acceptance_criteria>
  <done>
    `verify` job appended as the final sibling under `jobs:`. Job runs anonymously (no `docker/login-action`, no `packages: write` permission). Uses the CORRECTED attestation-manifest predicate (`vnd.docker.reference.type=attestation-manifest` annotation, NOT the WRONG `mediaType == application/vnd.in-toto+json` predicate from CONTEXT.md D-14). 3-attempt retry loop with 10s sleep handles GHCR eventual consistency. `timeout-minutes: 10` covers cold-pull edges. The complete `ci.yml` now has exactly 5 jobs: `test`, `compose-validate`, `build`, `merge`, `verify`. The workflow YAML is valid (parses without error).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub event → workflow runner | Push events trigger workflow runs; permissions block scopes what `GITHUB_TOKEN` can do |
| build job → GHCR | Per-arch build jobs push image layers + attestations using `secrets.GITHUB_TOKEN` with `packages: write` + `attestations: write` |
| merge job → GHCR | Manifest assembly uses `secrets.GITHUB_TOKEN` with `packages: write` (no attestation creation in this job, just manifest list write) |
| verify job → GHCR (anonymous) | Verify intentionally has NO credentials — proves the package public-toggle is in place |
| GHA cache (per-arch scope) → BuildKit | Per-arch cache scope prevents amd64/arm64 cache stomping across matrix legs |
| Untrusted PR head → workflow | PRs run `test` and `compose-validate` only (build/merge/verify gated by `if: github.event_name != 'pull_request'`) — fork PRs CANNOT push to GHCR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Spoofing | build job auth | mitigate | `docker/login-action@v4` against `ghcr.io` using `secrets.GITHUB_TOKEN` (per-job rotation, scoped to repo). NO long-lived PAT. NO third-party registry credentials. |
| T-05-10 | Tampering | image content via PR | mitigate | `if: github.event_name != 'pull_request'` on build/merge/verify means fork PRs cannot push images. Only direct pushes to `main`, tag pushes, and `workflow_dispatch` (which requires write access to the repo) trigger image builds. |
| T-05-11 | Tampering | image at rest in GHCR | mitigate | SHA-pinned tag (`sha-<7char>`) — tags are immutable in practice (even if mutable, the prod overlay references a specific SHA). SBOM + SLSA provenance attestations (D-14) provide cryptographic build-environment verification. Verify job catches missing attestations. |
| T-05-12 | Repudiation | who built this image | mitigate | `provenance: mode=max` includes full build-environment metadata (workflow file SHA, runner labels, build-args, source SHA) — auditable trail for every published image. |
| T-05-13 | Information disclosure | secrets in workflow logs | mitigate | NO `${{ secrets.* }}` echoed to stdout — `GITHUB_TOKEN` is consumed by `login-action` directly. Workflow-level `permissions: contents: read` default prevents accidental write scope from inheriting. |
| T-05-14 | Information disclosure | image contents | accept | Image is intentionally PUBLIC (D-08 manual toggle). The image contains no secrets — Dockerfile's runtime layer is the FastAPI app, no `.env` baked in. Standard public-package threat profile. |
| T-05-15 | Denial of service | GHA cache poisoning across forks | accept | GHA cache `scope=` keys are per-arch but per-repo; forks have separate caches. No fork-to-mainline cache poisoning is possible. Risk: zero. |
| T-05-16 | Denial of service | concurrency contention | mitigate | `cancel-in-progress: false` per D-12 — pushes queue and run sequentially; every `main` SHA gets its image (rollback invariant). The downside (~30s back-to-back-push wait for `test` job) is documented in the concurrency block comment. |
| T-05-17 | Elevation of privilege | verify job inheriting write scopes | mitigate | Verify job has NO explicit `permissions:` block AND inherits only `contents: read` from workflow level. Even if a future edit accidentally added `packages: write` at workflow level, the explicit absence of `docker/login-action` in the verify job's steps means anonymous pull is what runs. Defense in depth. |
| T-05-18 | Tampering | attestation predicate (verification correctness) | mitigate | The verify job uses the CORRECTED `vnd.docker.reference.type=attestation-manifest` annotation predicate per RESEARCH.md §Pitfall 3. The WRONG `mediaType == application/vnd.in-toto+json` predicate from CONTEXT.md D-14 is explicitly rejected by acceptance criterion `! grep -F 'application/vnd.in-toto+json'`. Severity: high (a wrong predicate would let attestation-missing images pass verification undetected). |
| T-05-19 | Denial of service | branch-protection drift | mitigate | Plan 04 documents the manual D-18 step (remove `cd-prod`/`cd-staging` from required-status-check rules). Without this, Phase 5's PR cannot merge. Severity: high. Mitigation lives in Plan 04. |
| T-05-20 | Spoofing | first-push verify failure misinterpreted as success | mitigate | The verify job exits non-zero on `denied: requested access to the resource is denied` (Pitfall 2). Plan 04 documents that the FIRST push verify failure is expected and resolved by the D-08 public-toggle, then re-running via `workflow_dispatch`. The 3-attempt retry loop is for EVENTUAL CONSISTENCY (Pitfall 9), not for hiding auth failures. |
</threat_model>

<verification>
- All seven `must_haves.truths` items map to concrete acceptance criteria in Tasks 1-3.
- The single artifact (`.github/workflows/ci.yml`) is verified via 30+ grep-based acceptance checks across all three tasks.
- All three `must_haves.key_links` (build→merge digest passing; merge→GHCR manifest assembly; verify→GHCR anonymous inspect) are grep-verifiable.
- The corrected attestation predicate (D-14 fix) is verified BOTH positively (`grep -F 'select(.annotations["vnd.docker.reference.type"] == "attestation-manifest")'` exits 0) AND negatively (`grep -F 'application/vnd.in-toto+json'` exits 1).
- No `:latest`, no `flavor: latest=true`, no QEMU, no legacy `docker manifest create` — all banned patterns assert as `exit 1`.
- The complete YAML is parse-checked with `python3 -c "import yaml; yaml.safe_load(...)"` in Task 3.
- The first-push verify failure (Pitfall 2) is acknowledged as expected behavior; full E2E success requires the manual D-08 toggle from Plan 04 followed by a re-run.
- No external services required for this plan to complete; the manual D-08 / D-18 operator actions are owned by Plan 04.
</verification>

<success_criteria>
- `.github/workflows/ci.yml` contains exactly 5 jobs: `test` (preserved), `compose-validate` (Task 1), `build` (matrix; Task 2), `merge` (Task 2), `verify` (Task 3)
- Workflow-level `on:` includes `push.tags: ['v*']` and `workflow_dispatch:` (per D-06)
- Workflow-level `concurrency:` has `group: ghcr-${{ github.ref }}` and `cancel-in-progress: false` (per D-12)
- All action `uses:` lines pin to the corrected v4/v6/v7/v8 set: `actions/checkout@v6`, `docker/setup-buildx-action@v4`, `docker/login-action@v4`, `docker/metadata-action@v6`, `docker/build-push-action@v7`, `actions/upload-artifact@v7`, `actions/download-artifact@v8` (per RESEARCH §Standard Stack — NOT the stale v5/v6 from CONTEXT.md)
- Build job runs on `ubuntu-latest` for amd64 and `ubuntu-24.04-arm` for arm64 (per D-10)
- Per-arch GHA cache uses `mode=max,scope=${{ env.ARCH }}` (per D-11 + Pitfall 4 + Pitfall 5)
- `provenance: mode=max` and `sbom: true` are both set on the build step (per D-14)
- The build job has `permissions: { contents: read, packages: write, attestations: write, id-token: write }` (Pitfall 7)
- The merge job has `permissions: { contents: read, packages: write }` (no attestations on merge)
- The verify job has NO `permissions:` block AND NO `docker/login-action` (anonymous per D-09)
- The verify job uses the CORRECTED attestation predicate (`vnd.docker.reference.type=attestation-manifest`) per RESEARCH.md §Pitfall 3
- The verify job's WRONG predicate from CONTEXT.md D-14 (`application/vnd.in-toto+json`) is NOT present anywhere
- 3-attempt retry loop with 10s sleep handles GHCR eventual consistency (per Pitfall 9)
- `timeout-minutes: 10` on verify (per CONTEXT.md "Claude's Discretion")
- No `:latest` tag literal anywhere in the workflow (CI-02 invariant)
- No `flavor: latest=true` in metadata-action input (CI-02 invariant)
- No QEMU action (`docker/setup-qemu-action`) — D-10 mandates native runners
- No legacy `docker manifest create` — uses `docker buildx imagetools create` only (RESEARCH §Anti-Patterns)
- Build, merge, verify all gated `if: github.event_name != 'pull_request'`; compose-validate has no gate (runs on PRs too as the merge gate)
- The complete YAML parses without error: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0
- The existing `test` job (lint/format/pip-audit/pytest with SQLite test fixtures) is preserved byte-for-byte per D-15
</success_criteria>

<output>
After completion, create `.planning/phases/05-ghcr-image-distribution-ci-validation/05-03-SUMMARY.md`
</output>
