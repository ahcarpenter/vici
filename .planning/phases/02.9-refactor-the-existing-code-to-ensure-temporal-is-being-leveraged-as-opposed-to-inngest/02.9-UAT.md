---
status: complete
phase: 02.9-refactor-the-existing-code-to-ensure-temporal-is-being-leveraged-as-opposed-to-inngest
source: 02.9-01-SUMMARY.md
started: 2026-04-02T00:00:00Z
updated: 2026-04-02T00:01:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running containers. Run `docker compose down -v && docker compose up -d --build`. All services start cleanly: postgres, temporal, temporal-ui, app. The app runs alembic migrations on startup without errors. `curl http://localhost:8000/health` returns HTTP 200. `curl http://localhost:8000/readyz` returns `{"status": "ok", "db": "connected"}`. No crash logs, no inngest references in startup output.
result: issue
reported: "uv run pytest has failing tests when executed"
severity: major
fix_applied: "Rewrote 3 integration tests (test_job_posting, test_unknown, test_worker_goal) from Inngest event/context model to Temporal activity model. Also set app.state.temporal_client mock on locally-created app instances. 76 tests now pass."

### 2. SMS webhook triggers Temporal workflow
expected: Send a POST to `/sms` in Twilio webhook format with a job-posting body (e.g. "Need a plumber tomorrow, 2 hrs, $80/hr, downtown Seattle"). The app accepts the request (202 or 200). A Temporal workflow (`ProcessMessageWorkflow`) starts — visible in the Temporal UI at http://localhost:8080. The workflow completes and a row is inserted in the `job` table with `message_type = 'job_posting'`. No errors in app logs.
result: issue
reported: "the temporal ui loads, though no workflow seems to be kicked off"
severity: major
fix_applied: "Fixed two lifespan bugs: (1) main.py was calling PipelineOrchestrator with old Inngest-era kwargs — rebuilt handlers list and corrected signature; (2) start_cron_if_needed only caught RPCError but SDK now raises WorkflowAlreadyStartedError — catches both. App now starts and workflow is triggered."

### 3. Temporal UI accessible
expected: Open http://localhost:8080 in a browser. The Temporal Web UI loads. After sending the SMS webhook from the previous test, the completed workflow run is visible in the UI with status "Completed".
result: pass

### 4. Workflow retry policy
expected: No active test needed — confirm via Temporal UI or logs that `ProcessMessageWorkflow` is configured with `maximum_attempts=4` (visible in workflow history or source). This matches the old Inngest behavior of 1 initial attempt + 3 retries.
result: pass

### 5. No Inngest references in source
expected: Run `grep -r "inngest" src/` from the project root. The result should be empty (zero matches). Inngest has been fully replaced by Temporal in all source files.
result: issue
reported: "it doesn't — inngest_queue_depth in src/metrics.py"
severity: cosmetic
fix_applied: "Renamed inngest_queue_depth to temporal_workflow_queue_depth in src/metrics.py and updated test. Only stale .pyc bytecode remains (not source)."

### 6. Test suite passes
expected: Run `pytest` from the project root. All 73 tests pass with no errors. No inngest imports appear in test output or warnings.
result: pass
note: 76 tests pass (3 new tests added during fix)

## Summary

total: 6
passed: 4
issues: 3
pending: 0
skipped: 0

## Gaps

- truth: "Posting to /webhook/sms starts a ProcessMessageWorkflow visible in Temporal UI"
  status: failed
  reason: "User reported: the temporal ui loads, though no workflow seems to be kicked off"
  severity: major
  test: 2
  artifacts: []
  missing: []

- truth: "uv run pytest passes — all 73 tests pass with no errors"
  status: failed
  reason: "User reported: uv run pytest has failing tests when executed"
  severity: major
  test: 1
  artifacts: []
  missing: []
