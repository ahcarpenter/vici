---
phase: 02.1-refactor-persistence-layer-and-service-boundaries
plan: 03
subsystem: api
tags: [fastapi, inngest, sqlalchemy, twilio, openai, dependency-injection, integration-testing]

requires:
  - phase: 02.1-01
    provides: Nested Settings, MessageRepository, AuditLogRepository, exception-based SMS router
  - phase: 02.1-02
    provides: ExtractionService (openai_client injected), PipelineOrchestrator, JobRepository, WorkRequestRepository

provides:
  - Full DI graph in FastAPI lifespan (openai_client -> ExtractionService -> PipelineOrchestrator -> app.state.orchestrator)
  - Inngest process_message function reads _orchestrator module var; calls orchestrator.run()
  - PipelineOrchestrator unknown branch handles Twilio reply (removed from inngest_client.py)
  - 3 integration happy-path tests covering job_posting, worker_goal, unknown branches

affects:
  - Phase 03 (GPT extraction feature — depends on orchestrator being wired)
  - tests/integration/ (full pipeline test patterns established here)

tech-stack:
  added: []
  patterns:
    - Module-level _orchestrator var in inngest_client.py set by lifespan — avoids circular imports from src.main
    - Integration tests build real PipelineOrchestrator with mock ExtractionService.process + noop Pinecone
    - Integration tests patch src.extraction.orchestrator.asyncio.to_thread to intercept Twilio calls

key-files:
  created:
    - tests/integration/test_job_posting.py
    - tests/integration/test_worker_goal.py
    - tests/integration/test_unknown.py
  modified:
    - src/main.py
    - src/inngest_client.py
    - src/extraction/orchestrator.py
    - tests/inngest/test_process_message.py

key-decisions:
  - "_orchestrator module-level var in inngest_client.py set by lifespan — cleanest circular import avoidance pattern"
  - "Integration tests use real PipelineOrchestrator with mocked ExtractionService.process (not a full mock orchestrator)"
  - "Twilio unknown reply moved from inngest_client.py to PipelineOrchestrator.run() unknown branch"
  - "Integration test session uses async_session fixture (function-scoped) patching get_sessionmaker in both inngest_client and orchestrator modules"

patterns-established:
  - "DI graph: lifespan sets ic._orchestrator = orchestrator after constructing full graph"
  - "Integration test pattern: build real orchestrator + mock external deps + patch get_sessionmaker"

requirements-completed:
  - layering/DI
  - test-restructuring

duration: 25min
completed: 2026-03-08
---

# Phase 02.1 Plan 03: Wire DI Graph and Integration Tests Summary

**Full DI graph wired in FastAPI lifespan via module-level _orchestrator singleton in inngest_client; 3 integration tests verify complete pipeline end-to-end without OpenAI or Twilio**

## Performance

- **Duration:** 25 min
- **Started:** 2026-03-08T07:00:00Z
- **Completed:** 2026-03-08T07:25:00Z
- **Tasks:** 2 (+ 1 checkpoint)
- **Files modified:** 7

## Accomplishments
- lifespan constructs full DI graph: AsyncOpenAI -> ExtractionService -> PipelineOrchestrator -> app.state.orchestrator + ic._orchestrator
- process_message inngest function reduced to DB lookup + orchestrator.run() call; all business logic in orchestrator
- Twilio unknown reply moved to PipelineOrchestrator.run() unknown branch
- 3 integration tests pass covering all 3 pipeline branches; full suite 43 passed, 0 skipped

## Task Commits

Each task was committed atomically:

1. **Task 1: Wire full DI graph in lifespan and update Inngest function** - `a2350e2` (feat)
2. **Task 2: Implement 3 integration happy-path tests** - `dc383ef` (feat)

**Plan metadata:** (docs commit — see below)

## Files Created/Modified
- `src/main.py` - Builds full DI graph; injects orchestrator into ic._orchestrator
- `src/inngest_client.py` - process_message reads _orchestrator module var; removed ExtractionService/TwilioClient inline construction
- `src/extraction/orchestrator.py` - Added asyncio.to_thread Twilio unknown reply; added UNKNOWN_REPLY_TEXT import
- `tests/inngest/test_process_message.py` - Rewrote to mock _orchestrator (not ExtractionService); 4 tests
- `tests/integration/test_job_posting.py` - Full pipeline test: webhook -> Job row + audit entries
- `tests/integration/test_worker_goal.py` - Full pipeline test: webhook -> WorkRequest row
- `tests/integration/test_unknown.py` - Full pipeline test: webhook -> unknown + Twilio reply verified

## Decisions Made
- Used module-level `_orchestrator` var in inngest_client.py set by lifespan instead of `from src.main import app` to avoid circular imports
- Integration tests build a real PipelineOrchestrator (not a mock) so repository layer is fully exercised; only ExtractionService.process and Pinecone are mocked
- Twilio reply for unknown branch moved from inngest_client.py to PipelineOrchestrator — orchestrator owns all pipeline logic

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Added Twilio unknown reply to orchestrator unknown branch**
- **Found during:** Task 1 (reading orchestrator.py)
- **Issue:** Plan stated orchestrator handles Twilio reply but the orchestrator's unknown branch had no Twilio call; it was only in inngest_client.py
- **Fix:** Added asyncio.to_thread call in orchestrator unknown branch; removed from inngest_client
- **Files modified:** src/extraction/orchestrator.py
- **Verification:** test_process_message_unknown passes; test_full_pipeline_unknown asserts to_thread called with UNKNOWN_REPLY_TEXT
- **Committed in:** a2350e2 (Task 1 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - bug)
**Impact on plan:** Required for correctness — orchestrator must own Twilio reply logic per plan must_haves.

## Issues Encountered
None.

## Next Phase Readiness
- Phase 02.1 fully complete: all 3 plans executed, full test suite green
- Awaiting human verification checkpoint before proceeding to Phase 03

---
*Phase: 02.1-refactor-persistence-layer-and-service-boundaries*
*Completed: 2026-03-08*
