---
phase: 02.6-ensure-research-docs-are-current
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/STACK.md
  - .planning/research/ARCHITECTURE.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "STACK.md contains no pgvector references"
    - "STACK.md header shows date 2026-03-08 and confidence HIGH"
    - "STACK.md Core Technologies table includes Inngest, Braintrust, Pinecone rows"
    - "ARCHITECTURE.md system diagram shows 5-gate webhook chain and Inngest async model"
    - "ARCHITECTURE.md project structure shows src/ not app/"
    - "ARCHITECTURE.md contains no pgvector references"
  artifacts:
    - path: ".planning/research/STACK.md"
      provides: "Accurate technology stack reference for Phase 3 planning"
      contains: "Inngest"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "Accurate architecture reference for Phase 3 planning"
      contains: "PipelineOrchestrator"
  key_links:
    - from: ".planning/research/STACK.md"
      to: ".planning/STATE.md"
      via: "Content accuracy — every fact must match STATE.md / PROJECT.md"
      pattern: "postgres:16"
    - from: ".planning/research/ARCHITECTURE.md"
      to: ".planning/STATE.md"
      via: "Content accuracy — module names must match Architecture Snapshot"
      pattern: "src/"
---

<objective>
Surgically patch STACK.md and ARCHITECTURE.md to reflect the actual implemented state after Phases 01–02.5, then add forward-looking sections for Phase 3/4.

Purpose: These docs feed directly into Phase 3 planning — stale facts (pgvector, BackgroundTasks, wrong module names) would mislead the planner and executor.
Output: Two research docs that accurately describe the built system with ✅/⏳ status annotations and Phase 3/4 forward-looking sections.
</objective>

<execution_context>
@/Users/ahcarpenter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahcarpenter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.6-ensure-research-docs-are-current/02.6-RESEARCH.md

<interfaces>
<!-- Source of truth for all facts in this plan. Extracted from planning docs. -->

Accurate stack (from RESEARCH.md "Accurate Stack" section):
- postgres:16 plain image — no pgvector extension — Pinecone is the vector store
- Inngest: process-message (3 retries, on_failure handler) + sync-pinecone-queue cron
- Braintrust: wraps AsyncOpenAI client via wrap_openai
- NOT in use: pgvector, FastAPI BackgroundTasks, tenacity (Inngest handles retries), sentry-sdk, gunicorn
- uv: package manager + Dockerfile builder stage

Actual project structure (from STATE.md Architecture Snapshot):
src/
├── main.py                    # FastAPI app + lifespan DI graph
├── config.py                  # Nested Pydantic Settings (4 sub-models)
├── database.py                # Async SQLAlchemy engine + sessionmaker
├── models.py                  # Central SQLModel aggregator
├── inngest_client.py          # process-message + sync-pinecone-queue Inngest functions
├── metrics.py                 # Prometheus metric singletons
├── exceptions.py              # Custom exceptions
├── sms/                       # Webhook route, MessageRepository, AuditLogRepository
├── extraction/                # ExtractionService, PipelineOrchestrator, schemas, pinecone_client
├── jobs/                      # JobRepository, Job SQLModel
├── work_requests/             # WorkRequestRepository, WorkRequest SQLModel
├── users/                     # User SQLModel
└── matches/                   # Match SQLModel (placeholder, Phase 3)

Actual architecture (from RESEARCH.md "Accurate Architecture"):
SMS Webhook → 5-gate chain → Inngest event emit → HTTP 200
Inngest process-message → PipelineOrchestrator.run()
  ├── Job branch: ExtractionService → JobRepository.create() flush → Pinecone
  ├── WorkRequest branch: ExtractionService → WorkRequestRepository.create() flush → commit
  └── Unknown branch: asyncio.to_thread(twilio_client.messages.create) → graceful SMS reply
Docker Compose: 8 services — postgres | opensearch | jaeger-collector | jaeger-query | app | inngest | prometheus | grafana

Actual data model tables: users, messages, jobs, work_requests, rate_limit, audit_log, matches, pinecone_sync_queue
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch STACK.md</name>
  <files>.planning/research/STACK.md</files>
  <action>
Apply surgical patches to STACK.md. Read the current file first, then apply all changes below in a single write.

**Header (lines 1-6):**
- Change domain description from "Python/FastAPI + Twilio + OpenAI + PostgreSQL/pgvector" to "Python/FastAPI + Twilio + OpenAI + PostgreSQL + Pinecone + Inngest"
- Change `Researched: 2026-03-05` to `Researched: 2026-03-08`
- Replace the MEDIUM confidence caveat paragraph with: `**Confidence:** HIGH — derived from the actual built system (Phases 01–02.5 complete). Source: STATE.md, PROJECT.md, REQUIREMENTS.md.`

**Core Technologies table — changes:**
- PostgreSQL row: change "pgvector extension is available... pgvector 0.7+" to "postgres:16 plain image — no vector column — Pinecone is the external vector store. Confidence: HIGH"
- DELETE the entire pgvector row (the row starting with `| pgvector | ^0.3 (Python)...`)
- Twilio row: replace "The Python helper library generates TwiML response XML which FastAPI returns as `Response(content=twiml, media_type="text/xml")`" with "Outbound SMS via `asyncio.to_thread()` wrapper (SDK is sync); webhook returns HTTP 200 after Inngest event emit — no TwiML response. Confidence: HIGH"
- Add three new rows after the twilio row:
  ```
  | Inngest | Cloud-hosted (local: Dev Server) | Async event queue — `process-message` function (3 retries, on_failure handler) + `sync-pinecone-queue` cron sweep (*/5 * * * *) | Confidence: HIGH |
  | Braintrust | current | LLM observability — wraps `AsyncOpenAI` client via `braintrust.wrap_openai()` before injection into ExtractionService | Confidence: HIGH |
  | Pinecone | current | Vector store — `text-embedding-3-small` (1536 dims); job embeddings written at creation time; failed writes queued in `pinecone_sync_queue` and retried by Inngest cron | Confidence: HIGH |
  ```

**Supporting Libraries table — changes:**
- tenacity row: update description to "Not used for Inngest retry (Inngest handles 3 retries natively). Kept as library but not active retry mechanism."
- sentry-sdk row: change to "NOT ADOPTED — not in use; remove from `uv add` commands"
- gunicorn row: change to "NOT ADOPTED — Render.com uses Docker runtime directly; not in use"
- Add rows for observability stack:
  ```
  | structlog | ^24.0 | Structured JSON logging — per-request context (phone hash, message_id, trace_id). ✅ Implemented (OBS-04) |
  | opentelemetry-sdk + opentelemetry-exporter-otlp | current | OTel distributed tracing — ALWAYS_ON sampler, OTLP gRPC export to Jaeger v2 collector. ✅ Implemented (OBS-03) |
  | prometheus-client | current | Prometheus metrics — custom GPT counters, latency histograms, queue depth gauge. ✅ Implemented (OBS-02) |
  | inngest | current | Inngest Python SDK — see Core Technologies above. ✅ Implemented (ASYNC-01, ASYNC-03) |
  | braintrust | current | Braintrust Python SDK — see Core Technologies above. ✅ Implemented (OBS-01) |
  | pinecone-client | current | Pinecone Python SDK — see Core Technologies above. ✅ Implemented (VEC-01) |
  | pytest-cov | current | Test coverage reporting — integrated with GitHub Actions CI. ✅ Implemented (PROD-05, PROD-08) |
  ```

**Installation commands section:**
- Remove `pgvector` from `uv add` command
- Add `inngest braintrust opentelemetry-sdk opentelemetry-exporter-otlp prometheus-client` to core runtime `uv add`
- Remove `sentry-sdk` from the command
- Keep `tenacity structlog` in the command (both present in codebase)

**Development Tools table:**
- Docker row: change `pgvector/pgvector:pg16` to `postgres:16` — "8-service Docker Compose: postgres, opensearch, jaeger-collector, jaeger-query, app, inngest, prometheus, grafana"

**What NOT to Use table:**
- Add row: `| FastAPI BackgroundTasks | Not used for this async pattern — Inngest is the async queue | Inngest event-driven processing |`
- Celery + Redis row: update "Use Instead" to "Inngest (already implemented)"

**Stack Patterns by Variant section:**
- Delete the "If deploying to Railway" section entirely
- Delete the "If deploying to Fly.io" section entirely
- Replace with: `**Deploying to Render.com (actual deployment target):** render.yaml Blueprint already exists (PROD-04 complete). Web service uses Docker runtime directly (no gunicorn). Pre-deploy migration hook: `alembic upgrade head`. GIT_SHA env var must be set manually or via deploy hooks. PostgreSQL 16 basic-256mb instance provisioned via Blueprint. First production deploy validation is Phase 4.`
- Update "If running locally" to use `postgres:16` not `pgvector/pgvector:pg16` and note `docker compose up` starts 8 services

**Critical Integration Patterns — Twilio Webhook Handler:**
- Replace the TwiML example code block entirely with the actual pattern:
  ```python
  # Actual implemented pattern — no TwiML returned
  @router.post("/webhook/sms")
  async def handle_sms(request: Request, ...):
      # 5-gate security chain: signature → idempotency → user → rate limit → persist
      await emit_message_received_event(message_id=message.id, sms_text=body)
      return Response(status_code=200)  # HTTP 200 after Inngest event emit; no TwiML
  ```
- Add note: "All GPT processing happens in Inngest `process-message` function outside the Twilio response window."

**Version Compatibility table:**
- Remove the `pgvector ^0.3 | sqlalchemy ^2.0` row

**Sources section at bottom:**
- Replace the note about web access unavailability with: "Sources: STATE.md, PROJECT.md, REQUIREMENTS.md (HIGH confidence — derived from built system). Updated 2026-03-08."
  </action>
  <verify>
    <automated>grep -c "pgvector" /Users/ahcarpenter/workspace/vici/.planning/research/STACK.md</automated>
  </verify>
  <done>STACK.md contains 0 references to "pgvector"; header shows 2026-03-08 and HIGH confidence; Core Technologies table has Inngest, Braintrust, Pinecone rows; postgres:16 plain image stated; TwiML return pattern replaced with HTTP 200 + Inngest emit; Railway/Fly.io sections removed.</done>
</task>

<task type="auto">
  <name>Task 2: Patch ARCHITECTURE.md</name>
  <files>.planning/research/ARCHITECTURE.md</files>
  <action>
Apply surgical patches to ARCHITECTURE.md. Read the current file first, then apply all changes below in a single write.

**Header:**
- Change `Researched: 2026-03-05` to `Researched: 2026-03-08`
- Change confidence line to: `**Confidence:** HIGH — derived from the actual built system (Phases 01–02.5 complete). Source: STATE.md, PROJECT.md, REQUIREMENTS.md.`

**System Overview diagram (the ASCII box diagram):**
Replace the entire diagram with the actual implemented architecture:

```
┌──────────────────────────────────────────────────────────────────────┐
│                            EXTERNAL                                   │
│  ┌──────────────┐                         ┌──────────────────────┐   │
│  │ Twilio SMS   │ POST /webhook/sms        │  OpenAI GPT API      │   │
│  │ (inbound)    │ ────────────────────►   │  (classify+extract)  │   │
│  └──────────────┘                         └──────────────────────┘   │
│  ┌──────────────┐                         ┌──────────────────────┐   │
│  │ Pinecone     │ ◄── job embeddings       │  Inngest Cloud       │   │
│  │ (vector DB)  │                          │  (async functions)   │   │
│  └──────────────┘                         └──────────────────────┘   │
└──────────────────────────────┬───────────────────────────────────────┘
                               │ webhook payload
                               ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    API LAYER (FastAPI + lifespan DI)                   │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │  POST /webhook/sms  — 5-gate security chain:                   │   │
│  │  1. Twilio signature validation (HTTPException 403 on failure) │   │
│  │  2. MessageSid idempotency check (unique constraint)           │   │
│  │  3. User get-or-create (phone_hash)                            │   │
│  │  4. Rate limit check (raw SQL, bypasses ORM cache)             │   │
│  │  5. Persist message to messages table                          │   │
│  │     └── Inngest event emit (message.received) → HTTP 200       │   │
│  └────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────┬───────────────────────────────────────┘
                               │ Inngest event
                               ▼
┌──────────────────────────────────────────────────────────────────────┐
│         INNGEST FUNCTION LAYER (process-message, 3 retries)           │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │  PipelineOrchestrator.run()                                     │   │
│  │  ├── Job branch: ExtractionService → JobRepository flush        │   │
│  │  │                → write_job_embedding() → Pinecone            │   │
│  │  ├── WorkRequest branch: ExtractionService                      │   │
│  │  │                → WorkRequestRepository flush → commit         │   │
│  │  └── Unknown branch: asyncio.to_thread(twilio.messages.create) │   │
│  └────────────────────────────────────────────────────────────────┘   │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │  sync-pinecone-queue cron (*/5 * * * *)                         │   │
│  │  Sweeps pinecone_sync_queue (max 50/run), retries embeddings    │   │
│  └────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────┬───────────────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────────────┐
│                          DATA LAYER                                    │
│  ┌────────────────────────────────────────────────────────────────┐   │
│  │  PostgreSQL 16 (plain)                                          │   │
│  │  Tables: users, messages, jobs, work_requests,                  │   │
│  │          rate_limit, audit_log, matches, pinecone_sync_queue    │   │
│  │  No vector column — Pinecone is the external vector store       │   │
│  └────────────────────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────────────────┘
```

**Component Responsibilities table:**
Replace the entire table with accurate components:

| Component | Responsibility | Actual Module |
|-----------|----------------|---------------|
| Webhook route | 5-gate security chain (sig validate, idempotency, user, rate limit, persist), Inngest event emit, return HTTP 200 | `src/sms/router.py` |
| PipelineOrchestrator | Full pipeline (Job / WorkRequest / Unknown branches), single commit per branch, Pinecone write, graceful unknown reply | `src/extraction/pipeline.py` |
| ExtractionService | Single GPT call via `beta.chat.completions.parse` returning `JobExtraction | WorkerExtraction | UnknownMessage` discriminated union | `src/extraction/service.py` |
| MatchService | ⏳ Phase 3 — earnings math SQL + ranked SMS formatter (placeholder exists) | `src/matches/` |
| MessageRepository | get-or-create user by phone_hash, persist message, dedup check | `src/sms/repository.py` |
| AuditLogRepository | Store raw SMS body + raw GPT response per message | `src/sms/repository.py` |
| JobRepository | CRUD for job records + ⏳ Phase 3 matching query | `src/jobs/repository.py` |
| WorkRequestRepository | CRUD for work_request records | `src/work_requests/repository.py` |
| inngest_client | `process-message` function (3 retries, on_failure) + `sync-pinecone-queue` cron; module-level `_orchestrator` var set by lifespan | `src/inngest_client.py` |
| metrics | Prometheus metric singletons (imported lazily inside functions to avoid circular imports) | `src/metrics.py` |
| Alembic migrations | Schema versioning — asyncio.run() pattern in env.py | `alembic/` |
| Settings | Nested Pydantic Settings (4 sub-models: db, twilio, openai, observability) via model_validator(mode=after) | `src/config.py` |
| Observability | structlog JSON + OTel ALWAYS_ON sampler → Jaeger v2 (OpenSearch) + Prometheus → Grafana | Multiple modules |

**Recommended Project Structure section:**
Replace the `app/` structure entirely with the actual `src/` structure from STATE.md Architecture Snapshot (copy it verbatim).

**Pattern 1 (Synchronous TwiML Response with BackgroundTasks):**
Replace the entire pattern section with:

**Pattern 1: Inngest Event-Driven Processing (Implemented)**

What: The webhook route returns HTTP 200 to Twilio immediately after emitting an Inngest `message.received` event. All GPT processing, storage, and Pinecone writes happen inside the `process-message` Inngest function — outside Twilio's response window.

When to use: Always (this is the implemented pattern).

Trade-offs:
- Pro: Eliminates webhook timeout risk — Twilio sees HTTP 200 within milliseconds
- Pro: Inngest handles retries (3 configured) with on_failure handler
- Pro: Pinecone failure doesn't rollback PostgreSQL transaction
- Con: Debugging requires checking Inngest function logs, not just the webhook

Example:
```python
# Actual pattern — src/sms/router.py
@router.post("/webhook/sms")
async def handle_sms(request: Request, ...):
    # 5-gate chain runs synchronously in webhook handler
    await emit_message_received_event(message_id=message.id, sms_text=body)
    return Response(status_code=200)

# Inngest function — src/inngest_client.py
@inngest_client.create_function(
    fn_id="process-message",
    trigger=inngest.TriggerEvent(event="message/received"),
    retries=3,
    on_failure=_handle_process_message_failure,
)
async def process_message(ctx, step):
    await _orchestrator.run(ctx.event.data["message_id"])
```

**Pattern 2 (Dependency Injection for DB Sessions):**
Update to reflect the actual pattern:
- Webhook route: sessions via `Depends(get_session)` per request
- Inngest functions: sessions created inside function body using `async_session_factory()` — NOT via Depends (Inngest functions are not FastAPI request handlers)
- Module-level `_orchestrator` var in `inngest_client.py` is set by FastAPI lifespan — cleanest circular import avoidance

**Data Flow — Inbound Job Posting Flow:**
Replace the entire data flow to show:
```
Twilio POST /webhook/sms
    │
    ▼ 5-gate chain
MessageRepository.get_or_create(phone_hash) → users table
MessageRepository.create(message) → messages table
AuditLogRepository.create(raw_body) → audit_log table
emit_message_received_event(message_id) → Inngest
    │ HTTP 200 returned to Twilio here
    ▼ (Inngest function, async)
PipelineOrchestrator.run(message_id)
    ├── ExtractionService.process() → OpenAI beta.chat.completions.parse
    │       └── Returns JobExtraction (discriminated union)
    ├── JobRepository.create(job) → session.flush() (no commit yet)
    ├── session.commit() (single commit for the branch)
    └── write_job_embedding(job.id) → Pinecone upsert
              └── on failure: pinecone_sync_queue INSERT (separate session)
```

**Data Flow — Inbound Worker Goal Flow:**
Update similarly to show WorkRequestRepository and the ⏳ Phase 3 matching step.

**Key Data Flows section:**
Replace item 4 (embedding flow) with:
"4. **Pinecone write path:** After JobRepository.create() flush and session.commit(), write_job_embedding() fires. On Pinecone failure, a separate session inserts into pinecone_sync_queue. Inngest cron sync-pinecone-queue sweeps queue every 5 minutes (max 50/run). No embedding column in PostgreSQL."

**Observability section:**
Replace the placeholder text with:
```
Implemented observability stack:
- structlog JSON structured logging — phone_hash, message_id, trace_id on every request (OBS-04)
- OpenTelemetry ALWAYS_ON sampler → OTLP gRPC → Jaeger v2 collector → OpenSearch 2.19.4 (OBS-03)
  - Manual spans: Inngest function, GPT call, Pinecone upsert, Twilio SMS
- Prometheus /metrics endpoint — request count, latency histograms, error rates, GPT token counters,
  latency histogram, pinecone_sync_queue depth gauge (OBS-02)
- Grafana pre-built FastAPI dashboard, auto-provisioned from Docker Compose (OBS-02)
- Braintrust LLM observability — wraps AsyncOpenAI via wrap_openai (OBS-01)
```

**Docker Setup section:**
Replace the yaml example with 8-service compose summary:
```yaml
# docker-compose.yml — 8 services
services:
  postgres:
    image: postgres:16  # plain — no pgvector extension
  opensearch:
    image: opensearchproject/opensearch:2.19.4
  jaeger-collector:    # Jaeger v2 with OTLP gRPC receiver
  jaeger-query:        # Jaeger v2 query/UI service
  app:
    build: .           # Multi-stage Dockerfile; non-root appuser
  inngest:             # Inngest Dev Server
  prometheus:          # Auto-provisioned via config/prometheus.yml
  grafana:             # Auto-provisioned with FastAPI dashboard
```
Note: "Multi-stage Dockerfile is implemented (PROD-01): builder stage (uv sync) + runtime stage (non-root appuser, HEALTHCHECK on /health)."

**Anti-Pattern 4 (pgvector):**
Replace entirely with two new anti-patterns learned during implementation:

**Anti-Pattern 4: Module-Level DI Vars Set at Import Time**
What: Setting `_orchestrator = PipelineOrchestrator(...)` at module top-level in `inngest_client.py`.
Why wrong: FastAPI lifespan hasn't run yet; dependencies (DB engine, OpenAI client) aren't initialized.
Do this instead: Set `_orchestrator = None` at module level; set it inside `app.lifespan()` after building the DI graph.

**Anti-Pattern 6: OpenSearch replicas > 0 on Single-Node**
What: Leaving default `number_of_replicas: 1` in the OpenSearch index template.
Why wrong: Single-node OpenSearch can't allocate replica shards → index health stays yellow → Jaeger index creation fails.
Do this instead: Set `number_of_replicas: 0` in the index template for local dev.

**Suggested Build Order section:**
Replace with: "Implemented vs. Pending" status matching REQUIREMENTS.md. Note that all infrastructure phases (01–02.5) are complete; Phase 3 is next (earnings math matching — MatchService in src/matches/).

**Migration Tooling — pgvector in migrations paragraph:**
Delete the paragraph that starts "pgvector in migrations:" and the ORM model vector column example. Replace with: "Note: postgres:16 plain image is used — no pgvector extension, no vector column. Pinecone is the external vector store."

**Sources section:**
Add to top of sources list: "PRIMARY: STATE.md, PROJECT.md, REQUIREMENTS.md (HIGH confidence — derived from built system, 2026-03-08)"
  </action>
  <verify>
    <automated>grep -c "pgvector" /Users/ahcarpenter/workspace/vici/.planning/research/ARCHITECTURE.md</automated>
  </verify>
  <done>ARCHITECTURE.md contains 0 references to "pgvector"; header shows 2026-03-08 and HIGH confidence; system diagram shows 5-gate webhook chain, Inngest function, PipelineOrchestrator; project structure shows src/ not app/; component table uses actual module names (PipelineOrchestrator, MessageRepository, WorkRequestRepository); docker setup shows 8 services with postgres:16.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -r "pgvector" .planning/research/STACK.md .planning/research/ARCHITECTURE.md` — must return 0 matches
2. `grep "2026-03-08" .planning/research/STACK.md .planning/research/ARCHITECTURE.md` — must match header lines in both files
3. `grep "HIGH" .planning/research/STACK.md .planning/research/ARCHITECTURE.md` — must match confidence lines in both
4. `grep "Inngest" .planning/research/STACK.md` — must match (Core Technologies row)
5. `grep "PipelineOrchestrator" .planning/research/ARCHITECTURE.md` — must match (component table)
6. `grep "src/" .planning/research/ARCHITECTURE.md` — must match (project structure section)
</verification>

<success_criteria>
- STACK.md: 0 pgvector references, date 2026-03-08, confidence HIGH, Inngest/Braintrust/Pinecone rows present, postgres:16 stated, Railway/Fly sections removed, Render.com section added, TwiML pattern replaced with HTTP 200 + Inngest emit
- ARCHITECTURE.md: 0 pgvector references, date 2026-03-08, confidence HIGH, system diagram shows 5-gate chain + Inngest async model, project structure shows src/, component table uses actual module names, 8-service Docker Compose shown, Anti-Pattern 4 pgvector replaced with module-level DI anti-pattern
</success_criteria>

<output>
After completion, create `.planning/phases/02.6-ensure-research-docs-are-current/02.6-01-SUMMARY.md`
</output>
