---
phase: 02.4-ensure-prometheus-instance-is-setup
plan: 02
type: execute
wave: 2
depends_on:
  - 02.4-01
files_modified:
  - docker-compose.yml
  - prometheus/prometheus.yml
  - grafana/provisioning/datasources/prometheus.yml
  - grafana/provisioning/dashboards/dashboards.yml
  - grafana/provisioning/dashboards/fastapi.json
autonomous: false
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03

must_haves:
  truths:
    - "docker compose up -d starts Prometheus at localhost:9090 and Grafana at localhost:3000"
    - "Prometheus shows the app target as UP (scraping /metrics successfully)"
    - "Grafana has a Prometheus datasource pre-provisioned pointing to http://prometheus:9090"
    - "Grafana shows a FastAPI dashboard with request rate, p50/p95 latency, error rate, and GPT metrics panels"
    - "No manual Grafana setup required after docker compose up"
  artifacts:
    - path: "prometheus/prometheus.yml"
      provides: "Prometheus scrape config with 5s interval targeting app:8000 and prometheus:9090"
    - path: "grafana/provisioning/datasources/prometheus.yml"
      provides: "Auto-provisioned Prometheus datasource pointing to http://prometheus:9090"
    - path: "grafana/provisioning/dashboards/dashboards.yml"
      provides: "Grafana dashboard provider config pointing to /etc/grafana/provisioning/dashboards"
    - path: "grafana/provisioning/dashboards/fastapi.json"
      provides: "Pre-built dashboard JSON with FastAPI + GPT metrics panels"
    - path: "docker-compose.yml"
      provides: "prometheus and grafana service blocks"
  key_links:
    - from: "prometheus/prometheus.yml"
      to: "app:8000/metrics"
      via: "Docker network hostname resolution"
      pattern: "targets.*app:8000"
    - from: "grafana/provisioning/datasources/prometheus.yml"
      to: "prometheus:9090"
      via: "Grafana provisioning volume mount"
      pattern: "url.*prometheus:9090"
---

<objective>
Add Prometheus and Grafana containers to docker-compose.yml with all config files mounted as volumes. Grafana is fully provisioned (datasource + dashboard) so zero manual setup is needed after `docker compose up`. Human verification confirms all three services are reachable and the dashboard shows live data.

Purpose: Closes the observability gap — metrics emitted by the app (METRICS-01/02/03 from Plan 01) are now collected, stored, and visualized.
Output: Two new Docker Compose services, four new config/provisioning files, human-verified dashboard.
</objective>

<execution_context>
@/Users/ahcarpenter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahcarpenter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.4-ensure-prometheus-instance-is-setup/02.4-CONTEXT.md
@.planning/phases/02.4-ensure-prometheus-instance-is-setup/02.4-RESEARCH.md
@.planning/phases/02.4-ensure-prometheus-instance-is-setup/02.4-01-SUMMARY.md

<interfaces>
<!-- Existing docker-compose.yml service pattern to follow -->

Existing services use:
- image: pinned version tag (e.g., postgres:16, opensearchproject/opensearch:2.19.4)
- healthcheck with interval: 10s, timeout: 5s, retries: 5
- volumes for config files (e.g., ./jaeger/collector-config.yaml:/etc/jaeger/collector-config.yaml)
- depends_on with condition: service_healthy or service_started
- Named Docker volumes do NOT currently exist — grafana_data will be the first

App service exposes port 8000 and is reachable at hostname "app" within Docker network.

Prometheus image: prom/prometheus:v3.1.0
Grafana image: grafana/grafana:11.4.0

prometheus_fastapi_instrumentator emits metrics with these label names:
- Metric: http_requests_total — labels: handler, method, status
- Metric: http_request_duration_seconds — labels: handler, method, status
Verify exact names by: curl http://localhost:8000/metrics | grep http_request

Custom metrics from Plan 01 (src/metrics.py):
- gpt_calls_total{classification_result="..."}
- gpt_call_duration_seconds (histogram)
- gpt_input_tokens_total
- gpt_output_tokens_total
- pinecone_sync_queue_depth (gauge)
- inngest_queue_depth (gauge, always 0)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Prometheus config and Grafana provisioning files</name>
  <files>
    prometheus/prometheus.yml,
    grafana/provisioning/datasources/prometheus.yml,
    grafana/provisioning/dashboards/dashboards.yml,
    grafana/provisioning/dashboards/fastapi.json
  </files>
  <action>
    Create directory structure:
    ```
    prometheus/
    └── prometheus.yml
    grafana/
    └── provisioning/
        ├── datasources/
        │   └── prometheus.yml
        └── dashboards/
            ├── dashboards.yml
            └── fastapi.json
    ```

    **prometheus/prometheus.yml:**
    ```yaml
    global:
      scrape_interval: 5s
      evaluation_interval: 5s

    scrape_configs:
      - job_name: "app"
        static_configs:
          - targets: ["app:8000"]
        metrics_path: /metrics

      - job_name: "prometheus"
        static_configs:
          - targets: ["prometheus:9090"]
    ```

    **grafana/provisioning/datasources/prometheus.yml:**
    ```yaml
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        uid: prometheus
        access: proxy
        url: http://prometheus:9090
        isDefault: true
        editable: false
    ```

    **grafana/provisioning/dashboards/dashboards.yml:**
    ```yaml
    apiVersion: 1
    providers:
      - name: default
        folder: ""
        type: file
        options:
          path: /etc/grafana/provisioning/dashboards
    ```

    **grafana/provisioning/dashboards/fastapi.json:**
    Build a minimal but complete Grafana dashboard JSON with these panels:
    1. HTTP Request Rate — `sum(rate(http_requests_total[5m])) by (handler)`
    2. HTTP p50 Latency — `histogram_quantile(0.50, sum(rate(http_request_duration_seconds_bucket[5m])) by (le, handler))`
    3. HTTP p95 Latency — `histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket[5m])) by (le, handler))`
    4. HTTP Error Rate — `sum(rate(http_requests_total{status=~"5.."}[5m]))`
    5. GPT Call Rate — `sum(rate(gpt_calls_total[5m])) by (classification_result)`
    6. GPT p95 Latency — `histogram_quantile(0.95, sum(rate(gpt_call_duration_seconds_bucket[5m])) by (le))`
    7. Pinecone Sync Queue Depth — `pinecone_sync_queue_depth`

    In the dashboard JSON, reference the datasource using the provisioned UID to avoid "datasource not found" errors:
    ```json
    "datasource": {"type": "prometheus", "uid": "prometheus"}
    ```

    Use a 3-column grid layout (gridPos width=8 for each panel, height=8).
    Set dashboard uid to "fastapi-vici", title to "Vici FastAPI Metrics".
    Set `"refresh": "5s"` and `"time": {"from": "now-30m", "to": "now"}`.

    Note on metric names: prometheus_fastapi_instrumentator uses `http_requests_total` and `http_request_duration_seconds`. Verify after `docker compose up` by running `curl http://localhost:8000/metrics | grep http_request` and adjust PromQL queries if the actual metric names differ.
  </action>
  <verify>
    <automated>ls prometheus/prometheus.yml grafana/provisioning/datasources/prometheus.yml grafana/provisioning/dashboards/dashboards.yml grafana/provisioning/dashboards/fastapi.json</automated>
  </verify>
  <done>All four config files exist in correct directory structure. fastapi.json is valid JSON (python3 -m json.tool grafana/provisioning/dashboards/fastapi.json outputs without error).</done>
</task>

<task type="auto">
  <name>Task 2: Add prometheus and grafana services to docker-compose.yml</name>
  <files>docker-compose.yml</files>
  <action>
    Add the following two services to `docker-compose.yml`. Add them after the `inngest` service. Also add a `volumes:` top-level section at the end of the file for the grafana_data named volume.

    ```yaml
      prometheus:
        image: prom/prometheus:v3.1.0
        volumes:
          - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
        ports:
          - "9090:9090"
        depends_on:
          - app
        healthcheck:
          test: ["CMD-SHELL", "wget -qO- http://localhost:9090/-/healthy || exit 1"]
          interval: 10s
          timeout: 5s
          retries: 5

      grafana:
        image: grafana/grafana:11.4.0
        environment:
          GF_SECURITY_ADMIN_USER: admin
          GF_SECURITY_ADMIN_PASSWORD: admin
          GF_AUTH_ANONYMOUS_ENABLED: "true"
          GF_AUTH_ANONYMOUS_ORG_ROLE: Viewer
        volumes:
          - ./grafana/provisioning:/etc/grafana/provisioning
          - grafana_data:/var/lib/grafana
        ports:
          - "3000:3000"
        depends_on:
          - prometheus
        healthcheck:
          test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health || exit 1"]
          interval: 10s
          timeout: 5s
          retries: 5

    volumes:
      grafana_data:
    ```

    Confirm port 3000 and 9090 are not already allocated in docker-compose.yml before adding (they are not in the existing file).
  </action>
  <verify>
    <automated>docker compose config --quiet 2>&1 | head -5</automated>
  </verify>
  <done>docker compose config validates without errors. prometheus and grafana services present in the composed output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Prometheus container scraping app /metrics at 5s interval, Grafana container with pre-provisioned datasource and FastAPI dashboard — all started via docker compose.
  </what-built>
  <how-to-verify>
    1. Start the stack: `docker compose up -d`
    2. Wait ~30 seconds for services to initialize
    3. Verify Prometheus is healthy: `curl -s http://localhost:9090/-/healthy` — should return "Prometheus Server is Healthy."
    4. Check app target is UP: `curl -s http://localhost:9090/api/v1/targets | python3 -m json.tool | grep -A3 '"job":"app"'` — look for `"health":"up"`
    5. Verify Grafana is healthy: `curl -s http://localhost:3000/api/health` — should return JSON with `{"database":"ok"}`
    6. Open Grafana in browser: http://localhost:3000 (login: admin/admin or use anonymous viewer)
    7. Navigate to Connections > Data sources — verify "Prometheus" datasource shows green (no error)
    8. Navigate to Dashboards > "Vici FastAPI Metrics" — verify dashboard loads and panels show data (some panels may show "No data" if no traffic yet; send a test request to generate traffic: `curl -X POST http://localhost:8000/webhook/sms`)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe which step failed</resume-signal>
</task>

</tasks>

<verification>
All infrastructure checks confirmed by human:
- INFRA-01: Prometheus + Grafana containers start and are reachable
- INFRA-02: app target is UP in Prometheus (status=up in /api/v1/targets)
- INFRA-03: Grafana datasource shows green, dashboard loads
</verification>

<success_criteria>
- docker compose up -d starts Prometheus at localhost:9090 and Grafana at localhost:3000
- Prometheus target list shows app job as UP
- Grafana has pre-provisioned Prometheus datasource (no manual setup)
- Grafana "Vici FastAPI Metrics" dashboard loads with 7 panels
- Human approves checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02.4-ensure-prometheus-instance-is-setup/02.4-02-SUMMARY.md`
</output>
