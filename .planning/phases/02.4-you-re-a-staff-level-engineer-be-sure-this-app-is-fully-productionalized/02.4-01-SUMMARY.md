---
phase: 02.5-you-re-a-staff-level-engineer-be-sure-this-app-is-fully-productionalized
plan: "01"
subsystem: infra

tags: [docker, multi-stage, prometheus, inngest, structlog]

requires:
  - phase: 02.4-ensure-prometheus-instance-is-setup
    provides: metrics.py with Counter/Gauge singletons and Prometheus stack

provides:
  - Multi-stage Dockerfile with builder + runtime stages, non-root USER, HEALTHCHECK
  - pipeline_failures_total Counter in src/metrics.py with 'function' label
  - Inngest process_message with retries=3 and on_failure=_handle_process_message_failure
  - Gauge updater logs metric staleness via structlog.warning instead of silently passing

affects:
  - Future Docker build/deploy workflows
  - Observability dashboards (new pipeline_failures_total metric available)
  - Inngest failure visibility

tech-stack:
  added: []
  patterns:
    - "Multi-stage Docker build: builder installs deps, runtime copies .venv only — no build tools in production image"
    - "Inngest on_failure handler: lazy import of metrics inside handler to avoid circular imports"
    - "structlog.get_logger().warning() in except blocks — never silent bare pass on observable failures"

key-files:
  created:
    - tests/test_pipeline_hardening.py
  modified:
    - Dockerfile
    - src/metrics.py
    - src/inngest_client.py
    - src/main.py

key-decisions:
  - "Lazy import of pipeline_failures_total inside _handle_process_message_failure (not module top) — consistent with Phase 02.4 circular import avoidance pattern"
  - "Runtime stage copies uv binary from ghcr.io/astral-sh/uv but no build tools — curl installed via apt only for HEALTHCHECK probe"
  - "HEALTHCHECK uses curl -f against /health (liveness endpoint) — avoids readyz DB dependency in health probe"

patterns-established:
  - "Inngest on_failure: always log at ERROR level with message_sid and attempt, then increment pipeline_failures_total counter"
  - "Gauge updater exception handling: structlog.warning with error= kwarg, never bare pass"

requirements-completed: [PROD-01, PROD-02, PROD-03]

duration: 20min
completed: 2026-03-08
---

# Phase 02.5 Plan 01: Application Layer Production Hardening Summary

**Multi-stage Dockerfile (builder + runtime, non-root, HEALTHCHECK), Inngest retries=3 with on_failure counter, and structlog-visible gauge staleness**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-03-08T12:00:00Z
- **Completed:** 2026-03-08T12:20:00Z
- **Tasks:** 2 (Task 1 + Task 2 TDD with RED/GREEN)
- **Files modified:** 5

## Accomplishments

- Rewrote Dockerfile as two-stage build: builder installs .venv, runtime copies only .venv — no pip/uv build tools in final image
- Container runs as non-root appuser with HEALTHCHECK probing /health every 30s
- Added pipeline_failures_total Prometheus Counter with 'function' label to src/metrics.py
- Wired Inngest process_message with retries=3 and _handle_process_message_failure on_failure handler that logs ERROR + increments counter
- Replaced silent bare except pass in _update_gauges with structlog.warning so metric staleness is observable

## Task Commits

Each task was committed atomically:

1. **Task 1: Rewrite Dockerfile as multi-stage with non-root user and HEALTHCHECK** - `509e4f0` (feat)
2. **TDD RED: Failing tests for pipeline hardening** - `7251569` (test)
3. **Task 2 GREEN: Add pipeline_failures_total, Inngest retries, fix gauge updater** - `6c9764a` (feat)

_TDD task has RED (test) + GREEN (feat) commits as expected._

## Files Created/Modified

- `Dockerfile` - Rewritten as two-stage: builder (uv sync) + runtime (non-root, HEALTHCHECK, exec-form CMD)
- `src/metrics.py` - Added pipeline_failures_total Counter with ["function"] label
- `src/inngest_client.py` - Added _handle_process_message_failure on_failure handler; process_message now has retries=3
- `src/main.py` - Replaced bare except pass with structlog.get_logger().warning() in _update_gauges
- `tests/test_pipeline_hardening.py` - 4 new tests for counter import, counter increment, retries config, and gauge warning

## Decisions Made

- Lazy import of pipeline_failures_total inside _handle_process_message_failure, consistent with Phase 02.4 circular import avoidance pattern established in src/extraction/service.py
- HEALTHCHECK uses curl against /health (liveness) not /readyz (requires DB) — health probe should not depend on DB connectivity
- uv binary copied into runtime stage to support CMD exec form; .venv contains all app dependencies

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## Next Phase Readiness

- Production container is hardened: non-root, minimal surface, health-checked
- pipeline_failures_total metric is available for Grafana dashboards (next plan may add it)
- All 68 tests pass
- Ready for next plan in Phase 02.5

---
*Phase: 02.5-you-re-a-staff-level-engineer-be-sure-this-app-is-fully-productionalized*
*Completed: 2026-03-08*
