---
phase: 02.3
slug: migrate-jaeger-to-v2-and-optimize-tracing-setup
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-08
---

# Phase 02.3 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest + pytest-asyncio |
| **Config file** | `pyproject.toml` (`[tool.pytest.ini_options]`) |
| **Quick run command** | `uv run pytest tests/ -x -q` |
| **Full suite command** | `uv run pytest tests/ -v` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/ -x -q`
- **After every plan wave:** Run `uv run pytest tests/ -v`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 02.3-01-01 | 01 | 0 | infra | smoke | `uv run python -c "import yaml; yaml.safe_load(open('jaeger/collector-config.yaml'))"` | ❌ W0 | ⬜ pending |
| 02.3-01-02 | 01 | 0 | infra | smoke | `uv run python -c "import yaml; yaml.safe_load(open('jaeger/query-config.yaml'))"` | ❌ W0 | ⬜ pending |
| 02.3-01-03 | 01 | 1 | otel-config | unit | `uv run pytest tests/test_otel_config.py -x -q` | ❌ W0 | ⬜ pending |
| 02.3-01-04 | 01 | 1 | otel-config | unit | `uv run pytest tests/test_otel_config.py -x -q` | ❌ W0 | ⬜ pending |
| 02.3-02-01 | 02 | 1 | spans | unit | `uv run pytest tests/extraction/test_extraction_service_spans.py -x -q` | ❌ W0 | ⬜ pending |
| 02.3-02-02 | 02 | 1 | spans | unit | `uv run pytest tests/inngest/test_process_message_spans.py -x -q` | ❌ W0 | ⬜ pending |
| 02.3-02-03 | 02 | 1 | spans | unit | `uv run pytest tests/test_pipeline_orchestrator.py -x -q` | ✅ | ⬜ pending |
| 02.3-02-04 | 02 | 1 | spans | unit | `uv run pytest tests/test_pipeline_orchestrator.py -x -q` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `jaeger/collector-config.yaml` — Jaeger v2 collector config for OpenSearch backend
- [ ] `jaeger/query-config.yaml` — Jaeger v2 query config
- [ ] `tests/test_otel_config.py` — sampler + resource attribute assertions using `InMemorySpanExporter`
- [ ] `tests/extraction/test_extraction_service_spans.py` — GPT span attribute assertions
- [ ] `tests/inngest/test_process_message_spans.py` — Inngest span attribute assertions

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Jaeger UI at localhost:16686 shows traces | docker-compose migration | Requires running stack | `docker-compose up`, send SMS webhook, open http://localhost:16686 |
| OpenSearch single-node is healthy | storage backend | Requires running stack | `docker-compose ps` — all containers show healthy |
| Traces visible end-to-end (webhook → Jaeger) | full pipeline | Requires running stack | Send test SMS, verify trace appears with all spans |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 15s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
