---
phase: 02.3-migrate-jaeger-to-v2-and-optimize-tracing-setup
verified: 2026-03-08T00:00:00Z
status: human_needed
score: 11/11 automated must-haves verified
human_verification:
  - test: "Start stack with `docker compose up --build -d`, wait for all services to be healthy, then open http://localhost:16686 and verify Jaeger UI loads and shows 'vici' as an available service"
    expected: "Jaeger UI loads; vici service visible in dropdown; traces appear after a pipeline run with spans named inngest.process_message, gpt.classify_and_extract, pinecone.upsert or twilio.send_sms"
    why_human: "Runtime container health and UI rendering cannot be verified programmatically without a live stack"
---

# Phase 02.3: Migrate Jaeger to v2 and Optimize Tracing Setup — Verification Report

**Phase Goal:** Migrate Jaeger to v2 architecture and add manual OTel spans to uninstrumented pipeline steps.
**Verified:** 2026-03-08
**Status:** human_needed — all automated checks passed; one human runtime check remains (noted in 02.3-02 PLAN)
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | docker-compose.yml contains no reference to jaegertracing/all-in-one | VERIFIED | `grep -c "all-in-one" docker-compose.yml` returns 0 |
| 2 | opensearch, jaeger-collector, and jaeger-query services exist in docker-compose.yml | VERIFIED | All three services present at lines 16, 30, 47 |
| 3 | app service OTEL_EXPORTER_OTLP_ENDPOINT points to http://jaeger-collector:4317 | VERIFIED | Line 72: `OTEL_EXPORTER_OTLP_ENDPOINT: http://jaeger-collector:4317` |
| 4 | _configure_otel() passes sampler=ALWAYS_ON to TracerProvider | VERIFIED | src/main.py line 71: `TracerProvider(resource=resource, sampler=ALWAYS_ON)` |
| 5 | TracerProvider Resource includes deployment.environment and service.version attributes | VERIFIED | src/main.py lines 65-69; test_otel_config.py assertions pass |
| 6 | ObservabilitySettings has service_version populated from git_sha | VERIFIED | src/config.py line 86: `service_version=self.git_sha` in _build_sub_models |
| 7 | Inngest process_message handler body wrapped in span with inngest.event and inngest.function attributes | VERIFIED | src/inngest_client.py lines 49-51; test_process_message_spans.py passes |
| 8 | ExtractionService.process() emits span gpt.classify_and_extract with gen_ai.system and gen_ai.request.model | VERIFIED | src/extraction/service.py lines 36-39; test_extraction_service_spans.py passes |
| 9 | PipelineOrchestrator Pinecone upsert path emits span with db.system=pinecone and db.operation=upsert | VERIFIED | src/extraction/orchestrator.py lines 105-108; orchestrator span test passes |
| 10 | PipelineOrchestrator unknown branch Twilio SMS call wrapped in span with messaging.system=twilio | VERIFIED | src/extraction/orchestrator.py lines 167-169; orchestrator span test passes |
| 11 | All span additions verified by unit tests using InMemorySpanExporter | VERIFIED | 55 tests pass; span tests use direct module-level tracer patching pattern |

**Score:** 11/11 automated truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `jaeger/collector-config.yaml` | Jaeger v2 OTLP receiver + OpenSearch exporter config | VERIFIED | Contains `otlp`, `opensearch:9200`, `replicas: 0`, `healthcheckv2` extension |
| `jaeger/query-config.yaml` | Jaeger v2 query UI config reading from OpenSearch | VERIFIED | Contains `jaeger_query` extension, same OpenSearch backend, `replicas: 0` |
| `src/config.py` | ObservabilitySettings with service_version field | VERIFIED | `service_version: str = "dev"` at line 28; wired via `_build_sub_models` |
| `src/main.py` | _configure_otel() with ALWAYS_ON sampler and extended resource attributes | VERIFIED | ALWAYS_ON at line 71; deployment.environment and service.version in Resource |
| `tests/test_otel_config.py` | Unit tests asserting sampler and resource attributes | VERIFIED | 6 tests; uses InMemorySpanExporter pattern; all pass |
| `src/inngest_client.py` | Inngest span wrapping process_message handler body | VERIFIED | Module-level tracer; `start_as_current_span("inngest.process_message")` at line 49 |
| `src/extraction/service.py` | GPT span in process() method | VERIFIED | Module-level tracer; `start_as_current_span("gpt.classify_and_extract")` at line 36 |
| `src/extraction/orchestrator.py` | Pinecone span and Twilio span in run() method | VERIFIED | `pinecone.upsert` at line 105; `twilio.send_sms` at line 167 |
| `tests/extraction/test_extraction_service_spans.py` | Assertions on GPT span attributes | VERIFIED | 2 tests; asserts gen_ai.system and gen_ai.request.model |
| `tests/inngest/test_process_message_spans.py` | Assertions on Inngest span attributes | VERIFIED | 2 tests; asserts inngest.event and inngest.function |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| docker-compose.yml app service | jaeger-collector:4317 | OTEL_EXPORTER_OTLP_ENDPOINT env var | VERIFIED | Line 72 |
| jaeger-collector | opensearch:9200 | server_urls in collector-config.yaml | VERIFIED | collector-config.yaml line 25 |
| src/main.py _configure_otel() | ALWAYS_ON sampler | TracerProvider(sampler=ALWAYS_ON) | VERIFIED | main.py line 71 |
| src/inngest_client.py process_message | OTel span "inngest.process_message" | tracer.start_as_current_span() | VERIFIED | inngest_client.py line 49 |
| src/extraction/service.py ExtractionService.process() | OTel span "gpt.classify_and_extract" | tracer.start_as_current_span() | VERIFIED | service.py line 36 |
| src/extraction/orchestrator.py job_posting branch | OTel span "pinecone.upsert" | tracer.start_as_current_span() | VERIFIED | orchestrator.py line 105 |
| src/extraction/orchestrator.py unknown branch asyncio.to_thread | OTel span "twilio.send_sms" | tracer.start_as_current_span() wrapping full to_thread call | VERIFIED | orchestrator.py line 167 |

---

### Requirements Coverage

No requirement IDs declared for this phase.

---

### Anti-Patterns Found

None. No TODO/FIXME/placeholder comments, empty implementations, or stub returns found in modified files.

Notable pattern: `sync_pinecone_queue` in inngest_client.py has a `# Phase 2 stub` comment (line 87) — this was pre-existing and outside the scope of phase 02.3. It is an info note, not a blocker.

---

### Human Verification Required

#### 1. Jaeger UI and End-to-End Trace Visibility

**Test:** Start the full stack with `docker compose up --build -d`. Wait for all services to report healthy via `docker compose ps`. Open http://localhost:16686.

**Expected:**
- Jaeger UI loads without error
- "vici" appears as a selectable service
- After triggering a pipeline event (or sending a test SMS webhook), a trace appears in Jaeger with child spans matching: `inngest.process_message`, `gpt.classify_and_extract`, and either `pinecone.upsert` or `twilio.send_sms` depending on message type
- Span attributes (gen_ai.system, db.system, messaging.system) are visible in the Jaeger span detail view
- `docker ps | grep all-in-one` returns nothing

**Why human:** Runtime container orchestration, OpenSearch cluster health (green/yellow), Jaeger UI rendering, and cross-container trace propagation cannot be verified without a live docker-compose stack. The 02.3-02 SUMMARY notes this was already human-verified during the checkpoint task, but the formal record belongs in this verification report.

---

### Gaps Summary

No gaps found. All automated must-haves from both plans (02.3-01 and 02.3-02) are satisfied by the actual codebase. The full test suite passes with 55 tests. The one item in human_verification reflects the runtime docker-compose check that cannot be automated — the SUMMARY indicates this was already confirmed by the user during the human-verify checkpoint task in plan 02.

---

_Verified: 2026-03-08_
_Verifier: Claude (gsd-verifier)_
