# Phase 02.8.1: SOLID Refactoring - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-02
**Phase:** 02.8.1-refactor-the-code-as-appropriate-taking-solid-principles-into-account
**Areas discussed:** Todos review, PipelineOrchestrator handlers, Repository base class

---

## Todos Review

| Option | Description | Selected |
|--------|-------------|----------|
| Temporal abstraction | Decouple Inngest so Temporal could replace it | |
| Tusk integration | Evaluate tusk for tooling/workflow | |
| Neither — skip both | Focus purely on SOLID refactoring | ✓ |

**User's choice:** Neither — skip both
**Notes:** Both todos deferred. Temporal is a larger architectural change. Tusk is not SOLID-related.

---

## PipelineOrchestrator Handlers

### Handler pattern

| Option | Description | Selected |
|--------|-------------|----------|
| Strategy / registry | Dict maps message_type → handler callable/class | |
| Chain of Responsibility | Handlers form a chain; each checks can_handle() | ✓ |
| Command objects | Each message type becomes a Command class | |
| You decide | Claude picks best GoF pattern | |

**User's choice:** Chain of Responsibility
**Notes:** User specified GoF patterns should be used as the structural vehicle for applying SOLID principles.

### Handler location

| Option | Description | Selected |
|--------|-------------|----------|
| src/extraction/handlers/ | Co-located with orchestrator | |
| src/pipeline/handlers/ | New top-level pipeline domain | ✓ |
| You decide | Claude picks location | |

**User's choice:** src/pipeline/handlers/
**Notes:** Pipeline becomes a first-class domain concept.

### Handler interface

| Option | Description | Selected |
|--------|-------------|----------|
| ABC with can_handle() + handle() | Classic GoF Chain of Responsibility | ✓ |
| ABC with handle_or_pass() only | Single method, simpler | |
| You decide | Claude picks interface shape | |

**User's choice:** Abstract base class with can_handle() + handle()

---

## Repository Base Class

### Base pattern

| Option | Description | Selected |
|--------|-------------|----------|
| Abstract base with _persist() helper | BaseRepository, Template Method GoF | ✓ |
| Standalone persist() utility function | Module-level helper, no inheritance | |
| Keep as-is | Leave repeated 3-line pattern | |

**User's choice:** Abstract base with _persist() helper

### Base location

| Option | Description | Selected |
|--------|-------------|----------|
| src/database.py | Central DB module | |
| src/repository.py | New dedicated file | ✓ |
| You decide | Claude picks location | |

**User's choice:** src/repository.py

---

## Claude's Discretion

- Exact method signature for `handle()` context parameter
- Whether `BaseRepository._persist` is classmethod, staticmethod, or instance method
- File naming within `src/pipeline/handlers/`
- Whether handler chain is injected via constructor or built internally

## Deferred Ideas

- Temporal migration — own phase
- Tusk integration — not SOLID-related
- Workflow engine abstraction — related to Temporal
