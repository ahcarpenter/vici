# Forensic Report

**Generated:** 2026-04-09T12:00:00Z
**Problem:** Deploy to dev within the GitHub Actions pipeline keeps failing

---

## Evidence Summary

### Git Activity
- **Last commit:** 2026-04-06 20:48:35 — "fix(infra): re-enable CRD install for external-secrets Helm release"
- **Commits (last 30):** 30 — ALL are `fix(cd):`, `fix(infra):`, or `debug(cd):` prefixed
- **Time span:** 2026-04-06 17:45:56 → 2026-04-06 20:48:35 (3 hours, 3 minutes)
- **Uncommitted changes:** No (8 untracked `.planning/debug/` files only)
- **Active worktrees:** 14 (1 main + 13 orphaned agent worktrees)

### GitHub Actions Runs (last 15)
- **CD Dev failures:** 8 consecutive failures (100% failure rate)
- **CI runs:** All pass (tests are green)
- **Latest failure (24058641714):** `ConcurrentUpdateError` — Pulumi state lock from cancelled run
- **Lock origin:** `runner@runnervm727z3 (pid 3973) at 2026-04-07T00:21:02Z` — a cancelled run at 00:19:53Z left a stale lock

### Planning State
- **Current milestone:** v1.0
- **Current phase:** Phase 03 (temporal-in-cluster) — marked as EXECUTING
- **Last session:** 2026-04-06T05:10:00Z
- **Progress:** 95% (19 of 20 phases complete, all 32 plans complete)
- **Next phase:** Phase 4 — End-to-End Integration & Deployment (not started)

### Debug Sessions (8 files in `.planning/debug/`)
| Debug Session | Status | Root Cause |
|---------------|--------|------------|
| pulumi-sdk-not-installed | awaiting_human_verify | infra/ had no pyproject.toml; `uv sync` was no-op |
| pulumi-gcp-provider-panic | awaiting_human_verify | `__pulumi_raw_state_delta` corrupted state crashes provider |
| pulumi-passphrase-mismatch | awaiting_human_verify | Stack secrets encrypted by Pulumi Cloud, not passphrase |
| deploy-dev-iam-403 | awaiting_human_verify | CI SA missing 7+ IAM roles for infrastructure management |
| cert-manager-helm-timeout | diagnosed | GKE Autopilot blocks kube-system leases; env var mismatch |
| temporal-schema-migration-job-failing | fixing | No Postgres user created for temporal DB instance |
| render-still-deploying | — | (separate from CD pipeline) |
| test-suite-validation | — | (separate from CD pipeline) |

### Artifact Completeness
| Phase | PLAN | CONTEXT | RESEARCH | SUMMARY | VERIFICATION |
|-------|------|---------|----------|---------|--------------|
| 01 Infrastructure | x | x | N/A | x | x |
| 02 GPT Extraction | x | x | N/A | x | x |
| 03 Earnings Matching | x | N/A | N/A | x | x |
| 04 E2E & Deploy | -- | -- | -- | -- | -- |

Phase 4 has no artifacts yet (not started).

## Anomalies Detected

### 1. Stuck Loop — Confidence: HIGH

**Evidence:** `.github/workflows/cd-base.yml` appears in **14 of the last 20 commits**. All 30 recent commits are `fix(cd):` or `fix(infra):` prefixes targeting the same pipeline. The commits span a single 3-hour session on 2026-04-06.

**Interpretation:** The CD pipeline was being debugged via a trial-and-error loop — push a fix, wait for CI, observe failure, push another fix. Each fix unmasked a different underlying issue (see cascading failures below). This is not autonomous agent runaway — it's manual debugging through CI, but the pattern is identical to a stuck loop: same file, consecutive commits, similar messages.

### 2. Cascading Failure Chain — Confidence: HIGH

**Evidence:** Six debug sessions document a **sequential chain of failures**, each unmasked only after the previous was fixed:

1. **pulumi-sdk-not-installed** — No pyproject.toml in infra/ (deps never installed)
2. **pulumi-gcp-provider-panic** — Corrupted `__pulumi_raw_state_delta` crashes GCP provider
3. **pulumi-passphrase-mismatch** — Stack secrets encrypted by Pulumi Cloud, incompatible with GCS backend
4. **deploy-dev-iam-403** — CI service account has only 3 IAM roles, needs 10+
5. **cert-manager-helm-timeout** — GKE Autopilot blocks kube-system leader election
6. **temporal-schema-migration-job-failing** — No Postgres user provisioned for temporal DB

**Interpretation:** The infra code was never successfully deployed before. Each `pulumi up` attempt revealed the next blocker in the dependency chain. This is a first-deployment scenario where accumulated IaC issues surface all at once.

### 3. Stale Pulumi State Lock (Immediate Blocker) — Confidence: HIGH

**Evidence:** Latest CI run (24058641714) fails with:
```
ConcurrentUpdateError: the stack is currently locked by 1 lock(s).
gs://vici-app-pulumi-state-dev/.pulumi/locks/.../6abf96b3-cf0f-4813-8fa0-8ea66a11e8f2.json
created by runner@runnervm727z3 (pid 3973) at 2026-04-07T00:21:02Z
```
A run was cancelled at 00:19:53Z but its lock was never released.

**Interpretation:** The immediate reason the pipeline fails right now is a stale lock file in the GCS state bucket. Every subsequent push will fail with the same error until the lock is cleared.

### 4. Workflow Complexity Accumulation — Confidence: MEDIUM

**Evidence:** `cd-base.yml` has grown to 215 lines with 4 repair/cleanup steps (`continue-on-error: true`) that run before the actual `pulumi up`:
1. "Repair corrupted state" — 50-line Python script stripping secret markers
2. "Repair snapshot integrity" — `pulumi state repair`
3. "Clean up orphaned cluster resources" — Helm uninstalls + CRD deletions + finalizer patches
4. "Refresh Pulumi state" — `pulumi refresh --yes`

**Interpretation:** The CD workflow has accumulated significant workaround complexity. Each fix for a CI failure added another pre-step rather than addressing the root cause in the IaC code. The `continue-on-error: true` flags mask whether these steps succeed or fail, making debugging harder.

### 5. Orphaned Worktrees — Confidence: MEDIUM

**Evidence:** `git worktree list` shows 13 agent worktrees plus 1 named "s" worktree — none appear to be active.

**Interpretation:** Previous Claude Code agent sessions crashed or completed without cleaning up their worktrees. Not a direct cause of the CD failure, but indicates interrupted debug sessions and wasted disk space.

### 6. Unresolved Debug Sessions — Confidence: HIGH

**Evidence:** All 6 CD-related debug sessions are in `awaiting_human_verify` or `fixing` status. None are `resolved`. The fixes described in each session have been committed to code, but:
- **pulumi-passphrase-mismatch** requires manual re-encryption of stack secrets
- **deploy-dev-iam-403** requires manual `gcloud` bootstrap commands
- **temporal-schema-migration-job-failing** is still in `fixing` status

**Interpretation:** Multiple diagnosed fixes require manual human actions (gcloud commands, secret re-encryption) that have not been performed. The code fixes alone are insufficient.

## Root Cause Hypothesis

The CD Dev pipeline has **never successfully completed a full deployment**. The infra code accumulated during Phases 1-3 was never integration-tested against a real GCP environment until the CD pipeline was wired up. This created a cascading chain of 6+ independent failures (missing deps, corrupted state, wrong encryption, missing IAM, Autopilot incompatibilities, missing DB users) that each required a separate fix-push-wait-observe cycle. The **immediate blocker** right now is a stale Pulumi state lock from a cancelled run, but even after clearing it, the **temporal-schema-migration-job-failing** issue remains unresolved, and several fixes require manual human actions that haven't been performed yet.

## Recommended Actions

### Immediate (unblock the pipeline)

1. **Clear the stale Pulumi lock:**
   ```bash
   gsutil rm gs://vici-app-pulumi-state-dev/.pulumi/locks/organization/vici-infra/dev/6abf96b3-cf0f-4813-8fa0-8ea66a11e8f2.json
   ```

2. **Add `pulumi cancel` to cd-base.yml** as a pre-step to prevent future stale locks from blocking deployments.

### Required manual actions (from unresolved debug sessions)

3. **Re-encrypt stack secrets** for passphrase-based backend (per `pulumi-passphrase-mismatch.md`):
   ```bash
   cd infra && pulumi stack change-secrets-provider passphrase --stack dev
   # Re-set all secret config values with `pulumi config set --secret`
   ```

4. **Bootstrap IAM roles** (per `deploy-dev-iam-403.md`):
   Run the gcloud commands to grant the CI SA the required deployer roles before Pulumi can self-manage them.

5. **Fix the temporal DB user** (per `temporal-schema-migration-job-failing.md`):
   Add `gcp.sql.User` resource to `infra/components/database.py`.

### Structural improvements

6. **Remove `continue-on-error: true`** from repair steps or add explicit error handling — silent failures make debugging harder.

7. **Clean up orphaned worktrees:**
   ```bash
   git worktree list | grep '\.claude/worktrees' | awk '{print $1}' | xargs -I{} git worktree remove --force {}
   ```

8. **Close resolved debug sessions** — update status in `.planning/debug/` files after manual actions are completed.

---

*Report generated by `/gsd-forensics`. Absolute paths redacted for portability.*
