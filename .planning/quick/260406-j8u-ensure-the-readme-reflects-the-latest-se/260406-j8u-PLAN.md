---
phase: quick-260406-j8u
title: "Update README to reflect latest setup"
plans: 1
---

# Quick Task: Update README to reflect latest setup

## Plan 1: Sync README environment variable tables with actual .env.*.example files

### Context

The README.md environment variable reference tables are out of date compared to the actual `.env.*.example` files. The Description (lines 1-3) and How It Works (lines 5-7) sections must be preserved exactly as-is.

### Tasks

#### Task 1: Add missing `.env.app` variables to README

- **files:** `README.md`
- **action:** Add two rows to the `.env.app` table after `OTEL_SERVICE_NAME`:
  - `SMS_RATE_LIMIT_WINDOW_SECONDS` | No | Rate limit sliding window duration in seconds (default: `60`) | Pre-filled
  - `SMS_RATE_LIMIT_MAX` | No | Max SMS messages per window (default: `5`) | Pre-filled
- **verify:** Both vars appear in `.env.app` table
- **done:** Two new rows added

#### Task 2: Fix `.env.jaeger-query` table — wrong variable

- **files:** `README.md`
- **action:** Replace the `SPAN_STORAGE_TYPE` row with `OTEL_EXPORTER_OTLP_ENDPOINT`. The actual `.env.jaeger-query.example` has `OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger-collector:4317`, not `SPAN_STORAGE_TYPE`. Update the description accordingly: "OTLP endpoint for self-reporting traces"
- **verify:** Table shows correct variable name
- **done:** Row corrected

#### Task 3: Add missing `.env.opensearch` variable

- **files:** `README.md`
- **action:** Add `OPENSEARCH_JAVA_OPTS` row to the `.env.opensearch` table: `OPENSEARCH_JAVA_OPTS` | No | JVM heap settings (default: `-Xms512m -Xmx512m`)
- **verify:** Variable appears in table
- **done:** New row added

#### Task 4: Add missing `.env.grafana` variables

- **files:** `README.md`
- **action:** Add two rows to the `.env.grafana` table:
  - `GF_AUTH_ANONYMOUS_ENABLED` | No | Allow anonymous read-only access (`true`)
  - `GF_AUTH_ANONYMOUS_ORG_ROLE` | No | Role for anonymous users (`Viewer`)
- **verify:** Both vars appear in table
- **done:** Two new rows added
