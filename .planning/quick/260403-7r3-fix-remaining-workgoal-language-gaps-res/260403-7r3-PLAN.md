---
phase: quick
plan: 260403-7r3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/extraction/schemas.py
  - src/extraction/prompts.py
  - src/pipeline/handlers/worker_goal.py
  - tests/extraction/test_schemas.py
  - tests/extraction/test_service.py
  - tests/extraction/test_metrics.py
  - tests/test_pipeline_orchestrator.py
  - tests/integration/test_worker_goal.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "No remaining references to 'worker_goal' message type — all say 'work_goal'"
    - "No remaining references to result.worker or ExtractionResult.worker field — all say work_goal"
    - "All tests pass after rename"
  artifacts:
    - path: src/extraction/schemas.py
      contains: "work_goal"
  key_links:
    - from: src/extraction/schemas.py
      to: src/pipeline/handlers/worker_goal.py
      via: "ExtractionResult.work_goal field access"
      pattern: "result\\.work_goal"
---

<objective>
Rename remaining "worker" / "worker_goal" language to "work_goal" across the extraction and pipeline domains.

Purpose: Complete the WorkRequest->WorkGoal rename started in prior phases. Three specific renames: (1) message_type literal "worker_goal" -> "work_goal", (2) ExtractionResult.worker field -> work_goal, (3) prompt JSON examples key "worker" -> "work_goal".
Output: Consistent "work_goal" naming with all tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/extraction/schemas.py
@src/extraction/prompts.py
@src/pipeline/handlers/worker_goal.py
@tests/extraction/test_schemas.py
@tests/extraction/test_service.py
@tests/extraction/test_metrics.py
@tests/test_pipeline_orchestrator.py
@tests/integration/test_worker_goal.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename schema field and message_type literal</name>
  <files>src/extraction/schemas.py, src/extraction/prompts.py</files>
  <action>
In src/extraction/schemas.py:
- Line 29: Change Literal value "worker_goal" to "work_goal"
- Line 31: Rename field `worker` to `work_goal` (keep type Optional[WorkerExtraction])

In src/extraction/prompts.py:
- Line 8: Change "worker_goal" to "work_goal" in the classification description
- Lines 85, 90, 100, 105: Change "worker_goal" to "work_goal" in example headings and message_type values
- Lines 57, 80, 92, 107, 122: Change JSON key "worker" to "work_goal" in all example responses
  </action>
  <verify>python -c "from src.extraction.schemas import ExtractionResult; r = ExtractionResult(message_type='work_goal'); assert r.work_goal is None"</verify>
  <done>ExtractionResult uses work_goal field and "work_goal" literal; prompts show "work_goal" everywhere</done>
</task>

<task type="auto">
  <name>Task 2: Update all consumers and tests</name>
  <files>src/pipeline/handlers/worker_goal.py, tests/extraction/test_schemas.py, tests/extraction/test_service.py, tests/extraction/test_metrics.py, tests/test_pipeline_orchestrator.py, tests/integration/test_worker_goal.py</files>
  <action>
In src/pipeline/handlers/worker_goal.py:
- Line 27: Change `result.message_type == "worker_goal"` to `"work_goal"`, change `result.worker` to `result.work_goal`
- Lines 39-40: Change `result.worker.target_earnings` and `result.worker.target_timeframe` to `result.work_goal.*`
- Line 46: Change SQL string `'worker_goal'` to `'work_goal'`

In ALL test files, find-and-replace:
- `message_type="worker_goal"` -> `message_type="work_goal"`
- `result.worker` / `.worker` (on ExtractionResult) -> `result.work_goal` / `.work_goal`
- `mock_result.message_type = "worker_goal"` -> `"work_goal"`
- String `"worker_goal"` in assertions -> `"work_goal"`
- `ExtractionResult(..., worker=worker)` -> `ExtractionResult(..., work_goal=worker)`

Do NOT rename the WorkerExtraction class, the WorkerGoalHandler class, the handler file name, or the test file names — those are separate concerns.
  </action>
  <verify>cd /Users/ahcarpenter/workspace/vici && python -m pytest tests/extraction/ tests/test_pipeline_orchestrator.py tests/integration/test_worker_goal.py -x -q</verify>
  <done>All tests pass with zero references to result.worker or "worker_goal" message type</done>
</task>

</tasks>

<verification>
```bash
# No remaining old references in src/ or tests/
grep -rn '"worker_goal"' src/ tests/ | grep -v '__pycache__' | grep -v '.pyc'
grep -rn 'result\.worker\b' src/ tests/ | grep -v '__pycache__' | grep -v '.pyc' | grep -v 'temporal/test_worker'
grep -rn '\.worker\s*is\s*not\s*None\|\.worker\s*is\s*None\|\.worker\.' src/ tests/ | grep -v '__pycache__' | grep -v 'temporal/'

# Full test suite
python -m pytest -x -q
```
</verification>

<success_criteria>
- Zero grep hits for "worker_goal" as a message_type literal across src/ and tests/
- Zero grep hits for result.worker or .worker field access on ExtractionResult
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260403-7r3-fix-remaining-workgoal-language-gaps-res/260403-7r3-SUMMARY.md`
</output>
