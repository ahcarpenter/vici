---
phase: quick
plan: 260403-969
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "ROADMAP progress table lists all 17 completed phases with correct status"
    - "ROADMAP execution order reflects actual phase sequence including all inserted phases"
    - "Phase 4 description references Temporal instead of Inngest"
    - "All completed plan checkboxes are marked [x] in ROADMAP"
    - "README project structure matches current src/ layout from STATE.md"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Accurate phase tracking"
    - path: "README.md"
      provides: "Current project documentation"
  key_links: []
---

<objective>
Update ROADMAP.md and README.md to accurately reflect the current state of the project after 17 completed phases.

Purpose: Root docs have drifted from reality -- ROADMAP progress table is missing ~10 phases, several completed plan checkboxes are unchecked, Phase 4 still references Inngest (replaced by Temporal in Phase 02.9), and execution order line is stale.
Output: Accurate ROADMAP.md and README.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ROADMAP.md staleness</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Fix the following issues in ROADMAP.md:

1. **Progress table (bottom):** Add ALL missing phases. The table currently lists only 10 rows but there are 20 phases total (17 complete, 2 not started, 1 in progress). Add rows for: 02.6, 02.7, 02.8, 02.8.1, 02.9, 02.10, 02.11, 02.12, 02.13, 02.13.1, 02.14. Mark completed ones as Complete with approximate date 2005-04-03 (based on STATE.md).

2. **Execution Order line:** Update from `1 -> 01.1 -> 2 -> 02.1 -> 02.3 -> 02.4 -> 02.5 -> 3 -> 4` to the full actual order: `1 -> 01.1 -> 2 -> 02.1 -> 02.3 -> 02.4 -> 02.5 -> 02.6 -> 02.7 -> 02.8 -> 02.8.1 -> 02.9 -> 02.10 -> 02.11 -> 02.12 -> 02.13 -> 02.13.1 -> 02.14 -> 3 -> 4`

3. **Unchecked plan boxes for completed phases:** Change `[ ]` to `[x]` for these plan entries that are complete per STATE.md:
   - 02.14-01-PLAN.md
   - 02.05-01-PLAN.md (under Phase 02.13)
   - 02.05-01-PLAN.md (under Phase 02.11)
   - 02.9-01-PLAN.md
   - 02.6-01, 02.6-02, 02.6-03
   - 02.7-01-PLAN.md
   - 02.8.1-PLAN.md

4. **Phase 4 Inngest references:** In Phase 4 title, goal, success criteria, and plan descriptions, replace "Inngest" with "Temporal". Specifically:
   - Phase 4 Goal: "Inngest `process-message` function" -> "Temporal ProcessMessageWorkflow"
   - Success criteria 4: "Inngest Cloud functions are registered" -> "Temporal workflows are registered"
   - Plan 04-01: "Inngest `process-message` function" -> "Temporal ProcessMessageWorkflow"
   - Plan 04-02: "Inngest Cloud wiring" -> "Temporal Cloud wiring"

5. **Phase 02.12 Goal:** Still says "[Urgent work - to be planned]" but the phase is complete. Update to reflect what was actually done: "Simplify architecture by distilling app essence and mapping domains canonically"

6. **Add Status/Completed fields** to phase details that are missing them (02.6 through 02.14 sections) -- mark as Complete with date ~2005-04-03 where STATE.md confirms completion.
  </action>
  <verify>grep -c "Inngest" .planning/ROADMAP.md should return 0 (only in historical Phase 1/2 descriptions where Inngest was the original tech). Verify progress table has ~20 rows. Verify no [ ] remains for completed plans.</verify>
  <done>ROADMAP progress table lists all phases, execution order is complete, all completed plans are checked, Phase 4 references Temporal, no stale Inngest references in forward-looking content</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md project structure and minor accuracy</name>
  <files>README.md</files>
  <action>
1. **Project structure section:** README currently lacks a project structure section. Add one after "Observability" and before "Deployment" that mirrors the Architecture Snapshot from STATE.md. Show the src/ tree with all current domains: main.py, config.py, database.py, models.py, repository.py, metrics.py, exceptions.py, sms/, extraction/, pipeline/ (with orchestrator, context, handlers/), temporal/ (with workflows, activities, worker), jobs/, work_goals/, users/, matches/.

2. **Verify Temporal references are correct:** README already references Temporal (good). Confirm no stale Inngest references exist. If any found, replace with Temporal equivalents.

3. **Verify env var table accuracy:** Cross-check .env.app table against what STATE.md says is built. The table looks accurate -- no changes expected, but confirm BRAINTRUST_API_KEY is still listed (it is used per Phase 02 decisions).
  </action>
  <verify>grep -c "Inngest" README.md should return 0. README should contain a "Project Structure" section with src/ tree.</verify>
  <done>README includes accurate project structure matching current codebase, no stale references</done>
</task>

</tasks>

<verification>
- `grep -c "Inngest" .planning/ROADMAP.md` returns 0 for forward-looking phases (Phases 3, 4)
- `grep -c "\- \[ \]" .planning/ROADMAP.md` returns only entries for Phase 3 and Phase 4 plans (not yet started)
- README.md contains `## Project Structure` section
</verification>

<success_criteria>
Both root-level docs accurately reflect the project's current state: 17 completed phases, Temporal as the workflow engine, and current src/ architecture.
</success_criteria>

<output>
After completion, create `.planning/quick/260403-969-update-the-root-level-docs-to-ensure-the/260403-969-SUMMARY.md`
</output>
