---
phase: quick-260403-4ps
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.app.development
  - .env.postgres.development
  - .env.temporal.development
  - .env.temporal-ui.development
  - .env.opensearch.development
  - .env.grafana.development
  - .env.jaeger-query.development
  - .env.app.example
  - .env.postgres.example
  - .env.temporal.example
  - .env.temporal-ui.example
  - .env.opensearch.example
  - .env.grafana.example
  - .env.jaeger-query.example
  - .gitignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every service that uses env vars loads exclusively from env_file: pointing to a per-service file"
    - "No hardcoded values remain in any environment: block in docker-compose.yml"
    - "docker compose up starts all 9 services without errors"
    - "Per-service .env.{service}.development files exist and are committed"
    - "Per-service .env.{service}.example files document every key"
    - "Staging and production env files are gitignored, development files are tracked"
  artifacts:
    - path: "docker-compose.yml"
      provides: "env_file: only, no inline environment: values"
    - path: ".env.app.development"
      provides: "All app service env vars for local dev"
    - path: ".env.postgres.development"
      provides: "POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD"
    - path: ".env.temporal.development"
      provides: "Temporal DB config referencing postgres values via interpolation"
  key_links:
    - from: "docker-compose.yml postgres service"
      to: ".env.postgres.development"
      via: "env_file:"
    - from: "docker-compose.yml temporal service"
      to: ".env.temporal.development"
      via: "env_file: — reuses same DB creds as postgres service"
    - from: "docker-compose.yml app service"
      to: ".env.app.development"
      via: "env_file: only (remove inline environment: block)"
---

<objective>
Restructure all Docker Compose env var injection to use per-service, per-environment env files exclusively. Remove all inline `environment:` blocks and hardcoded values from docker-compose.yml. Every service that requires env vars loads them from `.env.{service}.{env}`.

Purpose: Clean separation of configuration by service and environment — no config drift, no secret leakage, clear ownership.
Output: Refactored docker-compose.yml + 7 per-service dev env files + 7 example files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create per-service development env files and examples</name>
  <files>
    .env.app.development,
    .env.postgres.development,
    .env.temporal.development,
    .env.temporal-ui.development,
    .env.opensearch.development,
    .env.grafana.development,
    .env.jaeger-query.development,
    .env.app.example,
    .env.postgres.example,
    .env.temporal.example,
    .env.temporal-ui.example,
    .env.opensearch.example,
    .env.grafana.example,
    .env.jaeger-query.example
  </files>
  <action>
Create the following files. Development files contain real local dev defaults (no secrets except the existing `.env` values that are already committed). Example files contain the same keys with placeholder values and comments.

NOTE: The existing `.env` and `.env.development` contain the same values — migrate them into per-service files. Do NOT delete `.env` yet (other tooling may reference it); that cleanup is a separate concern.

**`.env.postgres.development`**
```
POSTGRES_DB=vici
POSTGRES_USER=vici
POSTGRES_PASSWORD=vici
```

**`.env.postgres.example`**
```
# PostgreSQL credentials
POSTGRES_DB=your_db_name
POSTGRES_USER=your_db_user
POSTGRES_PASSWORD=your_db_password
```

**`.env.temporal.development`**
Temporal auto-setup reads its own env var names. It cannot interpolate from another service's env file at runtime. Duplicate the postgres credentials here (they are local dev defaults, not production secrets):
```
DB=postgres12
DB_PORT=5432
POSTGRES_USER=vici
POSTGRES_PWD=vici
POSTGRES_SEEDS=postgres
```

**`.env.temporal.example`**
```
# Temporal persistence backend
DB=postgres12
DB_PORT=5432
# Must match POSTGRES_USER in .env.postgres.{env}
POSTGRES_USER=your_db_user
# Must match POSTGRES_PASSWORD in .env.postgres.{env}
POSTGRES_PWD=your_db_password
# Docker service name of postgres
POSTGRES_SEEDS=postgres
```

**`.env.temporal-ui.development`**
```
TEMPORAL_ADDRESS=temporal:7233
```

**`.env.temporal-ui.example`**
```
# Address of the Temporal frontend service (Docker service name + port)
TEMPORAL_ADDRESS=temporal:7233
```

**`.env.opensearch.development`**
```
discovery.type=single-node
DISABLE_SECURITY_PLUGIN=true
OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m
```

**`.env.opensearch.example`**
```
# OpenSearch cluster mode — use single-node for local dev
discovery.type=single-node
# Disable TLS/auth plugin for local dev only
DISABLE_SECURITY_PLUGIN=true
# JVM heap size — minimum 512m recommended
OPENSEARCH_JAVA_OPTS=-Xms512m -Xmx512m
```

**`.env.grafana.development`**
```
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=admin
GF_AUTH_ANONYMOUS_ENABLED=true
GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
```

**`.env.grafana.example`**
```
# Grafana admin credentials
GF_SECURITY_ADMIN_USER=admin
GF_SECURITY_ADMIN_PASSWORD=change_me_in_production
# Allow anonymous read-only access
GF_AUTH_ANONYMOUS_ENABLED=true
GF_AUTH_ANONYMOUS_ORG_ROLE=Viewer
```

**`.env.jaeger-query.development`**
```
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger-collector:4317
```

**`.env.jaeger-query.example`**
```
# OTLP endpoint that jaeger-query self-reports traces to
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger-collector:4317
```

**`.env.app.development`**
Migrate from existing `.env`, adjusting Docker-internal hostnames (postgres, temporal, jaeger-collector instead of localhost):
```
# Application
ENV=development
GIT_SHA=dev
WEBHOOK_BASE_URL=http://localhost:8000

# Database — must match .env.postgres.development
DATABASE_URL=postgresql+asyncpg://vici:vici@postgres:5432/vici

# Temporal
TEMPORAL_ADDRESS=temporal:7233
TEMPORAL_TASK_QUEUE=vici-queue
CRON_SCHEDULE_PINECONE_SYNC=*/5 * * * *

# Observability
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger-collector:4317
OTEL_SERVICE_NAME=vici

# Rate limiting
SMS_RATE_LIMIT_WINDOW_SECONDS=60
SMS_RATE_LIMIT_MAX=5

# Twilio (test credentials)
TWILIO_AUTH_TOKEN=72bb22212b7d638a6aae8f1586371413
TWILIO_ACCOUNT_SID=ACb474265766935f16b0fe629c810f495f
TWILIO_FROM_NUMBER=+15005550006

# External APIs
BRAINTRUST_API_KEY=sk-oA37kn12NiTisyquwJ02rBYluQnHZ0kxxG0wRMojtmCNAWAb
PINECONE_INDEX_HOST=https://vici-dev-w5hl9ds.svc.aped-4627-b74a.pinecone.io
PINECONE_API_KEY=pcsk_3cwncK_SeBcgYikAHVTkPKymBtiXujEo8XWNmCNc3xokfNysmLfeDDmqRuE3LDsFSx6D97
OPENAI_API_KEY=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
```

**`.env.app.example`**
```
# Application
ENV=development
GIT_SHA=dev
WEBHOOK_BASE_URL=https://your-domain.com

# Database — must match .env.postgres.{env}
# For Docker Compose: use service name "postgres" as host
# For local dev outside Docker: use "localhost"
DATABASE_URL=postgresql+asyncpg://your_db_user:your_db_password@postgres:5432/your_db_name

# Temporal
TEMPORAL_ADDRESS=temporal:7233
TEMPORAL_TASK_QUEUE=vici-queue
CRON_SCHEDULE_PINECONE_SYNC=*/5 * * * *

# Observability
OTEL_EXPORTER_OTLP_ENDPOINT=http://jaeger-collector:4317
OTEL_SERVICE_NAME=vici

# Rate limiting
SMS_RATE_LIMIT_WINDOW_SECONDS=60
SMS_RATE_LIMIT_MAX=5

# Twilio
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_FROM_NUMBER=+15005550006

# External APIs
BRAINTRUST_API_KEY=your_braintrust_api_key
PINECONE_INDEX_HOST=https://your-index.svc.pinecone.io
PINECONE_API_KEY=your_pinecone_api_key
OPENAI_API_KEY=your_openai_api_key
```
  </action>
  <verify>
    <automated>ls .env.app.development .env.postgres.development .env.temporal.development .env.temporal-ui.development .env.opensearch.development .env.grafana.development .env.jaeger-query.development .env.app.example .env.postgres.example .env.temporal.example .env.temporal-ui.example .env.opensearch.example .env.grafana.example .env.jaeger-query.example</automated>
  </verify>
  <done>All 14 files exist. Development files have real values; example files have placeholder values and comments for every key.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor docker-compose.yml to use env_file only, update .gitignore</name>
  <files>docker-compose.yml, .gitignore</files>
  <action>
**docker-compose.yml changes:**

Replace every `environment:` block with `env_file:` pointing to the per-service development file. Remove all inline values — no hardcoded strings, no `${VAR}` interpolation in the compose file itself (all interpolation lives in the env files).

The `app` service currently has both `env_file: .env` and an `environment:` block with overrides. Remove both and replace with a single `env_file: .env.app.development`.

Updated services:

```yaml
services:
  postgres:
    image: postgres:16
    env_file: .env.postgres.development
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vici"]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "5432:5432"

  opensearch:
    image: opensearchproject/opensearch:2.19.4
    env_file: .env.opensearch.development
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9200/_cluster/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 10
    ports:
      - "9200:9200"

  jaeger-collector:
    image: jaegertracing/jaeger:2.16.0
    command: --config /etc/jaeger/collector-config.yaml
    volumes:
      - ./jaeger/collector-config.yaml:/etc/jaeger/collector-config.yaml
    ports:
      - "4317:4317"
      - "4318:4318"
    depends_on:
      opensearch:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:13133/status || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  jaeger-query:
    image: jaegertracing/jaeger:2.16.0
    command: --config /etc/jaeger/query-config.yaml
    env_file: .env.jaeger-query.development
    volumes:
      - ./jaeger/query-config.yaml:/etc/jaeger/query-config.yaml
    ports:
      - "16686:16686"
    depends_on:
      opensearch:
        condition: service_healthy
      jaeger-collector:
        condition: service_started

  app:
    build: .
    depends_on:
      postgres:
        condition: service_healthy
      jaeger-collector:
        condition: service_started
      temporal:
        condition: service_healthy
    env_file: .env.app.development
    volumes:
      - ./src:/app/src
    ports:
      - "8000:8000"
    command: >
      sh -c "uv run alembic upgrade head && uv run uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload"

  temporal:
    image: temporalio/auto-setup:1.26.2
    env_file: .env.temporal.development
    ports:
      - "7233:7233"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "tctl", "--address", "temporal:7233", "cluster", "health"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

  temporal-ui:
    image: temporalio/ui:latest
    env_file: .env.temporal-ui.development
    ports:
      - "8080:8080"
    depends_on:
      - temporal

  prometheus:
    image: prom/prometheus:v3.1.0
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
    depends_on:
      - app
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9090/-/healthy || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

  grafana:
    image: grafana/grafana:11.4.0
    env_file: .env.grafana.development
    volumes:
      - ./grafana/provisioning:/etc/grafana/provisioning
      - grafana_data:/var/lib/grafana
    ports:
      - "3000:3000"
    depends_on:
      - prometheus
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/api/health || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  grafana_data:
```

Note: `jaeger-collector` and `prometheus` have no env vars — no `env_file:` needed.

**`.gitignore` changes:**

Replace the current env file rules with explicit per-service patterns:

Remove:
```
.env
.env.*
!.env.example
!.env.development
```

Add:
```
# Environment files — secrets
.env
.env.*

# Allow committed development defaults and examples
!.env.*.development
!.env.*.example
```

This pattern ensures:
- `.env.*.staging` and `.env.*.production` are gitignored (contain real secrets)
- `.env.*.development` files are committed (safe local defaults)
- `.env.*.example` files are committed (documentation)
- The legacy `.env` file remains gitignored
  </action>
  <verify>
    <automated>docker compose config --quiet 2>&1 | head -5 || echo "docker compose config failed"</automated>
  </verify>
  <done>
    - `docker compose config` exits 0 (valid YAML, all env_file references resolve)
    - No `environment:` blocks remain in docker-compose.yml (grep confirms)
    - `.gitignore` tracks `!.env.*.development` and `!.env.*.example`
    - `git status` shows all `.env.*.development` and `.env.*.example` files as untracked (ready to stage)
  </done>
</task>

</tasks>

<verification>
```bash
# No inline environment: values remain
grep -n "environment:" docker-compose.yml || echo "PASS: no environment blocks"

# All env_file references resolve
docker compose config --quiet

# Development files committed
git status --short | grep "env\." | head -20

# Staging/production gitignored (create and verify)
touch .env.app.staging && git status --short | grep "app.staging" && rm .env.app.staging
```
</verification>

<success_criteria>
- All 7 services with env vars use `env_file:` exclusively
- Zero inline `environment:` blocks or hardcoded values in docker-compose.yml
- 7 `.env.{service}.development` files exist and are git-tracked
- 7 `.env.{service}.example` files exist and are git-tracked
- `.env.{service}.staging` and `.env.{service}.production` files are gitignored
- `docker compose config` validates clean
- `docker compose up` starts all services (manual verify recommended after execution)
</success_criteria>

<output>
After completion, create `.planning/quick/260403-4ps-be-sure-all-environment-variable-key-val/260403-4ps-SUMMARY.md`
</output>
