---
phase: quick
plan: 260403-48b
subsystem: config, temporal, extraction, sms
tags: [constants, configuration, env-files, magic-numbers]
dependency_graph:
  requires: []
  provides: [src/temporal/constants.py, .env.development, TemporalSettings sub-model]
  affects: [src/config.py, src/temporal/workflows.py, src/temporal/worker.py, src/extraction/service.py, src/main.py, src/sms/repository.py]
tech_stack:
  added: []
  patterns: [named-constants-over-literals, env-file-per-environment, settings-sub-models]
key_files:
  created:
    - src/temporal/constants.py
    - .env.development
  modified:
    - src/config.py
    - src/temporal/workflows.py
    - src/temporal/worker.py
    - src/extraction/constants.py
    - src/extraction/service.py
    - src/main.py
    - src/sms/repository.py
    - src/sms/service.py
    - docker-compose.yml
    - .env
    - .gitignore
    - tests/conftest.py
decisions:
  - TemporalSettings sub-model added to Settings to group temporal config canonically
  - .env.development committed (no live secrets); .env.staging and .env.production gitignored
  - worker.py now calls get_settings() at runtime to resolve task_queue from env
metrics:
  duration: ~15 min
  completed: 2026-04-03
---

# Quick Task 260403-48b: Ensure All Magic Numbers Are Constantized — Summary

**One-liner:** All inline numeric/string literals governing behavior extracted to named constants; env-specific URLs and addresses driven from env files; three per-environment files created.

## What Was Done

### Task 1: Audit and Constantize Magic Numbers

**New file: `src/temporal/constants.py`**

All timedelta/retry/timeout literals extracted from `workflows.py` and `worker.py`:

| Constant | Value |
|---|---|
| `TASK_QUEUE` | `"vici-queue"` |
| `CRON_SCHEDULE_PINECONE_SYNC` | `"*/5 * * * *"` |
| `WORKFLOW_PINECONE_SYNC_ID` | `"sync-pinecone-queue-cron"` |
| `PROCESS_MSG_RETRY_MAX_ATTEMPTS` | `4` |
| `PROCESS_MSG_RETRY_INITIAL_INTERVAL` | `timedelta(seconds=1)` |
| `PROCESS_MSG_RETRY_BACKOFF_COEFFICIENT` | `2.0` |
| `PROCESS_MSG_RETRY_MAX_INTERVAL` | `timedelta(minutes=5)` |
| `PROCESS_MSG_ACTIVITY_TIMEOUT` | `timedelta(seconds=60)` |
| `FAILURE_ACTIVITY_TIMEOUT` | `timedelta(seconds=10)` |
| `PINECONE_SYNC_ACTIVITY_TIMEOUT` | `timedelta(seconds=120)` |
| `WORKER_SHUTDOWN_TIMEOUT_SECONDS` | `10.0` |

**Updated: `src/extraction/constants.py`**

Added GPT retry/timeout constants:

| Constant | Value |
|---|---|
| `GPT_CALL_TIMEOUT_SECONDS` | `30.0` |
| `GPT_RETRY_MAX_ATTEMPTS` | `4` |
| `GPT_RETRY_WAIT_MULTIPLIER` | `1` |
| `GPT_RETRY_WAIT_MIN_SECONDS` | `1` |
| `GPT_RETRY_WAIT_MAX_SECONDS` | `60` |
| `OPENAI_MAX_RETRIES` | `0` |

**Updated: `src/main.py`**

Added module-level constants:
- `_GAUGE_POLL_INTERVAL_SECONDS = 15`
- `_GAUGE_MAX_CONSECUTIVE_FAILURES = 5`

Imported `OPENAI_MAX_RETRIES` and `WORKER_SHUTDOWN_TIMEOUT_SECONDS` from domain constants.

### Task 2: Env-specific Values into Settings

**Updated: `src/config.py`**

- Added `TemporalSettings` sub-model (`address`, `task_queue`, `cron_schedule_pinecone_sync`)
- Added `SmsSettings.rate_limit_window_seconds`
- Removed hardcoded localhost defaults from `temporal_address`, `webhook_base_url`, `otel_exporter_otlp_endpoint`, `env` (all now `""`)
- Added flat fields: `temporal_task_queue`, `cron_schedule_pinecone_sync`, `sms_rate_limit_window_seconds`, `grafana_admin_user`, `grafana_admin_password`
- Extended `_validate_required_credentials` to check `temporal_address`, `webhook_base_url`, `env`

**Updated: `docker-compose.yml`**

- Grafana `GF_SECURITY_ADMIN_USER`/`GF_SECURITY_ADMIN_PASSWORD` now use `${GRAFANA_ADMIN_USER:-admin}` / `${GRAFANA_ADMIN_PASSWORD:-admin}`
- Added `SMS_RATE_LIMIT_WINDOW_SECONDS`, `TEMPORAL_TASK_QUEUE`, `CRON_SCHEDULE_PINECONE_SYNC` to app service env block

**Updated: `.env`**

- Removed `INNGEST_DEV=1` and `INNGEST_BASE_URL=http://localhost:8288` (Inngest replaced by Temporal)
- Added: `TEMPORAL_ADDRESS`, `TEMPORAL_TASK_QUEUE`, `CRON_SCHEDULE_PINECONE_SYNC`, `SMS_RATE_LIMIT_WINDOW_SECONDS`, `SMS_RATE_LIMIT_MAX`, `GRAFANA_ADMIN_USER`, `GRAFANA_ADMIN_PASSWORD`

**Created: `.env.development`**

Safe-to-commit development env file. Has all non-secret values. Secrets are commented-out placeholders.

**Created: `.env.staging` and `.env.production`** (gitignored)

Template files with correct `ENV` values and empty slots for operator-supplied values.

**Updated `.gitignore`**

Added `!.env.development` exception so the dev file is committed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `sms/repository.py` had inline `timedelta(seconds=60)`**
- **Found during:** Task 1 verification (`grep -rn "timedelta(seconds=[0-9]"`)
- **Issue:** Rate limit window hardcoded as `60` in repository, ignoring `RATE_LIMIT_WINDOW_SECONDS` from `src.sms.constants`
- **Fix:** Imported `RATE_LIMIT_WINDOW_SECONDS` from `src.sms.constants` and replaced the literal
- **Files modified:** `src/sms/repository.py`
- **Commit:** 8495cae

**2. [Rule 3 - Blocking] `sms/service.py` imported `TASK_QUEUE` from `src.temporal.worker`**
- **Found during:** Task 2 test run
- **Issue:** After removing `TASK_QUEUE` from `worker.py`, `sms/service.py` import broke
- **Fix:** Updated import to `from src.temporal.constants import TASK_QUEUE`
- **Files modified:** `src/sms/service.py`
- **Commit:** 26fb115

**3. [Rule 2 - Missing] Test conftest missing `TEMPORAL_ADDRESS` and `ENV` env vars**
- **Found during:** Task 2 test run after `_validate_required_credentials` expanded
- **Issue:** Tests failed with `ValidationError: Required credentials are missing: temporal_address, env`
- **Fix:** Added `os.environ.setdefault("TEMPORAL_ADDRESS", "localhost:7233")` and `os.environ.setdefault("ENV", "test")` to `tests/conftest.py`
- **Files modified:** `tests/conftest.py`
- **Commit:** 26fb115

## Self-Check: PASSED

All created files exist on disk. All task commits (4acdd97, 26fb115, 8495cae) verified in git log.
