---
phase: quick-260406-m06
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - src/jobs/repository.py
  - src/matches/formatter.py
  - src/matches/repository.py
  - src/matches/service.py
  - src/pipeline/handlers/job_posting.py
  - src/users/repository.py
autonomous: true
must_haves:
  truths:
    - "CI build fails when ruff lint finds errors"
    - "CI build fails when pytest tests fail"
    - "All current lint errors are resolved so CI passes cleanly"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI workflow with enforced lint and test steps"
      contains: "ruff check src/ tests/"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "ruff check"
      via: "Lint step without continue-on-error"
      pattern: "name: Lint"
---

<objective>
Enforce CI build failure on lint and test errors by removing the `continue-on-error: true` from the Lint step and fixing all 9 existing ruff lint violations so the build passes cleanly.

Purpose: The `continue-on-error: true` on the Lint step was a temporary workaround (per the TODO comment). Removing it without fixing existing lint errors would break CI immediately. Both changes must ship together.
Output: Clean CI pipeline that fails on any lint or test regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix all ruff lint violations</name>
  <files>src/jobs/repository.py, src/matches/formatter.py, src/matches/repository.py, src/matches/service.py, src/pipeline/handlers/job_posting.py, src/users/repository.py</files>
  <action>
Fix all 9 ruff lint errors across 6 files:

1. Auto-fix 4 import sorting errors (I001) by running `uv run ruff check --fix src/`:
   - src/jobs/repository.py
   - src/matches/service.py
   - src/pipeline/handlers/job_posting.py
   - src/users/repository.py

2. Manually fix 5 line-too-long errors (E501, max 88 chars):
   - src/jobs/repository.py:23 — wrap the docstring line about pay_type == 'hourly'
   - src/matches/formatter.py:52 — break the f-string into a multi-line expression or intermediate variable
   - src/matches/repository.py:19 — wrap the docstring line about pg_insert
   - src/matches/service.py:19 — break the comment onto the next line
   - src/pipeline/handlers/job_posting.py:50 — break the ternary onto multiple lines

After fixes, run `uv run ruff check src/ tests/` and confirm zero errors.
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && uv run ruff check src/ tests/</automated>
  </verify>
  <done>Zero ruff lint errors across src/ and tests/</done>
</task>

<task type="auto">
  <name>Task 2: Remove continue-on-error from CI Lint step</name>
  <files>.github/workflows/ci.yml</files>
  <action>
In `.github/workflows/ci.yml`, remove both lines from the Lint step:
- `continue-on-error: true`
- The `# TODO: circle back to ensure formatting is further normalized` comment

The Lint step should become simply:
```yaml
      - name: Lint
        run: uv run ruff check src/ tests/
```

The Test step is already correct (no continue-on-error) — do not modify it.
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && grep -c "continue-on-error" .github/workflows/ci.yml | grep -q "^0$" && echo "PASS: no continue-on-error found" || echo "FAIL: continue-on-error still present"</automated>
  </verify>
  <done>CI Lint step has no continue-on-error; both lint and test failures will fail the build</done>
</task>

</tasks>

<verification>
1. `uv run ruff check src/ tests/` exits 0 (all lint clean)
2. `uv run pytest tests/ -x --tb=short -q` passes (tests still green)
3. `grep "continue-on-error" .github/workflows/ci.yml` returns nothing
</verification>

<success_criteria>
- Zero ruff lint errors in src/ and tests/
- CI workflow Lint step no longer has continue-on-error
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/quick/260406-m06-ensure-github-actions-ci-build-fails-if-/260406-m06-SUMMARY.md`
</output>
