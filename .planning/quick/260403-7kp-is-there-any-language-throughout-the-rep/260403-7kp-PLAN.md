---
phase: quick
plan: 260403-7kp
type: execute
wave: 1
depends_on: []
files_modified:
  - src/**/*.py
  - tests/**/*.py
  - alembic/**/*.py
  - docker-compose*.yml
  - render.yaml
  - Dockerfile
  - "*.md"
autonomous: true
requirements: []
must_haves:
  truths:
    - "All variable names, comments, docstrings, error messages, and log strings use canonical Python/FastAPI idioms"
    - "No stale domain references (work_request, worker_request, WorkRequest, etc.) remain"
    - "Terminology is consistent across the entire codebase"
  artifacts: []
  key_links: []
---

<objective>
Audit the entire repository for non-canonical language and revise to be idiomatic.

Purpose: Ensure naming, comments, docstrings, error messages, and log strings are canonical Python/FastAPI and consistent with AGENTS.md conventions.
Output: Cleaner, more idiomatic codebase with no stale domain terminology.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and catalog all non-canonical language</name>
  <files>All src/**/*.py, tests/**/*.py, alembic/**/*.py</files>
  <action>
    Systematically grep and read through every Python file in the repo. Catalog issues into these categories:

    1. **Stale domain names**: Any remaining references to `work_request`, `WorkRequest`, `worker_request`, `WorkerRequest`, or other pre-rename terminology that should be `work_goal`/`WorkGoal`. Also check comments, docstrings, and string literals.

    2. **Non-idiomatic Python**: Variable names that violate PEP 8 (e.g., camelCase locals, abbreviations that obscure meaning, single-letter variables outside comprehensions), non-Pythonic patterns in comments/docstrings.

    3. **Inconsistent terminology**: Same concept referred to differently in different places (e.g., "phone_hash" vs "hashed_phone", "sms_text" vs "message_body" vs "body", "extraction" vs "classification" for the same operation).

    4. **Non-canonical error messages and log strings**: Vague messages like "error occurred", inconsistent capitalization/formatting in log lines, messages that don't follow structlog best practices (should use key=value event style).

    5. **Stale/misleading comments and docstrings**: Comments referencing Inngest (replaced by Temporal), outdated architecture descriptions, TODO comments for completed work.

    Write findings to a scratch file at `.planning/quick/260403-7kp-is-there-any-language-throughout-the-rep/AUDIT.md` with file path, line number, current text, and suggested revision for each finding.
  </action>
  <verify>
    <automated>test -f .planning/quick/260403-7kp-is-there-any-language-throughout-the-rep/AUDIT.md && wc -l .planning/quick/260403-7kp-is-there-any-language-throughout-the-rep/AUDIT.md</automated>
  </verify>
  <done>Complete catalog of all non-canonical language with file locations and suggested fixes</done>
</task>

<task type="auto">
  <name>Task 2: Apply all language revisions</name>
  <files>All files identified in AUDIT.md</files>
  <action>
    Using the AUDIT.md catalog from Task 1, apply all revisions:

    - Rename stale domain references to canonical names
    - Fix non-idiomatic variable names (preserve public API signatures — only rename internal/local variables)
    - Standardize terminology across the codebase
    - Rewrite vague error messages and log strings to be specific and follow structlog event style
    - Update or remove stale comments and docstrings
    - Fix any Inngest references that should say Temporal

    CONSTRAINTS:
    - Do NOT change any public API signatures, database column names, or migration files
    - Do NOT refactor logic or architecture — language/naming only
    - Do NOT change import paths or module names (those are structural changes)
    - Preserve all existing test assertions (update string literals in assertions if the production string changed)
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && python -m pytest tests/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All tests pass with revised language. No stale domain names, no non-idiomatic naming, consistent terminology throughout.</done>
</task>

<task type="auto">
  <name>Task 3: Final grep validation for stale terms</name>
  <files>None (read-only verification)</files>
  <action>
    Run targeted greps to confirm no stale terms remain:

    - `rg -i "work.?request" src/ tests/` should return zero Python hits (config/migration exceptions okay)
    - `rg -i "inngest" src/` should return zero hits
    - `rg "WorkRequest" src/ tests/` should return zero hits
    - `rg "worker_request" src/ tests/` should return zero hits

    If any hits found, fix them. Then run `ruff check src tests` and `ruff format --check src tests` to confirm no lint/format regressions.
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && ruff check src tests && ruff format --check src tests && ! rg -l "inngest" src/ && ! rg -l "WorkRequest" src/ tests/</automated>
  </verify>
  <done>Zero stale domain references, zero lint errors, zero format issues</done>
</task>

</tasks>

<verification>
- All tests pass
- ruff check and format clean
- No stale domain terminology (work_request, WorkRequest, inngest)
- Consistent naming conventions throughout
</verification>

<success_criteria>
Every Python file uses canonical, idiomatic language consistent with AGENTS.md. No stale domain references remain. All tests pass.
</success_criteria>

<output>
After completion, create `.planning/quick/260403-7kp-is-there-any-language-throughout-the-rep/260403-7kp-SUMMARY.md`
</output>
