---
phase: quick
plan: 260403-6m6
type: execute
wave: 1
depends_on: []
files_modified:
  - src/work_requests/models.py
  - src/work_requests/repository.py
  - src/work_requests/schemas.py
  - src/main.py
  - src/models.py
  - src/matches/models.py
  - src/pipeline/constants.py
  - src/pipeline/handlers/worker_goal.py
  - migrations/env.py
  - migrations/versions/2026-03-05_initial_schema.py
  - tests/conftest.py
  - tests/extraction/test_metrics.py
  - tests/extraction/test_persistence.py
  - tests/integration/test_worker_goal.py
  - tests/test_pipeline_orchestrator.py
  - README.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/FEATURES.md
  - .planning/codebase/STACK.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/FEATURES.md
  - .planning/research/SUMMARY.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "No file under src/ contains any variant of WorkRequest/work_request/WORK_REQUEST"
    - "The directory src/work_requests/ has been renamed to src/work_goals/"
    - "All imports referencing src.work_requests are updated to src.work_goals"
    - "All migration files use work_goal table name and WorkGoal class name"
    - "All tests pass after the rename"
  artifacts:
    - path: "src/work_goals/models.py"
      provides: "WorkGoal SQLModel"
    - path: "src/work_goals/repository.py"
      provides: "WorkGoalRepository"
    - path: "src/work_goals/schemas.py"
      provides: "WorkGoal Pydantic schemas"
  key_links:
    - from: "src/main.py"
      to: "src/work_goals/repository.py"
      via: "WorkGoalRepository import"
    - from: "src/models.py"
      to: "src/work_goals/models.py"
      via: "WorkGoal import aggregation"
---

<objective>
Rename the WorkRequest domain to WorkGoal across the entire codebase — directory, Python identifiers, SQL table names, migration files, tests, and documentation.

Purpose: Align domain language with the app's core value proposition (workers text an earnings goal, not a work request).
Output: src/work_goals/ directory with renamed classes, updated imports everywhere, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename src/work_requests/ directory and update all Python source files</name>
  <files>
    src/work_goals/models.py
    src/work_goals/repository.py
    src/work_goals/schemas.py
    src/main.py
    src/models.py
    src/matches/models.py
    src/pipeline/constants.py
    src/pipeline/handlers/worker_goal.py
  </files>
  <action>
    Step 1 — Rename the directory:
    ```
    mv src/work_requests src/work_goals
    ```

    Step 2 — In every file under src/work_goals/ and all other src/ files, apply these replacements (order matters — do longest/most-specific patterns first to avoid partial matches):

    | Find | Replace |
    |------|---------|
    | `WorkRequestRepository` | `WorkGoalRepository` |
    | `WorkRequests` | `WorkGoals` |
    | `WorkRequest` | `WorkGoal` |
    | `work_requests` | `work_goals` |
    | `work_request` | `work_goal` |
    | `WORK_REQUESTS` | `WORK_GOALS` |
    | `WORK_REQUEST` | `WORK_GOAL` |

    Apply to files:
    - src/work_goals/models.py — class name, table name (`__tablename__ = "work_goal"`), any string literals
    - src/work_goals/repository.py — class name, model reference
    - src/work_goals/schemas.py — class names
    - src/main.py — import path (`from src.work_goals ...`), variable names, DI wiring (`WorkGoalRepository`)
    - src/models.py — import path and re-export
    - src/matches/models.py — any FK or relationship references to work_goal
    - src/pipeline/constants.py — any WORK_REQUEST constant names/values
    - src/pipeline/handlers/worker_goal.py — any WorkRequest imports or references

    Do NOT rename the handler file itself (worker_goal.py) — it refers to the worker, not the domain model.
  </action>
  <verify>
    <automated>grep -r "work_request\|WorkRequest\|WORK_REQUEST" /Users/ahcarpenter/workspace/vici/src --include="*.py" | grep -v __pycache__ && echo "FOUND — fix remaining" || echo "CLEAN"</automated>
  </verify>
  <done>Zero matches for any WorkRequest variant in src/. src/work_goals/ directory exists with three files.</done>
</task>

<task type="auto">
  <name>Task 2: Update migration files and tests</name>
  <files>
    migrations/env.py
    migrations/versions/2026-03-05_initial_schema.py
    tests/conftest.py
    tests/extraction/test_metrics.py
    tests/extraction/test_persistence.py
    tests/integration/test_worker_goal.py
    tests/test_pipeline_orchestrator.py
  </files>
  <action>
    Apply the same replacement table from Task 1 to:

    - migrations/env.py — any import or reference to work_requests module or WorkRequest model
    - migrations/versions/2026-03-05_initial_schema.py — table name string `"work_request"` → `"work_goal"`, class name `WorkRequest` → `WorkGoal`, column references, index names that embed the table name (e.g. `work_request_user_id_fkey` → `work_goal_user_id_fkey`)
    - tests/conftest.py — imports, fixture variable names, model instantiation
    - tests/extraction/test_metrics.py — WorkRequest model usage
    - tests/extraction/test_persistence.py — WorkRequest model usage, import paths
    - tests/integration/test_worker_goal.py — WorkRequest imports and assertions
    - tests/test_pipeline_orchestrator.py — WorkRequest imports and assertions

    After edits, run the test suite to confirm no breakage:
    ```
    cd /Users/ahcarpenter/workspace/vici && python -m pytest tests/ -x -q 2>&1 | tail -20
    ```
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && grep -r "work_request\|WorkRequest\|WORK_REQUEST" migrations/ tests/ --include="*.py" | grep -v __pycache__ && echo "FOUND — fix remaining" || echo "CLEAN"; python -m pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>Zero WorkRequest references in migrations/ and tests/. pytest exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Update documentation and planning files</name>
  <files>
    README.md
    .planning/STATE.md
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/codebase/ARCHITECTURE.md
    .planning/codebase/FEATURES.md
    .planning/codebase/STACK.md
    .planning/research/ARCHITECTURE.md
    .planning/research/FEATURES.md
    .planning/research/SUMMARY.md
  </files>
  <action>
    Apply the same replacement table from Task 1 to all documentation files. Pay attention to:

    - Prose sentences: "WorkRequest domain" → "WorkGoal domain", "work_requests table" → "work_goals table"
    - Architecture diagrams / code snippets in markdown: update directory paths (`work_requests/` → `work_goals/`) and class names
    - DI graph in STATE.md: `WorkRequestRepository` → `WorkGoalRepository`
    - Schema table lists: `WorkRequest` → `WorkGoal`
    - Any kebab-case or path references: `work-request` → `work-goal`

    Note: Planning phase history docs (.planning/phases/**) contain historical context and can be updated for accuracy but are not load-bearing for the running system — update them as well so grep returns clean.

    Run a final sweep to confirm no planning docs missed:
    ```
    grep -r "work_request\|WorkRequest\|WORK_REQUEST\|work-request" /Users/ahcarpenter/workspace/vici/.planning --include="*.md" | grep -v phases/ | wc -l
    ```
    (phases/ historical docs are lower priority; the non-phases docs must be clean)
  </action>
  <verify>
    <automated>grep -r "work_request\|WorkRequest\|WORK_REQUEST\|work-request" /Users/ahcarpenter/workspace/vici/README.md /Users/ahcarpenter/workspace/vici/.planning/STATE.md /Users/ahcarpenter/workspace/vici/.planning/PROJECT.md /Users/ahcarpenter/workspace/vici/.planning/REQUIREMENTS.md /Users/ahcarpenter/workspace/vici/.planning/ROADMAP.md /Users/ahcarpenter/workspace/vici/.planning/codebase/ /Users/ahcarpenter/workspace/vici/.planning/research/ && echo "FOUND — fix remaining" || echo "CLEAN"</automated>
  </verify>
  <done>Zero WorkRequest references in README.md and all non-phases .planning/ docs. Planning phase history docs updated where found.</done>
</task>

</tasks>

<verification>
Full sweep after all tasks:

```bash
grep -r "work_request\|WorkRequest\|WORK_REQUEST\|work-request" \
  /Users/ahcarpenter/workspace/vici/src \
  /Users/ahcarpenter/workspace/vici/migrations \
  /Users/ahcarpenter/workspace/vici/tests \
  /Users/ahcarpenter/workspace/vici/README.md \
  --include="*.py" --include="*.md" | grep -v __pycache__ \
  && echo "RESIDUE FOUND" || echo "ALL CLEAN"

cd /Users/ahcarpenter/workspace/vici && python -m pytest tests/ -x -q 2>&1 | tail -5
```

Expected: "ALL CLEAN" and pytest exits 0.
</verification>

<success_criteria>
- src/work_goals/ directory exists; src/work_requests/ does not
- Zero occurrences of WorkRequest/work_request/WORK_REQUEST/work-request in src/, migrations/, tests/, README.md, and primary .planning/ docs
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/quick/260403-6m6-rename-workrequest-domain-to-workgoal/260403-6m6-SUMMARY.md`
</output>
