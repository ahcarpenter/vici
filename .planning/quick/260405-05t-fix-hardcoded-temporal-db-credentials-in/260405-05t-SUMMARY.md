---
phase: 260405-05t
plan: 01
subsystem: infra
tags: [security, pulumi, temporal, credentials]
dependency_graph:
  requires: []
  provides: [temporal-encrypted-db-credentials]
  affects: [infra/components/temporal.py, infra/Pulumi.dev.yaml]
tech_stack:
  added: []
  patterns: [pulumi-secret-config, output-apply]
key_files:
  created: []
  modified:
    - infra/components/temporal.py
    - infra/Pulumi.dev.yaml
decisions:
  - Use cfg.require_secret() for both temporal_db_user and temporal_db_password
  - Rebuild _SCHEMA_COMMANDS with pulumi.Output.all().apply() since credentials are now Output[str]
  - Remove _SQL_TOOL constant; replace with _SQL_TOOL_PREFIX (credentials injected via apply lambda)
metrics:
  duration: ~5m
  completed: 2026-04-05
  tasks_completed: 2
  files_modified: 2
---

# Phase 260405-05t Plan 01: Fix Hardcoded Temporal DB Credentials Summary

**One-liner:** Moved Temporal DB credentials from plaintext constants in temporal.py to Pulumi encrypted config secrets via `cfg.require_secret()`.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Store Temporal DB credentials as Pulumi config secrets | 690ad8f | infra/Pulumi.dev.yaml |
| 2 | Replace hardcoded credentials with Pulumi config secrets in temporal.py | 4db2701 | infra/components/temporal.py |

## Changes Made

### infra/Pulumi.dev.yaml
- Added `vici-infra:temporal_db_user` as `secure:` encrypted entry
- Added `vici-infra:temporal_db_password` as `secure:` encrypted entry

### infra/components/temporal.py
- Added `cfg` to import from `config` module
- Replaced `_TEMPORAL_DB_USER = "postgres"` with `cfg.require_secret("temporal_db_user")`
- Replaced `_TEMPORAL_DB_PASS = "JPfajtd..."` with `cfg.require_secret("temporal_db_password")`
- Removed `_SQL_TOOL` f-string constant (credentials were embedded as plaintext)
- Added `_SQL_TOOL_PREFIX` constant (no credentials)
- Rebuilt `_SCHEMA_COMMANDS` using `pulumi.Output.all(_TEMPORAL_DB_USER, _TEMPORAL_DB_PASS).apply()` to correctly handle `Output[str]` values

## Deviations from Plan

None - plan executed exactly as written.

## Threat Flags

None - this plan's purpose was to remediate T-05t-01 (plaintext credentials in source). No new threat surface introduced.

## Self-Check: PASSED

- infra/Pulumi.dev.yaml contains two `secure:` encrypted temporal_db entries
- `grep -rq "JPfajtdjMqJBseAr3rOc6iE1KGkms7yUCEavQfBqd9k" infra/` returns no matches
- `grep "require_secret" infra/components/temporal.py` shows both credential lookups
- Commits 690ad8f and 4db2701 exist in git log
