---
phase: quick
plan: 260403-30x
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/FEATURES.md
  - .planning/codebase/PITFALLS.md
  - .planning/codebase/STACK.md
  - .planning/codebase/SUMMARY.md
  - .planning/STATE.md
  - README.md
autonomous: true
must_haves:
  truths:
    - "All docs reflect Temporal replacing Inngest"
    - "All docs reflect pipeline/ domain with handler pattern from 02.12"
    - "Architecture diagram matches actual src/ layout"
  artifacts:
    - path: ".planning/codebase/ARCHITECTURE.md"
      provides: "Current architecture with pipeline domain"
    - path: ".planning/codebase/STACK.md"
      provides: "Temporal in stack, no Inngest references"
    - path: "README.md"
      provides: "Accurate setup and architecture overview"
  key_links: []
---

<objective>
Update all project documentation to reflect changes from Phases 02.9 through 02.12.

Purpose: Docs are stale from March 8 (Phase 02.6). Since then: Inngest replaced by Temporal (02.9), TracingInterceptor added (02.10), edge-case hardening (02.11), and domain canonicalization moving orchestrator to src/pipeline/ with handler pattern (02.12).

Output: All .planning/codebase/ docs, STATE.md, and README.md accurate to current codebase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md
@.planning/codebase/FEATURES.md
@.planning/codebase/PITFALLS.md
@.planning/codebase/SUMMARY.md
@README.md
@src/main.py
@src/pipeline/orchestrator.py
@src/pipeline/handlers/base.py
@src/temporal/activities.py
@src/temporal/workflows.py
@src/temporal/worker.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update codebase docs (ARCHITECTURE, STACK, FEATURES, PITFALLS, SUMMARY)</name>
  <files>.planning/codebase/ARCHITECTURE.md, .planning/codebase/STACK.md, .planning/codebase/FEATURES.md, .planning/codebase/PITFALLS.md, .planning/codebase/SUMMARY.md</files>
  <action>
Read each doc, then update to reflect current state:

1. **ARCHITECTURE.md**: Replace all Inngest references with Temporal (workflows, activities, worker). Update src/ tree to show `pipeline/` domain with `handlers/` subdirectory (base.py, job_posting.py, unknown.py, worker_goal.py), `context.py`, `orchestrator.py`. Remove `inngest_client.py` from tree. Show `temporal/` domain (activities.py, workflows.py, worker.py). Update DI graph: PipelineOrchestrator now lives in `src.pipeline.orchestrator`, uses handler registry pattern. Note TracingInterceptor on Temporal client.

2. **STACK.md**: Replace Inngest with Temporal in orchestration section. Add temporalio SDK. Note TracingInterceptor for OTel integration. Remove any Inngest SDK references.

3. **FEATURES.md**: Update pipeline/orchestration features to reflect Temporal workflows and handler pattern. Mark edge-case hardening (02.11) items as complete. Update status markers.

4. **PITFALLS.md**: Add pitfalls discovered in 02.9-02.12: Temporal RPCError for cron idempotency, TracingInterceptor on Client.connect only (worker inherits), handler registration in lifespan DI. Remove Inngest-specific pitfalls or mark as historical.

5. **SUMMARY.md**: Update the executive summary to reflect Temporal, pipeline domain, and completed phases through 02.12.

For all docs: grep for "inngest" (case-insensitive) and replace/update every occurrence. Set date to 2026-04-03.
  </action>
  <verify>
    <automated>grep -ri "inngest" .planning/codebase/ | grep -v "historical\|replaced\|migrated" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>All five codebase docs accurately reflect the Temporal-based, pipeline-domain architecture with no stale Inngest references.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.md and STATE.md</name>
  <files>README.md, .planning/STATE.md</files>
  <action>
1. **README.md**: Read current content. Update architecture overview section to reflect `src/pipeline/` domain and Temporal. Update any setup instructions that reference Inngest (should reference Temporal worker/server instead). Update docker-compose service list if it mentions Inngest (now temporal instead). Ensure the local dev instructions mention starting Temporal dev server.

2. **STATE.md**: Update Architecture Snapshot section:
   - Replace `inngest_client.py` with `temporal/` domain
   - Replace `extraction/` PipelineOrchestrator reference with `pipeline/` domain
   - Update DI Graph to show handler registry pattern
   - Update src/ tree to match actual current layout
   - Set last_updated to 2026-04-03
  </action>
  <verify>
    <automated>grep -ri "inngest" README.md .planning/STATE.md | grep -v "historical\|replaced\|migrated\|Phase 01\|Phase 02\|Decisions" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>README.md and STATE.md reflect current architecture. No stale Inngest references outside of historical decision logs.</done>
</task>

</tasks>

<verification>
- `grep -ri "inngest" .planning/codebase/ README.md` returns only historical/migration references
- Architecture diagrams in all docs match `find src -type f -name "*.py" | sort` output
- STATE.md src/ tree matches actual directory structure
</verification>

<success_criteria>
All project documentation is current as of 2026-04-03, reflecting Temporal orchestration, pipeline domain with handler pattern, and completed phases through 02.12.
</success_criteria>

<output>
After completion, create `.planning/quick/260403-30x-update-all-docs/260403-30x-SUMMARY.md`
</output>
