---
phase: 01.1-apply-revised-3nf-schema-and-propagate-throughout-app
plan: 01
subsystem: database
tags: [sqlmodel, alembic, postgresql, migrations, 3nf]

requires:
  - phase: 01-infrastructure-foundation
    provides: Alembic env.py, async SQLAlchemy setup, initial migration file

provides:
  - User SQLModel at src/users/models.py with phone_hash UNIQUE, created_at TIMESTAMPTZ
  - Message, RateLimit, AuditLog at src/sms/models.py with integer FKs
  - Job at src/jobs/models.py with user_id/message_id FKs and CHECK constraints
  - WorkRequest at src/work_requests/models.py with user_id/message_id FKs
  - Match at src/matches/models.py with job_id/work_request_id FKs
  - src/models.py aggregator importing all 7 model classes
  - Alembic migration rewritten to 3NF schema with TIMESTAMPTZ and integer FKs

affects:
  - 01.1-02 (service layer propagation)
  - 01.1-03 (router and test propagation)
  - Phase 2 (GPT integration uses Message and Job models)

tech-stack:
  added: []
  patterns:
    - "TIMESTAMPTZ via sa_column=sa.Column(sa.DateTime(timezone=True), nullable=False)"
    - "FK with ondelete via sa_column=sa.Column(sa.Integer, sa.ForeignKey(..., ondelete=...))"
    - "CHECK constraints via __table_args__ = (CheckConstraint(...),)"
    - "UNIQUE constraints via __table_args__ = (UniqueConstraint(...),)"

key-files:
  created:
    - src/users/__init__.py
    - src/users/models.py
    - src/work_requests/__init__.py
    - src/work_requests/models.py
    - src/matches/__init__.py
    - src/matches/models.py
  modified:
    - src/sms/models.py
    - src/jobs/models.py
    - src/models.py
    - migrations/versions/2026-03-05_initial_schema.py

key-decisions:
  - "Phone/InboundMessage/Worker classes replaced by User/Message/WorkRequest with integer FKs — eliminates phone_hash string pseudo-FKs"
  - "Match placed in dedicated src/matches/ domain rather than appended to sms/models.py — clear domain separation"
  - "src/workers/ deleted in full — WorkRequest in src/work_requests/ is the correct concept"
  - "Migration revision ID 001 preserved — this is the root migration (down_revision=None)"

patterns-established:
  - "FK columns use sa_column=sa.Column(sa.Integer, sa.ForeignKey(target, ondelete=...)) — never Field(foreign_key=) for ondelete support"
  - "All datetime columns use sa.DateTime(timezone=True) — never sa.DateTime() bare"

requirements-completed: []

duration: 20min
completed: 2026-03-07
---

# Phase 01.1 Plan 01: 3NF Schema and Model Reorganization Summary

**7-table 3NF SQLModel schema with integer FKs, TIMESTAMPTZ datetime columns, and Alembic migration rewritten in place — replacing phone_hash string pseudo-FKs throughout**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-03-07
- **Completed:** 2026-03-07
- **Tasks:** 3
- **Files modified:** 10

## Accomplishments

- Created User, WorkRequest domains (src/users/, src/work_requests/); deleted src/workers/ entirely
- Rewrote src/sms/models.py (Message, RateLimit, AuditLog), src/jobs/models.py (integer FKs, CHECK constraints); added src/matches/models.py
- Rewrote Alembic migration to create all 7 tables in FK dependency order with TIMESTAMPTZ and proper ondelete actions
- All 7 SQLModel classes importable from src.models and registered in SQLModel.metadata

## Task Commits

1. **Task 1: Create User model and work_requests domain** - `8fc575a` (feat)
2. **Task 2: Rewrite sms/jobs models; add Match domain; update src/models.py** - `4475e96` (feat)
3. **Task 3: Rewrite Alembic migration to 3NF schema** - `82a4c3c` (feat)

## Files Created/Modified

- `src/users/models.py` - User SQLModel with phone_hash UNIQUE, created_at TIMESTAMPTZ
- `src/work_requests/models.py` - WorkRequest with user_id/message_id FKs, target_earnings CHECK
- `src/matches/models.py` - Match with job_id/work_request_id FKs, UNIQUE constraint
- `src/sms/models.py` - Message (new), RateLimit (integer FK), AuditLog (message_id nullable FK); Phone/InboundMessage removed
- `src/jobs/models.py` - user_id/message_id FKs added, phone_hash removed, CHECK constraints added
- `src/models.py` - imports all 7 model classes from domain paths
- `migrations/versions/2026-03-05_initial_schema.py` - full 3NF rewrite, 7 tables, TIMESTAMPTZ, FKs with ondelete

## Decisions Made

- Phone/InboundMessage/Worker classes removed and replaced by User/Message/WorkRequest — integer FK referential integrity replaces phone_hash string pseudo-FKs
- Match placed in dedicated src/matches/ domain for clean domain separation
- Migration revision ID 001 preserved as root (down_revision=None unchanged)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All 7 SQLModel classes exist at correct domain paths and are importable
- Migration file is syntactically valid and creates tables in correct FK dependency order
- Plan 02 can proceed: service layer (sms/service.py, phone registration) needs updating to use User/Message models
- Plan 03 can proceed: router, tests, and inngest worker need updating to use new model paths

---
*Phase: 01.1-apply-revised-3nf-schema-and-propagate-throughout-app*
*Completed: 2026-03-07*
