---
phase: 02.6-ensure-research-docs-are-current
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/FEATURES.md
  - .planning/research/PITFALLS.md
autonomous: true
requirements: [DOC-02]

must_haves:
  truths:
    - "FEATURES.md header shows date 2026-03-08 and confidence HIGH"
    - "Every MVP Launch With checklist item in FEATURES.md is annotated ✅ or ⏳ (Phase N)"
    - "FEATURES.md contains no pgvector references"
    - "FEATURES.md has a Phase 3 forward-looking section (MATCH-01, MATCH-02, MATCH-03)"
    - "FEATURES.md has a Phase 4 forward-looking section (STR-03, SEC-05, DEP-03)"
    - "PITFALLS.md contains no pgvector references"
    - "PITFALLS.md has asyncio.to_thread pitfall for Twilio sync SDK"
    - "PITFALLS.md has ALWAYS_ON vs ParentBasedTraceIdRatio pitfall"
    - "PITFALLS.md Pitfall 2 (webhook timeout) is annotated as resolved via Inngest"
  artifacts:
    - path: ".planning/research/FEATURES.md"
      provides: "Accurate feature status reference with ✅/⏳ annotations for Phase 3 planning"
      contains: "Phase 3"
    - path: ".planning/research/PITFALLS.md"
      provides: "Accurate pitfall reference including implementation lessons for Phase 3 planning"
      contains: "asyncio.to_thread"
  key_links:
    - from: ".planning/research/FEATURES.md"
      to: ".planning/REQUIREMENTS.md"
      via: "✅/⏳ annotations match REQUIREMENTS.md traceability table"
      pattern: "✅"
    - from: ".planning/research/PITFALLS.md"
      to: ".planning/STATE.md"
      via: "New pitfalls derived from STATE.md Accumulated Context"
      pattern: "module-level"
---

<objective>
Surgically patch FEATURES.md and PITFALLS.md to reflect the actual implemented state after Phases 01–02.5. Annotate every feature row with ✅/⏳ status, remove all pgvector references, add Phase 3/4 forward-looking sections to FEATURES.md, and add new pitfalls learned during implementation to PITFALLS.md.

Purpose: These docs feed into Phase 3 planning — stale feature status (all unchecked) and missing implementation pitfalls would mislead both the planner and executor.
Output: Two research docs with accurate ✅/⏳ status, zero pgvector references, and Phase 3/4 forward-looking content.
</objective>

<execution_context>
@/Users/ahcarpenter/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ahcarpenter/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02.6-ensure-research-docs-are-current/02.6-RESEARCH.md

<interfaces>
<!-- Canonical ✅/⏳ status from REQUIREMENTS.md traceability -->

Implemented (✅):
- SEC-01 Twilio signature validation
- SEC-02 MessageSid idempotency
- SEC-03 Rate limiting per phone number
- SEC-04 Raw SMS + GPT response audit storage (audit_log table)
- IDN-01 Phone number auto-registration
- IDN-02 created_at timestamp for phone recycling detection
- EXT-01 GPT classification (job / worker / unknown)
- EXT-02 Job field extraction
- EXT-03 Worker goal extraction
- EXT-04 Graceful unknown message SMS reply
- STR-01 Job storage in PostgreSQL
- STR-02 WorkRequest storage in PostgreSQL
- VEC-01 Pinecone embedding write + pinecone_sync_queue retry
- OBS-01 through OBS-04 Full observability stack
- ASYNC-01 Inngest event emit + HTTP 200 webhook return
- ASYNC-03 Inngest Dev Server in Docker Compose
- DEP-01 Docker Compose 8 services
- DEP-02 /health liveness endpoint
- PROD-01 through PROD-08 All production hardening

Pending (⏳):
- SEC-05 STOP/START pass-through — Phase 4
- STR-03 Job poster confirmation SMS — Phase 4
- MATCH-01 Earnings math SQL query — Phase 3
- MATCH-02 Ranked SMS formatter (3-5 results, 160-char) — Phase 3
- MATCH-03 Empty match fallback reply — Phase 3
- ASYNC-02 Full pipeline including matching + outbound SMS — Phase 3/4
- DEP-03 Render.com production deploy validation — Phase 4 (render.yaml exists)

New pitfalls from STATE.md Accumulated Context:
1. Twilio sync SDK in async: asyncio.to_thread() required (Phase 02-gpt)
2. Module-level vars for Inngest DI: set in lifespan, not import time (Phase 02.1-03)
3. OTel ALWAYS_ON vs ParentBasedTraceIdRatio: use ALWAYS_ON (Phase 02.3)
4. OpenSearch replicas=0 for single-node: replicas>0 causes yellow health (Phase 02.3)
5. OTel context not propagated into asyncio.to_thread: span in async context, not in sync fn (Phase 02.3)
6. Circular imports with Prometheus metrics: lazy import inside function (Phase 02.4-01, 02.5)
7. jaeger_query in collector extensions list: not a valid collector extension (Phase 02.3)
8. HEALTHCHECK probing readiness endpoint: /health must be liveness only (Phase 02.5)
9. Async Alembic env.py: asyncio.run() + conn.run_sync() pattern (Phase 01-01)
10. expire_on_commit=False required: async SQLAlchemy can't lazy-load after commit (Phase 01-01)
11. Rate limit SELECT bypasses ORM cache: use raw SQL (Phase 02.1-01)
12. Inngest Function not directly callable in tests: call _handler(ctx) (Phase 02-gpt)
13. Patch target for wrapped OpenAI: patch src.extraction.service.wrap_openai (Phase 02-gpt)

Phase 3 forward-looking content (from RESEARCH.md):
- MATCH-01: SQL query — pay_rate * estimated_duration >= target_earnings, ORDER BY ideal_datetime ASC NULLS LAST, estimated_duration ASC NULLS LAST, LIMIT 5, WHERE pay_rate IS NOT NULL AND estimated_duration IS NOT NULL
- MATCH-02: SMS format — "1. $25/hr 3hr Tue 9am downtown", 3-5 results, 160-char segment awareness
- MATCH-03: Empty match reply — "No jobs match your $X goal yet. We'll text you when one is posted."
- New module: src/matches/service.py (placeholder src/matches/ exists)
- Integration: PipelineOrchestrator WorkRequest branch calls MatchService after WorkRequestRepository.create()

Phase 4 forward-looking content:
- STR-03: Job poster confirmation SMS in PipelineOrchestrator job branch (after Pinecone write)
- SEC-05: STOP/START classification in Inngest process-message before GPT call
- DEP-03: Render.com first deploy — health check, Inngest Cloud connectivity, Pinecone write, Twilio sig validation with production URL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch FEATURES.md</name>
  <files>.planning/research/FEATURES.md</files>
  <action>
Apply surgical patches to FEATURES.md. Read the current file first, then apply all changes below in a single write.

**Header:**
- Change `Researched: 2026-03-05` to `Researched: 2026-03-08`
- Replace the MEDIUM confidence paragraph with: `**Confidence:** HIGH — derived from the actual built system (Phases 01–02.5 complete). Source: REQUIREMENTS.md traceability table, STATE.md, PROJECT.md.`

**Table Stakes table — annotate each row's "Notes" column by prepending status:**
- Twilio X-Twilio-Signature validation → prepend "✅ Implemented (SEC-01). "
- Single-endpoint message classification → prepend "✅ Implemented (EXT-01). "
- Structured data extraction from free-text SMS → prepend "✅ Implemented (EXT-02, EXT-03). "
- Phone number as identity (auto-registration) → prepend "✅ Implemented (IDN-01, IDN-02). "
- SMS confirmation reply to job poster → prepend "⏳ Pending Phase 4 (STR-03). Unknown-branch graceful reply is ✅ Implemented (EXT-04). Full confirmation SMS pending. "
- Ranked job list reply to worker → prepend "⏳ Pending Phase 3 (MATCH-01, MATCH-02). "
- Idempotency on webhook retries → prepend "✅ Implemented (SEC-02). "
- Synchronous HTTP 200 within 10 seconds → prepend "✅ Resolved via Inngest (ASYNC-01). Webhook returns HTTP 200 immediately after event emit; all processing in Inngest function outside Twilio's response window. "
- Graceful handling of malformed / unclassifiable SMS → prepend "✅ Implemented (EXT-04). "
- Rate limiting per phone number → prepend "✅ Implemented (SEC-03). PostgreSQL-backed TTL counters (no Redis). "
- HTTPS-only endpoint → prepend "✅ Infrastructure — Render.com provides TLS. "
- Webhook returns valid TwiML → update to: "✅ Resolved differently — webhook returns HTTP 200 (not TwiML); Twilio is satisfied by HTTP 200. The Twilio Python library is used only for outbound SMS and signature validation, not for generating TwiML responses. "

**Anti-Features section — "Async webhook response" row:**
Update the "Why Problematic" and "Alternative" columns:
- "Why Problematic": "Inngest IS the implemented async pattern. FastAPI BackgroundTasks (the original anti-feature concern) is correctly avoided — Inngest provides better retry, observability, and failure handling than BackgroundTasks."
- "Alternative": "Inngest event-driven processing — ✅ Implemented (ASYNC-01, ASYNC-03). Return HTTP 200 after event emit; all heavy work in Inngest process-message function."

**Feature Dependencies diagram:**
- Find line `[pgvector schema]` and its downstream line — delete both lines
- Replace with: `[Pinecone embedding write]` + `    └──enhances (v2)──> [Earnings Math Matching] (semantic search, if added later)`
- Add: `[Inngest process-message]` + `    └──required by──> [Earnings Math Matching]` + `    └──required by──> [SMS reply flows]`

**MVP Definition — Launch With checklist:**
Replace all `- [ ]` with ✅ or ⏳ annotations:
- `- [ ] **Twilio X-Twilio-Signature validation**` → `- ✅ **Twilio X-Twilio-Signature validation** (SEC-01)`
- `- [ ] **MessageSid idempotency**` → `- ✅ **MessageSid idempotency** (SEC-02)`
- `- [ ] **Rate limiting per phone number**` → `- ✅ **Rate limiting per phone number** (SEC-03, PostgreSQL-backed)`
- `- [ ] **Phone number as identity (auto-registration)**` → `- ✅ **Phone number as identity (auto-registration)** (IDN-01, IDN-02)`
- `- [ ] **Single-endpoint classification (job post vs. worker goal)**` → `- ✅ **Single-endpoint GPT classification** (EXT-01)`
- `- [ ] **Structured extraction: job fields**` → `- ✅ **Structured extraction: job fields** (EXT-02 — description, ideal_datetime, flexibility, estimated_duration, location, pay_rate)`
- `- [ ] **Structured extraction: worker goal**` → `- ✅ **Structured extraction: worker goal** (EXT-03 — target_earnings, target_timeframe)`
- `- [ ] **Graceful fallback for unclassifiable messages**` → `- ✅ **Graceful fallback for unclassifiable messages** (EXT-04 — SMS reply via asyncio.to_thread)`
- `- [ ] **Earnings math matching**` → `- ⏳ **Earnings math matching** (MATCH-01 — Phase 3)`
- `- [ ] **SMS confirmation reply to job poster**` → `- ⏳ **SMS confirmation reply to job poster** (STR-03 — Phase 4; graceful unknown reply ✅ done)`
- `- [ ] **SMS ranked job list reply to worker**` → `- ⏳ **SMS ranked job list reply to worker** (MATCH-02, MATCH-03 — Phase 3)`
- `- [ ] **STOP/START keyword pass-through**` → `- ⏳ **STOP/START keyword pass-through** (SEC-05 — Phase 4)`
- `- [ ] **HTTP response within timeout budget**` → `- ✅ **HTTP response within timeout budget** (ASYNC-01 — Inngest event-driven; returns HTTP 200 immediately)`

**Add Phase 3 Forward-Looking section** (after "Future Consideration (v2+)" block, before Feature Prioritization Matrix):

```markdown
---

## Phase 3: Earnings Math Matching (Next Phase)

**Requirements:** MATCH-01, MATCH-02, MATCH-03

### What Will Be Implemented

| Requirement | Description | Implementation |
|-------------|-------------|----------------|
| MATCH-01 | Earnings math SQL query | `JobRepository.find_matching()` — `WHERE pay_rate IS NOT NULL AND estimated_duration IS NOT NULL AND pay_rate * estimated_duration >= :target_earnings ORDER BY ideal_datetime ASC NULLS LAST, estimated_duration ASC NULLS LAST LIMIT 5` |
| MATCH-02 | Ranked SMS formatter | `MatchService.format_sms()` — condensed format: `1. $25/hr 3hr Tue 9am downtown`, 3–5 results, 160-char segment awareness |
| MATCH-03 | Empty match fallback | `"No jobs match your $X goal yet. We'll text you when one is posted."` |

**New module:** `src/matches/service.py` — MatchService. Placeholder `src/matches/` directory already exists with Match SQLModel stub.

**Integration point:** PipelineOrchestrator WorkRequest branch — after `WorkRequestRepository.create()` flush and `session.commit()`, call `MatchService.find_and_format()`, send result via `asyncio.to_thread(twilio_client.messages.create)`.

**NULL handling policy:** jobs with NULL `pay_rate` or NULL `estimated_duration` are excluded from match results (can't verify earnings goal).

---

## Phase 4: Outbound SMS + Production Deploy

**Requirements:** STR-03, SEC-05, DEP-03

### What Will Be Implemented

| Requirement | Description | Implementation |
|-------------|-------------|----------------|
| STR-03 | Job poster confirmation SMS | Outbound SMS in PipelineOrchestrator job branch (after Pinecone write). Format: "Got it: $25/hr, 3hr, downtown. Reply again to update." via asyncio.to_thread. |
| SEC-05 | STOP/START pass-through | Classify STOP/START keywords in Inngest process-message function before GPT call — never attempt processing or outbound SMS to STOP-registered number |
| DEP-03 | Render.com production deploy | render.yaml Blueprint exists (PROD-04 complete). First deploy validates: /health returns 200, Inngest Cloud connectivity, Pinecone write, Twilio sig validation with production URL. |

---
```

**Feature Prioritization Matrix:**
Annotate the Priority column with ✅/⏳ status:
- Implemented P1 rows: add "(✅ done)" after "P1"
- MATCH-01, MATCH-02, MATCH-03: add "(⏳ Phase 3)"
- Confirmation SMS to poster (STR-03): add "(⏳ Phase 4)"
- STOP/START pass-through: add "(⏳ Phase 4)"

**Sources section at bottom:**
Replace the training-knowledge caveat with: "Sources: REQUIREMENTS.md traceability table, STATE.md, PROJECT.md (HIGH confidence — derived from built system). Updated 2026-03-08."
  </action>
  <verify>
    <automated>grep -c "pgvector" /Users/ahcarpenter/workspace/vici/.planning/research/FEATURES.md</automated>
  </verify>
  <done>FEATURES.md contains 0 references to "pgvector"; header shows 2026-03-08 and HIGH; all 13 MVP checklist items annotated ✅ or ⏳; Phase 3 forward-looking section present with MATCH-01/02/03; Phase 4 forward-looking section present with STR-03/SEC-05/DEP-03; "Async webhook response" anti-feature updated to reflect Inngest.</done>
</task>

<task type="auto">
  <name>Task 2: Patch PITFALLS.md</name>
  <files>.planning/research/PITFALLS.md</files>
  <action>
Apply surgical patches to PITFALLS.md. Read the current file first, then apply all changes below in a single write.

**Header:**
- Change `Researched: 2026-03-05` to `Researched: 2026-03-08`
- Replace the MEDIUM confidence paragraph with: `**Confidence:** HIGH — original pitfalls verified against implementation; new pitfalls derived directly from STATE.md Accumulated Context (Phases 01–02.5). Source: STATE.md decisions, PROJECT.md.`

**DELETE Pitfall 7 entirely** (the section starting "### Pitfall 7: pgvector Index Not Created Before Production Load" through "**Phase to address:**"). This pitfall is entirely inapplicable — no pgvector in use.

**Update Pitfall 2 (Webhook Timeout):**
In the "How to avoid" section, prepend: "✅ **Resolved via Inngest (Phase 01-03, ASYNC-01):** Webhook emits `message.received` Inngest event and returns HTTP 200 immediately. All GPT processing happens in `process-message` Inngest function with 3 retries configured. Idempotency implemented via MessageSid unique constraint (SEC-02)."
At end of the pitfall, add a line: "**Status: ✅ Resolved.** Resolution: Inngest event-driven model (not BackgroundTasks)."

**Update Pitfall 3 (Phone recycling):**
At end, add: "**Status: ✅ Addressed.** `users.created_at` is in the schema; user records use integer FKs."

**Update Pitfall 5 (Classify-then-extract):**
At end, add: "**Status: ✅ Addressed.** Discriminated union `JobExtraction | WorkerExtraction | UnknownMessage` implemented via `beta.chat.completions.parse`. Unknown branch sends graceful SMS reply (EXT-04)."

**Update Pitfall 6 (Sync DB in async):**
At end, add: "**Status: ✅ Addressed.** `create_async_engine` + `AsyncSession` + asyncpg from Phase 01-01. `expire_on_commit=False` set on `async_sessionmaker`."

**Update Pitfall 8 (Rate limiting):**
At end, add: "**Status: ✅ Addressed.** PostgreSQL TTL counter implemented (SEC-03) — no Redis required. Rate limit SELECT uses raw SQL to bypass ORM identity cache (Phase 02.1-01 decision)."

**Update Pitfall 10 (Audit trail):**
At end, add: "**Status: ✅ Addressed.** `audit_log` table stores raw SMS body and raw GPT response per message (SEC-04). `AuditLogRepository` in `src/sms/repository.py`."

**Note Pitfall 4 and 9 as remaining concerns:**
- Pitfall 4 (GPT hallucination): add "**Status: Active concern.** Pydantic discriminated union validates output. GPT hallucination of NULL fields remains a Phase 3 risk — ensure null handling in earnings math query."
- Pitfall 9 (Null handling): add "**Status: Active concern for Phase 3.** Earnings math SQL must explicitly exclude jobs with NULL pay_rate or NULL estimated_duration."

**Technical Debt Patterns table:**
- Remove the `ivfflat over hnsw index` row
- Add row: `| Module-level DI vars set at import time | Faster initial wiring | Inngest function gets uninitialized dependencies (None) | Only acceptable if set inside FastAPI lifespan before first event fires |`

**Integration Gotchas table:**
- Remove the `pgvector extension / CREATE EXTENSION vector` row
- Update the "Twilio reply timing" row: change "Use `BackgroundTasks`" to "Use Inngest — emit event before returning HTTP 200; all processing in Inngest function"

**Performance Traps table:**
- Remove the `No pgvector index` row

**"Looks Done But Isn't" checklist:**
- Remove the `pgvector HNSW index` item
- Change `- [ ] **pgvector HNSW index**...` to nothing (delete)
- Add: `- [ ] **Inngest function wired to orchestrator:** Looks done when function is registered — verify _orchestrator module var is set by lifespan (not None) before first event fires`
- Add: `- [ ] **OTel traces appearing in Jaeger:** Looks done when OTel is initialized — verify with ALWAYS_ON sampler; check OpenSearch replica count (replicas=0 for single-node)`

**Recovery Strategies table:**
- Remove the `pgvector index missing at scale` row

**Pitfall-to-Phase Mapping table:**
- Remove the `pgvector index missing` row

**ADD new "Implementation Pitfalls (Phases 01–02.5 Lessons)" section** after the Pitfall 10 section and before Technical Debt Patterns. Title it `## Implementation Pitfalls (Phases 01–02.5 Lessons Learned)` with preamble "The following pitfalls were discovered during implementation (Phases 01–02.5). They are concrete, project-specific lessons derived from STATE.md Accumulated Context."

Add each of the following pitfalls as subsections:

---

**### Pitfall 11: Twilio Sync SDK Blocks Async Event Loop**

What goes wrong: `twilio_client.messages.create(...)` called directly inside `async def` blocks the asyncio event loop for the duration of the HTTP call.

Why it happens: The official `twilio-python` SDK uses `requests` (blocking sync). Easy to miss because it works fine in synchronous code.

How to avoid: Always wrap in `asyncio.to_thread()`: `await asyncio.to_thread(twilio_client.messages.create, to=..., from_=..., body=...)`. Create the span in the async context wrapping the call, not inside the sync function — OTel context is not propagated into threads.

Phase addressed: Phase 02-gpt (Unknown branch), applicable to Phase 4 outbound SMS.

---

**### Pitfall 12: Module-Level Vars for Inngest DI**

What goes wrong: `_orchestrator = PipelineOrchestrator(...)` at module top-level in `inngest_client.py` fails — the DI graph isn't built at import time.

Why it happens: Inngest functions are registered at module import; they can't receive injected dependencies directly from FastAPI's Depends system.

How to avoid: Declare `_orchestrator: PipelineOrchestrator | None = None` at module level. Set the var inside `app.lifespan()` after building the full DI graph. Access via `assert _orchestrator is not None` at the start of the Inngest function handler. Same pattern applies to `_openai_client`.

Phase addressed: Phase 02.1-03, Phase 02.5.

---

**### Pitfall 13: OTel ALWAYS_ON vs ParentBasedTraceIdRatio Sampler**

What goes wrong: `ParentBasedTraceIdRatio` sampler causes ambiguous sampling — if there's no parent span (most inbound requests), sampling falls back to root sampler; if there is a parent, sampling is based on parent's decision. Traces go missing inconsistently.

How to avoid: Use `ALWAYS_ON` sampler for unambiguous local coverage. `ParentBasedTraceIdRatio` is only useful when you want to inherit sampling decisions from an upstream service — not applicable here.

Phase addressed: Phase 02.3.

---

**### Pitfall 14: OpenSearch replicas > 0 on Single-Node**

What goes wrong: Default `number_of_replicas: 1` in index template — single-node OpenSearch can't allocate replica shards → index health stays yellow → Jaeger index creation fails silently.

How to avoid: Set `number_of_replicas: 0` in the OpenSearch index template for local dev. Jaeger's index template config (in jaeger.yaml) controls this.

Phase addressed: Phase 02.3.

---

**### Pitfall 15: Circular Imports with Prometheus Metrics**

What goes wrong: Importing `from src.metrics import gpt_call_counter` at module top-level in `service.py` creates a circular import: `service.py` → `metrics.py` → (possibly) back through the DI graph.

How to avoid: Lazy-import Prometheus metrics inside the function that uses them: `from src.metrics import gpt_call_counter` inside the `process()` method body. Consistent with `if TYPE_CHECKING` pattern for types.

Phase addressed: Phase 02.4-01, Phase 02.5.

---

**### Pitfall 16: Inngest Function Not Directly Callable in Tests**

What goes wrong: Calling `await process_message(ctx)` in tests raises an error — the Inngest `Function` wrapper is not directly callable.

How to avoid: Call `await process_message._handler(ctx)` in tests. The `_handler` attribute is the raw async function before Inngest wraps it.

Phase addressed: Phase 02-gpt.

---

**### Pitfall 17: Patch Target for Braintrust-Wrapped OpenAI Client**

What goes wrong: `mock.patch("braintrust.wrap_openai")` does not intercept the call — `service.py` uses a direct import: `from braintrust import wrap_openai`. The mock patches the source, not the import site.

How to avoid: Always patch at the import site in the module under test: `mock.patch("src.extraction.service.wrap_openai")`.

Phase addressed: Phase 02-gpt.

---

**### Pitfall 18: HEALTHCHECK Must Use Liveness Endpoint**

What goes wrong: Docker HEALTHCHECK that probes a readiness endpoint (one that checks DB connectivity) fails during DB startup → container restarts in a loop before the database is healthy.

How to avoid: `/health` must return HTTP 200 based on liveness only (no DB connection). DB connectivity is a readiness concern, not a liveness concern. Twilio's 15-second timeout means a DB-dependent health check can also cascade into webhook failures.

Phase addressed: Phase 02.5.

---

**Sources section at bottom:**
Add to sources: "PRIMARY: STATE.md Accumulated Context (40+ implementation decisions), PROJECT.md, REQUIREMENTS.md (HIGH confidence — derived from built system). Updated 2026-03-08."
  </action>
  <verify>
    <automated>grep -c "pgvector" /Users/ahcarpenter/workspace/vici/.planning/research/PITFALLS.md</automated>
  </verify>
  <done>PITFALLS.md contains 0 references to "pgvector"; header shows 2026-03-08 and HIGH; Pitfall 7 (pgvector index) is deleted; Pitfall 2 annotated as ✅ Resolved via Inngest; Pitfalls 3, 5, 6, 8, 10 annotated with ✅ Addressed; Pitfalls 4 and 9 noted as active Phase 3 concerns; new Pitfalls 11–18 added covering asyncio.to_thread, module-level DI, ALWAYS_ON sampler, OpenSearch replicas, circular imports, Inngest testing, patch targets, HEALTHCHECK; ivfflat/pgvector rows removed from Technical Debt, Gotchas, Performance Traps, Recovery, Mapping tables.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -r "pgvector" .planning/research/FEATURES.md .planning/research/PITFALLS.md` — must return 0 matches
2. `grep "2026-03-08" .planning/research/FEATURES.md .planning/research/PITFALLS.md` — must match header lines in both
3. `grep "✅" .planning/research/FEATURES.md | wc -l` — must be >= 10 (all implemented items annotated)
4. `grep "Phase 3" .planning/research/FEATURES.md` — must match (forward-looking section exists)
5. `grep "asyncio.to_thread" .planning/research/PITFALLS.md` — must match (Pitfall 11)
6. `grep "ALWAYS_ON" .planning/research/PITFALLS.md` — must match (Pitfall 13)
7. `grep "Pitfall 7" .planning/research/PITFALLS.md` — must return 0 (deleted)
</verification>

<success_criteria>
- FEATURES.md: 0 pgvector references, date 2026-03-08, confidence HIGH, all 13 MVP checklist items annotated ✅/⏳, "Async webhook response" anti-feature updated to reflect Inngest, Phase 3 and Phase 4 forward-looking sections added
- PITFALLS.md: 0 pgvector references, date 2026-03-08, confidence HIGH, Pitfall 7 deleted, Pitfalls 2/3/5/6/8/10 annotated with resolution status, 8 new implementation pitfalls (11–18) added from STATE.md decisions
</success_criteria>

<output>
After completion, create `.planning/phases/02.6-ensure-research-docs-are-current/02.6-02-SUMMARY.md`
</output>
