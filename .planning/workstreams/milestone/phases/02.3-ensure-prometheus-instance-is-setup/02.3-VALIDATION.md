---
phase: 02.4
slug: ensure-prometheus-instance-is-setup
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-08
---

# Phase 02.4 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest + pytest-asyncio (asyncio_mode = auto) |
| **Config file** | pyproject.toml `[tool.pytest.ini_options]` |
| **Quick run command** | `uv run pytest tests/extraction/ -x -q` |
| **Full suite command** | `uv run pytest tests/ -q` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `uv run pytest tests/extraction/ -x -q`
- **After every plan wave:** Run `uv run pytest tests/ -q`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 02.4-01-01 | 01 | 0 | METRICS-01,02,03 | unit stubs | `uv run pytest tests/extraction/test_metrics.py -x -q` | ❌ W0 | ⬜ pending |
| 02.4-01-02 | 01 | 1 | METRICS-01 | unit | `uv run pytest tests/extraction/ -x -k "gpt_metric"` | ❌ W0 | ⬜ pending |
| 02.4-01-03 | 01 | 1 | METRICS-02 | unit | `uv run pytest tests/extraction/ -x -k "gpt_duration"` | ❌ W0 | ⬜ pending |
| 02.4-01-04 | 01 | 1 | METRICS-03 | unit | `uv run pytest tests/ -x -k "pinecone_gauge"` | ❌ W0 | ⬜ pending |
| 02.4-01-05 | 01 | 2 | INFRA-01 | manual smoke | `docker compose up -d && curl http://localhost:9090/-/healthy && curl http://localhost:3000/api/health` | N/A | ⬜ pending |
| 02.4-01-06 | 01 | 2 | INFRA-02 | manual smoke | `curl http://localhost:9090/api/v1/targets` | N/A | ⬜ pending |
| 02.4-01-07 | 01 | 2 | INFRA-03 | manual smoke | Access `localhost:3000`, verify datasource green | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/extraction/test_metrics.py` — stubs for METRICS-01, METRICS-02, METRICS-03
- [ ] `tests/conftest.py` — add Prometheus registry cleanup fixture to prevent duplicate registration errors across test session

*Existing pytest-asyncio infrastructure covers all other needs.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Prometheus + Grafana containers start and are reachable | INFRA-01 | Docker networking requires live containers | `docker compose up -d && curl http://localhost:9090/-/healthy && curl http://localhost:3000/api/health` |
| `app` target is UP in Prometheus | INFRA-02 | Requires live Prometheus scraping live app | `curl http://localhost:9090/api/v1/targets` — check `app` job status=up |
| Grafana datasource points to Prometheus and returns data | INFRA-03 | Requires browser/UI verification | Access `localhost:3000`, navigate to Connections > Data sources, verify Prometheus datasource shows green |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
