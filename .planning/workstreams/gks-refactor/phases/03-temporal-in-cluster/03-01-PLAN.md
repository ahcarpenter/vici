---
phase: 03-temporal-in-cluster
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/components/opensearch.py
autonomous: true
requirements:
  - TEMPORAL-02
  - TEMPORAL-03
must_haves:
  truths:
    - "OpenSearch pod is Running in the observability namespace"
    - "OpenSearch responds to health check on port 9200"
    - "OpenSearch is single-node with number_of_replicas: 0 index template"
  artifacts:
    - path: "infra/components/opensearch.py"
      provides: "OpenSearch Helm release as Pulumi component"
      exports: ["opensearch_release"]
  key_links:
    - from: "infra/components/opensearch.py"
      to: "opensearch-cluster-master.observability.svc.cluster.local:9200"
      via: "Helm release creates ClusterIP service"
      pattern: "opensearch-cluster-master"
---

<objective>
Deploy OpenSearch in the observability namespace as a single-node instance for Temporal visibility and future Jaeger use.

Purpose: OpenSearch must be ready before the Temporal Helm release (TEMPORAL-03). Separating this into its own plan ensures OpenSearch is fully healthy before Plan 02 wires Temporal to it.
Output: `infra/components/opensearch.py` with `opensearch_release` export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gks-refactor/ROADMAP.md
@.planning/workstreams/gks-refactor/REQUIREMENTS.md
@.planning/workstreams/gks-refactor/phases/03-temporal-in-cluster/03-CONTEXT.md
@.planning/workstreams/gks-refactor/phases/03-temporal-in-cluster/03-RESEARCH.md

@infra/components/migration.py
@infra/components/namespaces.py
@infra/components/secrets.py
@infra/__main__.py

<interfaces>
<!-- From infra/components/namespaces.py -->
namespaces: dict — keys include "observability", provides Namespace resource for depends_on
k8s_provider: k8s.Provider — the GKE cluster provider, pass via opts=ResourceOptions(provider=k8s_provider)

<!-- From infra/components/secrets.py (Helm release pattern) -->
Pattern: k8s.helm.v3.Release with repository_opts, pinned chart version, create_namespace=False, opts=ResourceOptions(provider=k8s_provider, depends_on=[...])
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create OpenSearch Pulumi component</name>
  <files>infra/components/opensearch.py</files>
  <read_first>
    - infra/components/migration.py (Auth Proxy sidecar pattern, constant naming convention)
    - infra/components/secrets.py (Helm release pattern via Pulumi)
    - infra/components/namespaces.py (k8s_provider, namespaces dict)
    - infra/__main__.py (import pattern)
  </read_first>
  <action>
Create `infra/components/opensearch.py` with the following:

**Constants at module top (per established pattern):**
- `_OPENSEARCH_CHART_VERSION = "2.37.0"` (pinned 2.x — OpenSearch 3.x breaks Temporal ES client, per D-08)
- `_OPENSEARCH_REPO = "https://opensearch-project.github.io/helm-charts/"` (per D-08)
- `_OPENSEARCH_JAVA_OPTS = "-Xmx512m -Xms512m"`
- `_OPENSEARCH_MEMORY_REQUEST = "1Gi"`
- `_OPENSEARCH_MEMORY_LIMIT = "2Gi"`
- `_OPENSEARCH_CPU_REQUEST = "500m"`
- `_OPENSEARCH_CPU_LIMIT = "1"`
- `_OPENSEARCH_STORAGE_SIZE = "10Gi"`

**Imports:**
```python
import pulumi
import pulumi_kubernetes as k8s
from pulumi import ResourceOptions

from components.namespaces import k8s_provider, namespaces
```

**OpenSearch Helm release** using `k8s.helm.v3.Release`:
- `chart="opensearch"`, `version=_OPENSEARCH_CHART_VERSION`
- `repository_opts` with `repo=_OPENSEARCH_REPO`
- `namespace="observability"`, `create_namespace=False`
- Helm values:
  - `singleNode: True` (D-07 — single-node safe)
  - `replicas: 1`
  - `opensearchJavaOpts: _OPENSEARCH_JAVA_OPTS`
  - `resources.requests`: `cpu: _OPENSEARCH_CPU_REQUEST`, `memory: _OPENSEARCH_MEMORY_REQUEST`
  - `resources.limits`: `cpu: _OPENSEARCH_CPU_LIMIT`, `memory: _OPENSEARCH_MEMORY_LIMIT`
  - `persistence.enabled: True`, `persistence.size: _OPENSEARCH_STORAGE_SIZE`
  - `config.opensearch.yml`: `"plugins.security.disabled: true\n"` (cluster-internal only, acceptable for v1.0)
- `opts=ResourceOptions(provider=k8s_provider, depends_on=[namespaces["observability"]])`

**Index template Job** — Create a `k8s.batch.v1.Job` named `"opensearch-index-template"` that runs after the OpenSearch release is ready. This Job uses a `curlimages/curl:8.7.1` image to POST an index template to OpenSearch that sets `number_of_replicas: 0` (per D-07/OBS-01):
- `namespace="observability"`
- `restart_policy="Never"`, `backoff_limit=0`, `ttl_seconds_after_finished=300`
- Container command: `["sh", "-c"]`
- Container args: a single string that:
  1. Waits for OpenSearch to be ready: `until curl -sf http://opensearch-cluster-master:9200/_cluster/health; do sleep 5; done`
  2. Creates an index template: `curl -X PUT http://opensearch-cluster-master:9200/_index_template/single_node_defaults -H 'Content-Type: application/json' -d '{"index_patterns":["*"],"template":{"settings":{"number_of_replicas":0}},"priority":1}'`
- `depends_on=[opensearch_release]`

**Exports:**

Add a module-level constant (not underscore-prefixed, so it is importable by other components):
```python
OPENSEARCH_SERVICE_HOST = "opensearch-cluster-master.observability.svc.cluster.local"
```

```python
pulumi.export("opensearch_service", f"{OPENSEARCH_SERVICE_HOST}:9200")
```

Export both `opensearch_release` and `OPENSEARCH_SERVICE_HOST` at module level for import by other components (e.g., `temporal.py`).
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && python -c "import ast; ast.parse(open('infra/components/opensearch.py').read()); print('SYNTAX OK')" && ruff check infra/components/opensearch.py && ruff format --check infra/components/opensearch.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_OPENSEARCH_CHART_VERSION" infra/components/opensearch.py` returns at least 1
    - `grep "2.37.0" infra/components/opensearch.py` matches (pinned chart version)
    - `grep "singleNode" infra/components/opensearch.py` matches
    - `grep "number_of_replicas" infra/components/opensearch.py` matches (index template Job)
    - `grep "plugins.security.disabled: true" infra/components/opensearch.py` matches
    - `grep "opensearch_release" infra/components/opensearch.py` matches (export)
    - `grep "OPENSEARCH_SERVICE_HOST" infra/components/opensearch.py` matches (importable constant)
    - `grep "observability" infra/components/opensearch.py` matches (correct namespace)
    - `python -c "import ast; ast.parse(open('infra/components/opensearch.py').read())"` exits 0
  </acceptance_criteria>
  <done>infra/components/opensearch.py exists with OpenSearch Helm release, index template Job, and opensearch_release export. All constants are named, no magic numbers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cluster-internal traffic to OpenSearch | Pods in any namespace can reach OpenSearch ClusterIP on port 9200 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | OpenSearch ClusterIP service | accept | ClusterIP-only (not exposed via Ingress); security plugin disabled is acceptable for v1.0 internal-only use. Post-v1.0 hardening will re-enable TLS + auth. |
| T-03-02 | Tampering | OpenSearch data | accept | No external access; only Temporal server and Jaeger (Phase 4) write to OpenSearch. Namespace network policies are a post-v1.0 item. |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('infra/components/opensearch.py').read())"` — syntax valid
- `grep "2.37.0" infra/components/opensearch.py` — chart version pinned to 2.x
- `grep "singleNode" infra/components/opensearch.py` — single-node mode enabled
</verification>

<success_criteria>
- infra/components/opensearch.py exists and is syntactically valid Python
- OpenSearch Helm release pinned to chart version 2.37.0
- Index template Job ensures number_of_replicas: 0
- Component follows established patterns (constants at top, ResourceOptions with provider and depends_on)
</success_criteria>

<output>
After completion, create `.planning/workstreams/gks-refactor/phases/03-temporal-in-cluster/03-01-SUMMARY.md`
</output>
