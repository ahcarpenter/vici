---
phase: 03-temporal-in-cluster
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - infra/__main__.py
autonomous: false
requirements:
  - TEMPORAL-01
  - TEMPORAL-02
  - TEMPORAL-03
  - TEMPORAL-04
  - TEMPORAL-05
  - TEMPORAL-06
must_haves:
  truths:
    - "infra/__main__.py imports from both opensearch.py and temporal.py"
    - "pulumi preview completes without errors"
    - "Temporal pods are Running in the temporal namespace after pulumi up"
    - "OpenSearch pod is Running in the observability namespace"
    - "Temporal UI is accessible via port-forward"
  artifacts:
    - path: "infra/__main__.py"
      provides: "Updated entry point with opensearch and temporal component imports"
      contains: "from components.temporal import"
  key_links:
    - from: "infra/__main__.py"
      to: "infra/components/opensearch.py"
      via: "import registration"
      pattern: "from components.opensearch import"
    - from: "infra/__main__.py"
      to: "infra/components/temporal.py"
      via: "import registration"
      pattern: "from components.temporal import"
---

<objective>
Wire OpenSearch and Temporal components into the Pulumi entry point, run pulumi preview, and verify the full deployment.

Purpose: Complete the Phase 3 integration by registering new components and validating the entire Temporal stack works end-to-end.
Output: Updated `infra/__main__.py`, verified deployment.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gks-refactor/ROADMAP.md
@.planning/workstreams/gks-refactor/phases/03-temporal-in-cluster/03-CONTEXT.md
@.planning/workstreams/gks-refactor/phases/03-temporal-in-cluster/03-01-SUMMARY.md
@.planning/workstreams/gks-refactor/phases/03-temporal-in-cluster/03-02-SUMMARY.md

@infra/__main__.py
@infra/components/opensearch.py
@infra/components/temporal.py

<interfaces>
<!-- From infra/components/opensearch.py (Plan 01) -->
opensearch_release: k8s.helm.v3.Release

<!-- From infra/components/temporal.py (Plan 02) -->
temporal_schema_job: k8s.batch.v1.Job
temporal_release: k8s.helm.v3.Release
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register opensearch and temporal components in __main__.py</name>
  <files>infra/__main__.py</files>
  <read_first>
    - infra/__main__.py (current state — see existing import pattern)
    - infra/components/opensearch.py (confirm export names)
    - infra/components/temporal.py (confirm export names)
  </read_first>
  <action>
Add two import lines to `infra/__main__.py`, following the existing pattern of importing named exports with `# noqa: F401` comments:

After the line `from components.migration import migration_job  # noqa: F401`:

```python
from components.opensearch import opensearch_release  # noqa: F401
from components.temporal import temporal_schema_job, temporal_release  # noqa: F401
```

Keep the existing comment block at the bottom unchanged. The imports trigger Pulumi resource registration — no additional code needed.
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && python -c "import ast; ast.parse(open('infra/__main__.py').read()); print('SYNTAX OK')" && ruff check infra/__main__.py && ruff format --check infra/__main__.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from components.opensearch import opensearch_release" infra/__main__.py` matches
    - `grep "from components.temporal import temporal_schema_job, temporal_release" infra/__main__.py` matches
    - `python -c "import ast; ast.parse(open('infra/__main__.py').read())"` exits 0
  </acceptance_criteria>
  <done>infra/__main__.py imports opensearch_release, temporal_schema_job, and temporal_release. File is syntactically valid.</done>
</task>

<task type="auto">
  <name>Task 2: Verify TEMPORAL_HOST secret definition exists (TEMPORAL-05)</name>
  <files>infra/components/secrets.py</files>
  <read_first>
    - infra/components/secrets.py (confirm _SECRET_DEFINITIONS contains temporal-host tuple)
  </read_first>
  <action>
Read `infra/components/secrets.py` and confirm the `_SECRET_DEFINITIONS` list contains the tuple `("temporal-host", "vici", "temporal-host")`. This entry already exists (provisioned in Phase 2). No code changes needed — this task is a verification-only read.

If the entry is missing (unexpected), add it to `_SECRET_DEFINITIONS` following the existing tuple pattern.

Additionally, confirm that the GCP Secret Manager value for `temporal-host` must be set to `temporal-frontend.temporal.svc.cluster.local:7233` in the `{ENV}/temporal-host` secret. If the value has not been set yet, the executor must set it via:
```bash
echo -n "temporal-frontend.temporal.svc.cluster.local:7233" | gcloud secrets versions add "{ENV}/temporal-host" --data-file=-
```
Replace `{ENV}` with the current environment slug from `infra/config.py`.
  </action>
  <verify>
    <automated>grep -q 'temporal-host.*vici.*temporal-host' infra/components/secrets.py && echo "TEMPORAL_HOST in _SECRET_DEFINITIONS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "temporal-host" infra/components/secrets.py` matches in `_SECRET_DEFINITIONS`
    - GCP Secret Manager value set (verified by human in Task 3)
  </acceptance_criteria>
  <done>TEMPORAL_HOST secret definition confirmed in _SECRET_DEFINITIONS. GCP Secret Manager value set to temporal-frontend.temporal.svc.cluster.local:7233.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Temporal stack deployment</name>
  <files>infra/__main__.py</files>
  <action>
Human verifies the full Phase 3 Temporal stack deployment by running pulumi up and checking cluster state. No code changes in this task — verification only.
  </action>
  <what-built>Complete Phase 3 Temporal stack: OpenSearch in observability namespace, Temporal schema migration Job, Temporal Server Helm release with UI, all wired into Pulumi entry point.</what-built>
  <how-to-verify>
    1. Run `cd /Users/ahcarpenter/workspace/vici/infra && pulumi preview --stack gks-refactor` — should show new resources (OpenSearch release, index template Job, schema migration Job, Temporal Helm release) with no errors
    2. Run `cd /Users/ahcarpenter/workspace/vici/infra && pulumi up --stack gks-refactor --yes` — deploy all resources
    3. Verify OpenSearch: `kubectl get pods -n observability` — opensearch pod should be Running
    4. Verify schema Job: `kubectl get jobs -n temporal` — temporal-schema-migration should show SUCCEEDED
    5. Verify Temporal pods: `kubectl get pods -n temporal` — frontend, history, matching, worker pods should be Running
    6. Verify Temporal UI: `kubectl port-forward svc/temporal-web 8080:8080 -n temporal` then open http://localhost:8080 — UI should load and show registered namespaces
    7. Verify TEMPORAL_HOST secret exists: `kubectl get secret temporal-host -n vici -o jsonpath='{.data.TEMPORAL_HOST}' | base64 -d` should output `temporal-frontend.temporal.svc.cluster.local:7233`
    8. (Optional) Test workflow: `kubectl run temporal-test --image=temporalio/admin-tools --rm -it --restart=Never -n temporal -- temporal workflow run --workflow-type helloWorld --task-queue test --address temporal-frontend.temporal.svc.cluster.local:7233`
  </how-to-verify>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && grep "from components.temporal import" infra/__main__.py && grep "from components.opensearch import" infra/__main__.py && echo "WIRED OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from components.opensearch import" infra/__main__.py` matches
    - `grep "from components.temporal import" infra/__main__.py` matches
    - `pulumi preview --stack gks-refactor` exits 0 (verified by human)
    - `kubectl get pods -n temporal` shows Running pods (verified by human)
    - `kubectl get pods -n observability` shows Running OpenSearch pod (verified by human)
  </acceptance_criteria>
  <done>Human has verified: pulumi preview clean, OpenSearch Running, schema Job SUCCEEDED, Temporal pods Running, Temporal UI accessible, TEMPORAL_HOST secret correct.</done>
  <resume-signal>Type "approved" if all checks pass, or describe which checks failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | This plan only wires existing components and verifies — no new trust boundaries introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Tampering | __main__.py import ordering | accept | Pulumi resolves its own DAG from depends_on; import order is cosmetic |
</threat_model>

<verification>
- `python -c "import ast; ast.parse(open('infra/__main__.py').read())"` — syntax valid
- `grep "opensearch" infra/__main__.py` — OpenSearch component registered
- `grep "temporal" infra/__main__.py` — Temporal component registered
- `pulumi preview` completes with expected new resources
</verification>

<success_criteria>
- infra/__main__.py imports opensearch and temporal components
- pulumi preview shows no errors
- After pulumi up: OpenSearch Running, schema Job SUCCEEDED, Temporal pods Running, Temporal UI accessible
- TEMPORAL_HOST secret resolves to temporal-frontend.temporal.svc.cluster.local:7233
</success_criteria>

<output>
After completion, create `.planning/workstreams/gks-refactor/phases/03-temporal-in-cluster/03-03-SUMMARY.md`
</output>
