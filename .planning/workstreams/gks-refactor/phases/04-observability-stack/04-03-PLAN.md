---
phase: 04-observability-stack
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - infra/__main__.py
  - infra/components/secrets.py
autonomous: true
requirements:
  - OBS-05
must_haves:
  truths:
    - "All observability components are registered in the Pulumi entry point"
    - "OTEL_EXPORTER_OTLP_ENDPOINT secret is synced from GCP Secret Manager to the vici namespace"
  artifacts:
    - path: "infra/__main__.py"
      provides: "Observability component registration"
      contains: "from components.jaeger import"
    - path: "infra/components/secrets.py"
      provides: "OTEL ExternalSecret resource"
      contains: "OTEL_EXPORTER_OTLP_ENDPOINT"
  key_links:
    - from: "infra/__main__.py"
      to: "infra/components/jaeger.py"
      via: "import registration"
      pattern: "from components\\.jaeger import"
    - from: "infra/__main__.py"
      to: "infra/components/prometheus.py"
      via: "import registration"
      pattern: "from components\\.prometheus import"
---

<objective>
Wire Jaeger and Prometheus components into the Pulumi entry point and create the OTEL_EXPORTER_OTLP_ENDPOINT ExternalSecret.

Purpose: Makes all observability resources deployable via `pulumi up` and ensures the app can discover the trace collector endpoint.
Output: Updated `infra/__main__.py` with new imports; updated `infra/components/secrets.py` with OTEL ExternalSecret.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gks-refactor/phases/04-observability-stack/04-CONTEXT.md
@infra/__main__.py
@infra/components/secrets.py
@infra/components/jaeger.py
@infra/components/prometheus.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register observability components in __main__.py</name>
  <files>infra/__main__.py</files>
  <read_first>
    - infra/__main__.py
    - infra/components/jaeger.py
    - infra/components/prometheus.py
  </read_first>
  <action>
Add two import lines to `infra/__main__.py` after the existing `from components.temporal import` line:

```python
from components.jaeger import jaeger_collector_deployment, jaeger_query_deployment  # noqa: F401
from components.prometheus import kube_prometheus_release, fastapi_service_monitor  # noqa: F401
```

Follow the existing pattern: each import triggers resource registration. The `# noqa: F401` comment suppresses unused-import warnings (same as all other lines in the file).
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && grep -c "from components.jaeger import\|from components.prometheus import" infra/__main__.py</automated>
  </verify>
  <acceptance_criteria>
    - infra/__main__.py contains `from components.jaeger import jaeger_collector_deployment, jaeger_query_deployment`
    - infra/__main__.py contains `from components.prometheus import kube_prometheus_release, fastapi_service_monitor`
    - Both lines have `# noqa: F401` comments
    - File still parses as valid Python
  </acceptance_criteria>
  <done>Both observability component modules are registered in the Pulumi entry point and will be included in `pulumi up`.</done>
</task>

<task type="auto">
  <name>Task 2: Add OTEL_EXPORTER_OTLP_ENDPOINT ExternalSecret</name>
  <files>infra/components/secrets.py</files>
  <read_first>
    - infra/components/secrets.py
  </read_first>
  <action>
Read `infra/components/secrets.py` to understand the existing ExternalSecret pattern. Add a new ExternalSecret resource for `OTEL_EXPORTER_OTLP_ENDPOINT` in the `vici` namespace.

The secret value in GCP Secret Manager should be `http://jaeger-collector.observability.svc.cluster.local:4317`.

Follow the exact same pattern as existing ExternalSecret resources in the file. The new resource should:
- Be named `"otel-exporter-otlp-endpoint"` (or follow the naming convention already in the file)
- Be in namespace `"vici"`
- Reference the `vici` namespace SecretStore
- Map GCP Secret Manager key `{env}/otel-exporter-otlp-endpoint` to Kubernetes secret key `OTEL_EXPORTER_OTLP_ENDPOINT`
- Use `depends_on` on the vici SecretStore (follow existing pattern)

If the existing code uses a loop or dict to define ExternalSecrets, add the new entry to that structure instead of creating a standalone resource. Match the existing pattern exactly.

**Important:** The GCP Secret Manager value (`http://jaeger-collector.observability.svc.cluster.local:4317`) must be populated manually by the operator or via `gcloud secrets versions add`. This is a user_setup concern — the ExternalSecret CR just defines the sync.
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && grep -c "OTEL_EXPORTER_OTLP_ENDPOINT\|otel-exporter-otlp-endpoint" infra/components/secrets.py</automated>
  </verify>
  <acceptance_criteria>
    - infra/components/secrets.py contains `OTEL_EXPORTER_OTLP_ENDPOINT` or `otel-exporter-otlp-endpoint`
    - The ExternalSecret follows the same pattern as existing ones in the file
    - The resource targets the `vici` namespace
    - File still parses as valid Python
  </acceptance_criteria>
  <done>OTEL_EXPORTER_OTLP_ENDPOINT ExternalSecret exists in secrets.py, targeting the vici namespace SecretStore, ready to sync from GCP Secret Manager.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GCP Secret Manager -> K8s Secret | ESO syncs secrets; IAM controls access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | OTEL endpoint secret value | accept | Value is a cluster-internal URL (not a credential); low sensitivity |
</threat_model>

<verification>
- `grep "from components.jaeger import" infra/__main__.py` returns a match
- `grep "from components.prometheus import" infra/__main__.py` returns a match
- `grep "OTEL_EXPORTER_OTLP_ENDPOINT\|otel-exporter-otlp-endpoint" infra/components/secrets.py` returns at least 1 match
- Both files parse as valid Python
</verification>

<success_criteria>
- infra/__main__.py imports both jaeger and prometheus components
- infra/components/secrets.py has OTEL ExternalSecret
- `pulumi preview` would include all observability resources (not verified here — deferred to deploy)
</success_criteria>

<output>
After completion, create `.planning/workstreams/gks-refactor/phases/04-observability-stack/04-03-SUMMARY.md`
</output>
