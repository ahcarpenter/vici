---
phase: 04-observability-stack
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - infra/__main__.py
  - infra/components/secrets.py
autonomous: true
requirements:
  - OBS-05
must_haves:
  truths:
    - "All observability components are registered in the Pulumi entry point"
    - "OTEL_EXPORTER_OTLP_ENDPOINT secret is synced from GCP Secret Manager to the observability namespace"
  artifacts:
    - path: "infra/__main__.py"
      provides: "Observability component registration"
      contains: "from components.jaeger import"
    - path: "infra/components/secrets.py"
      provides: "OTEL ExternalSecret resource"
      contains: "OTEL_EXPORTER_OTLP_ENDPOINT"
  key_links:
    - from: "infra/__main__.py"
      to: "infra/components/jaeger.py"
      via: "import registration"
      pattern: "from components\\.jaeger import"
    - from: "infra/__main__.py"
      to: "infra/components/prometheus.py"
      via: "import registration"
      pattern: "from components\\.prometheus import"
---

<objective>
Wire Jaeger and Prometheus components into the Pulumi entry point and verify the OTEL_EXPORTER_OTLP_ENDPOINT ExternalSecret is correctly defined.

Purpose: Makes all observability resources deployable via `pulumi up` and ensures the app can discover the trace collector endpoint.
Output: Updated `infra/__main__.py` with new imports; verified `infra/components/secrets.py` already has the OTEL ExternalSecret.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gks-refactor/phases/04-observability-stack/04-CONTEXT.md
@infra/__main__.py
@infra/components/secrets.py
@infra/components/jaeger.py
@infra/components/prometheus.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register observability components in __main__.py</name>
  <files>infra/__main__.py</files>
  <read_first>
    - infra/__main__.py
    - infra/components/jaeger.py
    - infra/components/prometheus.py
  </read_first>
  <action>
Add two import lines to `infra/__main__.py` after the existing `from components.temporal import` line:

```python
from components.jaeger import jaeger_collector_deployment, jaeger_query_deployment  # noqa: F401
from components.prometheus import kube_prometheus_release, fastapi_service_monitor  # noqa: F401
```

Follow the existing pattern: each import triggers resource registration. The `# noqa: F401` comment suppresses unused-import warnings (same as all other lines in the file).
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && grep -q "from components.jaeger import" infra/__main__.py && grep -q "from components.prometheus import" infra/__main__.py && echo "ok"</automated>
  </verify>
  <acceptance_criteria>
    - infra/__main__.py contains `from components.jaeger import jaeger_collector_deployment, jaeger_query_deployment`
    - infra/__main__.py contains `from components.prometheus import kube_prometheus_release, fastapi_service_monitor`
    - Both lines have `# noqa: F401` comments
    - File still parses as valid Python
  </acceptance_criteria>
  <done>Both observability component modules are registered in the Pulumi entry point and will be included in `pulumi up`.</done>
</task>

<task type="auto">
  <name>Task 2: Verify OTEL ExternalSecret exists and update namespace to vici</name>
  <files>infra/components/secrets.py</files>
  <read_first>
    - infra/components/secrets.py
  </read_first>
  <action>
The `_SECRET_DEFINITIONS` list in `infra/components/secrets.py` already contains an entry for the OTEL endpoint:

```python
("otel-exporter-otlp-endpoint", "observability", "otel-exporter-otlp-endpoint"),
```

This entry is at line 29 of the file. It currently targets the `observability` namespace. Update the namespace from `"observability"` to `"vici"` so the app pods in the `vici` namespace can mount the secret directly:

```python
("otel-exporter-otlp-endpoint", "vici", "otel-exporter-otlp-endpoint"),
```

The existing loop at lines 132-154 already creates the ExternalSecret CR using this tuple, so no other changes are needed. The loop produces:
- Resource name: `ext-secret-otel-exporter-otlp-endpoint`
- Metadata: `name="otel-exporter-otlp-endpoint"`, `namespace="vici"`
- secretKey: `OTEL_EXPORTER_OTLP_ENDPOINT` (derived from slug via `.upper().replace("-", "_")`)
- remoteRef key: `{ENV}/otel-exporter-otlp-endpoint`
- depends_on: `secret_stores["vici"]` and `sm_secrets["otel-exporter-otlp-endpoint"]`

**Important:** The GCP Secret Manager value (`http://jaeger-collector.observability.svc.cluster.local:4317`) must be populated manually by the operator via `gcloud secrets versions add`. The ExternalSecret CR just defines the sync.
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && grep -c "otel-exporter-otlp-endpoint" infra/components/secrets.py | grep -q "[1-9]" && grep -c "OTEL_EXPORTER_OTLP_ENDPOINT" infra/components/secrets.py | grep -q "[1-9]" && echo "ok"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "otel-exporter-otlp-endpoint" infra/components/secrets.py` returns 1 or more
    - `grep -c "OTEL_EXPORTER_OTLP_ENDPOINT" infra/components/secrets.py` returns 1 or more
    - The tuple targets namespace `"vici"` (not `"observability"`)
    - File still parses as valid Python
  </acceptance_criteria>
  <done>OTEL_EXPORTER_OTLP_ENDPOINT ExternalSecret targets the vici namespace SecretStore, ready to sync from GCP Secret Manager.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GCP Secret Manager -> K8s Secret | ESO syncs secrets; IAM controls access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Information Disclosure | OTEL endpoint secret value | accept | Value is a cluster-internal URL (not a credential); low sensitivity |
</threat_model>

<verification>
- `grep -q "from components.jaeger import" infra/__main__.py` exits 0
- `grep -q "from components.prometheus import" infra/__main__.py` exits 0
- `grep -c "otel-exporter-otlp-endpoint" infra/components/secrets.py` returns >= 1
- `grep -c "OTEL_EXPORTER_OTLP_ENDPOINT" infra/components/secrets.py` returns >= 1
- Both files parse as valid Python
</verification>

<success_criteria>
- infra/__main__.py imports both jaeger and prometheus components
- infra/components/secrets.py has OTEL ExternalSecret targeting vici namespace
- `pulumi preview` would include all observability resources (not verified here — deferred to deploy)
</success_criteria>

<output>
After completion, create `.planning/workstreams/gks-refactor/phases/04-observability-stack/04-03-SUMMARY.md`
</output>
