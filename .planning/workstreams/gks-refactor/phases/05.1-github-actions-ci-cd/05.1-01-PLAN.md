---
phase: 05.1-github-actions-ci-cd
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/config.py
  - infra/components/app.py
  - tests/infra/test_cd_workflows_static.py
  - tests/infra/test_cd_static.py
autonomous: true
requirements: [CD-05]

must_haves:
  truths:
    - "config.py exports IMAGE_TAG derived from cfg.get('imageTag') with ENV fallback"
    - "app.py uses IMAGE_TAG (not ENV) in the container image tag"
    - "Static test suite validates all CD requirement contracts before workflow files exist"
  artifacts:
    - path: "infra/config.py"
      provides: "IMAGE_TAG config key"
      contains: "IMAGE_TAG"
    - path: "infra/components/app.py"
      provides: "Dynamic image tag from config"
      contains: "IMAGE_TAG"
    - path: "tests/infra/test_cd_workflows_static.py"
      provides: "Static YAML validation for CD-01 through CD-05"
    - path: "tests/infra/test_cd_static.py"
      provides: "AST validation for imageTag config and app.py usage"
  key_links:
    - from: "infra/components/app.py"
      to: "infra/config.py"
      via: "IMAGE_TAG import"
      pattern: "from config import.*IMAGE_TAG"
---

<objective>
Add IMAGE_TAG config support to Pulumi and create the static test scaffold that validates all CD requirements.

Purpose: The IMAGE_TAG config key enables CI to pass `--config vici-infra:imageTag=<sha>` so deploys use the exact commit SHA as the container image tag instead of the environment name. The test scaffold (Wave 0) must exist before the workflow files so Plan 02 can verify its work against concrete assertions.

Output: Updated `config.py` and `app.py`; two new test files covering CD-01 through CD-05 and imageTag config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/gks-refactor/ROADMAP.md
@.planning/workstreams/gks-refactor/REQUIREMENTS.md
@.planning/workstreams/gks-refactor/STATE.md
@.planning/workstreams/gks-refactor/phases/05.1-github-actions-ci-cd/05.1-CONTEXT.md
@.planning/workstreams/gks-refactor/phases/05.1-github-actions-ci-cd/05.1-RESEARCH.md

<interfaces>
<!-- Existing config.py exports (will be modified) -->
From infra/config.py:
```python
cfg = pulumi.Config()
ENV: str = cfg.require("env")
CLUSTER_NAME: str = cfg.require("cluster_name")
REGISTRY_NAME: str = cfg.require("registry_name")
PROJECT_ID: str = gcp_cfg.require("project")
REGION: str = gcp_cfg.get("region") or "us-central1"
APP_HOSTNAME: str = cfg.require("app_hostname")
GITHUB_ORG: str = cfg.require("github_org")
```

From infra/components/app.py line 11-12:
```python
from config import ENV
# ...
# line 121:
image=pulumi.Output.concat(registry_url, "/vici:", ENV),
```

<!-- Test pattern precedent -->
From tests/infra/test_observability_static.py:
```python
import ast
import pathlib
INFRA_DIR = pathlib.Path(__file__).resolve().parents[2] / "infra" / "components"

def _read_source(filename: str) -> str:
    path = INFRA_DIR / filename
    assert path.exists(), f"Expected {path} to exist"
    return path.read_text()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static test scaffold for CD workflows and imageTag config</name>
  <files>tests/infra/test_cd_workflows_static.py, tests/infra/test_cd_static.py</files>
  <read_first>
    - tests/infra/test_observability_static.py (pattern to follow for AST and file parsing)
    - .github/workflows/ci.yml (verify CD-05 no-GCP-steps assertion baseline)
    - .planning/workstreams/gks-refactor/phases/05.1-github-actions-ci-cd/05.1-RESEARCH.md (Validation Architecture section — test map)
  </read_first>
  <action>
Create two test files following the established pattern from `test_observability_static.py`.

**File 1: `tests/infra/test_cd_workflows_static.py`**

Uses `yaml.safe_load` (PyYAML) to parse workflow files. Define a module-level constant:
```python
WORKFLOWS_DIR = pathlib.Path(__file__).resolve().parents[2] / ".github" / "workflows"
```

Test classes and methods:

1. `TestCD01DevAutoDeployOnMain`:
   - `test_cd_dev_triggers_on_push_to_main`: Load `cd-dev.yml`, assert `on.push.branches` contains `"main"`.
   - `test_cd_dev_calls_cd_base_with_up`: Assert the job's `uses` value is `./.github/workflows/cd-base.yml` and `with.command` is `"up"` and `with.stack` is `"dev"`.

2. `TestCD02StagingManualDispatchOnly`:
   - `test_cd_staging_triggers_on_workflow_dispatch_only`: Load `cd-staging.yml`, assert `on` has key `workflow_dispatch` and does NOT have key `pull_request`.
   - `test_cd_staging_calls_cd_base_with_up`: Assert job uses `cd-base.yml` with `command: up` and `stack: staging`.

3. `TestCD03ProdEnvironmentApproval`:
   - `test_cd_prod_triggers_on_workflow_dispatch`: Load `cd-prod.yml`, assert `on` has key `workflow_dispatch`.
   - `test_cd_prod_passes_environment_prod`: Assert the job passes `with.environment` equal to `"prod"`.

4. `TestCD04WIFAuth`:
   - `test_cd_base_build_job_uses_wif_auth`: Load `cd-base.yml`, find the `build` job, assert one step has `uses: google-github-actions/auth@v3`.
   - `test_cd_base_deploy_job_uses_wif_auth`: Same for `deploy` job.
   - `test_cd_base_no_static_key_steps`: Assert no step in either job contains string `GOOGLE_APPLICATION_CREDENTIALS` or `gcloud auth activate-service-account`.
   - `test_cd_base_has_id_token_write_permission`: Assert both `build` and `deploy` jobs have `permissions.id-token` set to `"write"`.

5. `TestCD05CIUnchanged`:
   - `test_ci_yml_has_no_gcp_auth_steps`: Load `ci.yml`, walk all steps, assert no step `uses` value contains `google-github-actions` and no step `run` value contains `gcloud`.

6. `TestCDBaseStructure`:
   - `test_cd_base_has_two_jobs`: Load `cd-base.yml`, assert `jobs` has exactly two keys: `build` and `deploy`.
   - `test_cd_base_deploy_needs_build`: Assert `deploy` job has `needs` containing `"build"`.
   - `test_cd_base_build_outputs_sha`: Assert `build` job has `outputs.sha` defined.
   - `test_cd_base_uses_docker_buildx_cache`: Assert `build` job has a step using `docker/build-push-action` with `cache-from: type=gha` in `with`.
   - `test_cd_base_build_only_pushes_on_up`: Assert the `docker/build-push-action` step has `if: inputs.command == 'up'`.
   - `test_cd_base_has_gcp_project_input`: Assert `on.workflow_call.inputs` includes `gcp_project` with `required: true`.
   - `test_cd_base_health_check_on_dev_up`: Assert `deploy` job has a step with `if` containing `inputs.stack == 'dev'` and `inputs.command == 'up'`, and the step's `run` contains `curl` and `/health`.
   - `test_cd_base_uses_pulumi_config_map_for_image_tag`: Assert `deploy` job's `pulumi/actions` step has `with.config-map` containing `imageTag`.

**File 2: `tests/infra/test_cd_static.py`**

Uses `ast` module to parse Python source. Define:
```python
INFRA_DIR = pathlib.Path(__file__).resolve().parents[2] / "infra"
COMPONENTS_DIR = INFRA_DIR / "components"
```

Test classes:

1. `TestImageTagConfig`:
   - `test_config_exports_image_tag`: Parse `config.py` via AST. Assert there is a module-level assignment to `IMAGE_TAG`.
   - `test_config_image_tag_has_env_fallback`: Read `config.py` source text, assert it contains `cfg.get("imageTag") or ENV`.

2. `TestAppUsesImageTag`:
   - `test_app_imports_image_tag`: Read `app.py` source text, assert it contains `IMAGE_TAG` in an import line matching `from config import`.
   - `test_app_image_uses_image_tag_not_env`: Read `app.py` source text. Find the line with `registry_url` and `/vici:`. Assert it contains `IMAGE_TAG` and does NOT end with `, ENV)`.

All tests should use `pytest.mark.parametrize` where it reduces duplication but keep individual test methods where clarity demands it. Import `yaml` (PyYAML is already a dev dependency via the project).
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && uv run pytest tests/infra/test_cd_workflows_static.py::TestCD05CIUnchanged -x --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - tests/infra/test_cd_workflows_static.py exists and contains `class TestCD01DevAutoDeployOnMain`
    - tests/infra/test_cd_workflows_static.py contains `class TestCD02StagingManualDispatchOnly`
    - tests/infra/test_cd_workflows_static.py contains `class TestCD03ProdEnvironmentApproval`
    - tests/infra/test_cd_workflows_static.py contains `class TestCD04WIFAuth`
    - tests/infra/test_cd_workflows_static.py contains `class TestCD05CIUnchanged`
    - tests/infra/test_cd_workflows_static.py contains `class TestCDBaseStructure`
    - tests/infra/test_cd_static.py contains `class TestImageTagConfig`
    - tests/infra/test_cd_static.py contains `class TestAppUsesImageTag`
    - Tests that check ci.yml (CD-05) pass immediately (ci.yml already has no GCP steps)
    - Tests that check cd-base.yml, cd-dev.yml, cd-staging.yml, cd-prod.yml FAIL (expected — workflow files not yet rewritten)
    - Tests that check config.py IMAGE_TAG FAIL (expected — not yet added)
  </acceptance_criteria>
  <done>Both test files exist, import correctly, and the CD-05 ci.yml test passes. All other tests fail as expected (RED state for Plan 02).</done>
</task>

<task type="auto">
  <name>Task 2: Add IMAGE_TAG config key to config.py and update app.py</name>
  <files>infra/config.py, infra/components/app.py</files>
  <read_first>
    - infra/config.py (current state — 13 lines)
    - infra/components/app.py (current state — line 11 import, line 121 image tag)
  </read_first>
  <action>
**infra/config.py** — Add one line after the `GITHUB_ORG` line (line 12):

```python
IMAGE_TAG: str = cfg.get("imageTag") or ENV      # CI passes --config vici-infra:imageTag=<sha>; local fallback to ENV
```

This reads the `vici-infra:imageTag` config key (set via `pulumi up --config vici-infra:imageTag=a1b2c3d` or `pulumi/actions@v6 config-map`). When not set (local dev, manual pulumi up), falls back to `ENV` (e.g., "dev") which is the existing behavior.

**infra/components/app.py** — Two changes:

1. Line 11: Change import from:
```python
from config import ENV
```
to:
```python
from config import ENV, IMAGE_TAG
```

2. Line 121: Change image tag from:
```python
image=pulumi.Output.concat(registry_url, "/vici:", ENV),
```
to:
```python
image=pulumi.Output.concat(registry_url, "/vici:", IMAGE_TAG),
```

Per D-07 and D-08: Docker images are tagged with 7-char git SHA in CI. The `IMAGE_TAG` config key receives this SHA via `--config vici-infra:imageTag=<sha>`. The Pulumi project name is `vici-infra` (from `infra/Pulumi.yaml`), so `cfg.get("imageTag")` correctly reads the `vici-infra:imageTag` namespaced key.
  </action>
  <verify>
    <automated>cd /Users/ahcarpenter/workspace/vici && uv run pytest tests/infra/test_cd_static.py -x --tb=short -q</automated>
  </verify>
  <acceptance_criteria>
    - infra/config.py contains the line `IMAGE_TAG: str = cfg.get("imageTag") or ENV`
    - infra/components/app.py contains `from config import ENV, IMAGE_TAG`
    - infra/components/app.py contains `registry_url, "/vici:", IMAGE_TAG` (not `ENV`)
    - tests/infra/test_cd_static.py all pass (GREEN)
  </acceptance_criteria>
  <done>config.py exports IMAGE_TAG with ENV fallback. app.py uses IMAGE_TAG for the container image tag. All test_cd_static.py tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config.py input | imageTag config value comes from CLI flag (CI) or stack config (local) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5.1-01 | Tampering | config.py IMAGE_TAG | accept | imageTag is a non-secret config value (git SHA); no injection risk since Pulumi config values are string-typed and used only in container image URI |
</threat_model>

<verification>
- `uv run pytest tests/infra/test_cd_static.py -x --tb=short -q` passes (IMAGE_TAG config and app.py usage verified)
- `uv run pytest tests/infra/test_cd_workflows_static.py::TestCD05CIUnchanged -x` passes (ci.yml unchanged)
- `grep "IMAGE_TAG" infra/config.py` returns the IMAGE_TAG assignment line
- `grep "IMAGE_TAG" infra/components/app.py` returns the import and usage lines
</verification>

<success_criteria>
- IMAGE_TAG config key exists in config.py with ENV fallback
- app.py uses IMAGE_TAG for container image tag
- Test scaffold exists and validates all CD requirements via static YAML/AST parsing
- CD-05 tests pass (ci.yml has no GCP steps)
- CD-01 through CD-04 and structure tests fail (workflow files not yet rewritten — RED state for Plan 02)
</success_criteria>

<output>
After completion, create `.planning/workstreams/gks-refactor/phases/05.1-github-actions-ci-cd/05.1-01-SUMMARY.md`
</output>
