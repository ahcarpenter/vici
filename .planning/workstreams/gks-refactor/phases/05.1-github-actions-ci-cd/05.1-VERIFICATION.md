---
phase: 05.1-github-actions-ci-cd
verified: 2026-04-11T16:00:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Configure GitHub Environment named 'prod' with required reviewers"
    expected: "GitHub repo Settings → Environments → 'prod' exists with at least one required reviewer; cd-prod.yml workflow_dispatch runs pause the deploy job until approval"
    why_human: "GitHub Environment configuration lives in repo settings dashboard, not in the codebase. The workflow file correctly passes environment: prod to cd-base.yml, but the approval gate only exists if the Environment is configured in the dashboard. This is the user_setup task from 05.1-02-PLAN.md."
  - test: "Set GitHub repo secrets after first pulumi up: GCP_WIF_PROVIDER, GCP_CI_SA_EMAIL, PULUMI_CONFIG_PASSPHRASE"
    expected: "GitHub repo Settings → Secrets and variables → Actions lists all three secrets as repository secrets populated with values from Pulumi stack outputs"
    why_human: "Secret values are produced by pulumi stack output after cluster provisioning, and must be entered into GitHub dashboard manually. Workflow files reference these secret names correctly but cannot verify the secrets themselves exist."
  - test: "Runtime validation: push a commit to main and observe cd-dev Actions run"
    expected: "Actions tab shows CD Dev run: build job completes with a 7-char SHA tag pushed to us-central1-docker.pkg.dev/vici-app-dev/vici-images/vici, deploy job runs pulumi up --stack dev, health check returns 200 from https://dev.usevici.com/health"
    why_human: "End-to-end CD pipeline cannot be verified statically. Requires live GitHub Actions run, real WIF handshake, real Docker build/push, real pulumi up, and real cluster rollout."
  - test: "Runtime validation: workflow_dispatch cd-prod.yml and verify approval gate blocks deploy until reviewer approves"
    expected: "cd-prod run pauses deploy job with 'Waiting for review' status; after reviewer approves, deploy job runs pulumi up --stack prod"
    why_human: "GitHub Environment approval gate behavior only manifests at runtime in GitHub Actions UI. Requires a human reviewer to exercise the approval flow."
---

# Phase 05.1: GitHub Actions CI/CD Verification Report

**Phase Goal:** CD workflow files fully match locked decisions — two-job reusable base, Docker GHA layer cache, SHA-only tagging with Pulumi config-map passthrough, staging dispatch-only, prod environment approval gate, and post-deploy health check

**Verified:** 2026-04-11T16:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | cd-base.yml has two jobs (build + deploy) with WIF auth, Docker GHA cache, SHA-only image tagging | VERIFIED | cd-base.yml lines 30-108: jobs dict has exactly `{build, deploy}`; deploy has `needs: [build]`; both jobs have `permissions.id-token: write`; both use `google-github-actions/auth@v3`; build uses `docker/build-push-action@v6` with `cache-from: type=gha` and `cache-to: type=gha,mode=max`; tag is `...vici:${{ steps.sha.outputs.sha }}` from `git rev-parse --short=7 HEAD` |
| 2 | cd-dev.yml triggers on push to main and calls cd-base.yml with command=up and stack=dev | VERIFIED | cd-dev.yml line 3-5: `"on": push.branches: [main]`; lines 12-17: `uses: ./.github/workflows/cd-base.yml` with `stack: dev`, `command: up`, `gcp_project: vici-app-dev` |
| 3 | cd-staging.yml triggers only on workflow_dispatch (no pull_request trigger) | VERIFIED | cd-staging.yml line 3-4: `"on": workflow_dispatch:`; grep confirms no `pull_request` key anywhere in file |
| 4 | cd-prod.yml triggers on workflow_dispatch with environment: prod approval gate | VERIFIED (code); needs human verification for GitHub Environment config | cd-prod.yml lines 3-4 (workflow_dispatch trigger), line 16 (`environment: prod` passed to cd-base.yml), which applies it to the deploy job at cd-base.yml:66 `environment: ${{ inputs.environment }}` |
| 5 | config.py exports IMAGE_TAG from cfg.get("imageTag") with ENV fallback; app.py uses IMAGE_TAG | VERIFIED | config.py:13 `IMAGE_TAG: str = cfg.get("imageTag") or ENV`; app.py:11 `from config import ENV, IMAGE_TAG`; app.py:124 `image=pulumi.Output.concat(registry_url, "/vici:", IMAGE_TAG)` |
| 6 | Static test suite validates all CD contracts via YAML and AST parsing | VERIFIED | 23/23 tests pass (6 test classes in test_cd_workflows_static.py + 2 test classes in test_cd_static.py) |

**Score:** 6/6 truths verified in code

### Plan-Level Must-Haves (from PLAN frontmatter)

**Plan 01 truths:**
- config.py exports IMAGE_TAG derived from cfg.get('imageTag') with ENV fallback — VERIFIED (config.py:13)
- app.py uses IMAGE_TAG (not ENV) in the container image tag — VERIFIED (app.py:124)
- Static test suite validates all CD requirement contracts before workflow files exist — VERIFIED (tests created in Wave 1 commit 50914d5)

**Plan 02 truths:**
- cd-base.yml has two jobs: build and deploy — VERIFIED
- cd-dev.yml triggers on push to main — VERIFIED
- cd-staging.yml workflow_dispatch only — VERIFIED
- cd-prod.yml workflow_dispatch + environment=prod — VERIFIED (code-side)
- All workflows use WIF auth — VERIFIED
- Docker images tagged with 7-char SHA only — VERIFIED
- Deploy job passes imageTag to Pulumi via config-map — VERIFIED (cd-base.yml:91)
- Dev deploy includes post-deploy health check — VERIFIED (cd-base.yml:95-108)
- All callers use singular secrets GCP_WIF_PROVIDER / GCP_CI_SA_EMAIL — VERIFIED (all three caller files, secrets block)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `.github/workflows/cd-base.yml` | Two-job reusable workflow | VERIFIED | 109 lines, `workflow_call` defined, `jobs: {build, deploy}`, all required inputs/secrets/outputs present |
| `.github/workflows/cd-dev.yml` | Dev auto-deploy on push to main | VERIFIED | 22 lines, push-main trigger, `gcp_project: vici-app-dev`, singular secret refs |
| `.github/workflows/cd-staging.yml` | Staging manual deploy | VERIFIED | 21 lines, workflow_dispatch only, no pull_request, `gcp_project: vici-app-staging` |
| `.github/workflows/cd-prod.yml` | Prod manual with environment gate | VERIFIED | 22 lines, workflow_dispatch, `environment: prod`, `gcp_project: vici-app-prod` |
| `infra/config.py` | IMAGE_TAG config key | VERIFIED | Line 13 contains `IMAGE_TAG: str = cfg.get("imageTag") or ENV` |
| `infra/components/app.py` | Dynamic image tag from config | VERIFIED | Line 11 imports IMAGE_TAG; line 124 uses it in `pulumi.Output.concat` |
| `tests/infra/test_cd_workflows_static.py` | YAML validation for CD-01..CD-05 | VERIFIED | 332 lines, 6 test classes (TestCD01..TestCD05, TestCDBaseStructure), 19 test methods, all pass |
| `tests/infra/test_cd_static.py` | AST validation for imageTag config | VERIFIED | 117 lines, 2 test classes (TestImageTagConfig, TestAppUsesImageTag), 4 test methods, all pass |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| cd-dev.yml | cd-base.yml | workflow_call | WIRED | Line 13: `uses: ./.github/workflows/cd-base.yml` |
| cd-staging.yml | cd-base.yml | workflow_call | WIRED | Line 12: `uses: ./.github/workflows/cd-base.yml` |
| cd-prod.yml | cd-base.yml | workflow_call | WIRED | Line 12: `uses: ./.github/workflows/cd-base.yml` |
| cd-base.yml deploy job | Pulumi imageTag config | config-map JSON | WIRED | Line 91: `config-map: '{"vici-infra:imageTag": {"value": "${{ needs.build.outputs.sha }}", "secret": false}}'` — references build job's sha output |
| infra/components/app.py | infra/config.py | IMAGE_TAG import + usage | WIRED | app.py:11 `from config import ENV, IMAGE_TAG`; app.py:124 uses IMAGE_TAG in image concat |
| cd-base.yml build job | cd-base.yml deploy job | `needs.build.outputs.sha` | WIRED | build job declares `outputs.sha: ${{ steps.sha.outputs.sha }}` (line 37); deploy has `needs: [build]` (line 65) and references `${{ needs.build.outputs.sha }}` (line 91) |
| All callers | WIF secrets | GCP_WIF_PROVIDER / GCP_CI_SA_EMAIL | WIRED | All three caller files pass these as `WIF_PROVIDER` and `WIF_SERVICE_ACCOUNT` secrets to cd-base.yml |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|-------------------|--------|
| cd-base.yml deploy:pulumi | config-map imageTag value | `needs.build.outputs.sha` ← build job's `steps.sha.outputs.sha` ← `git rev-parse --short=7 HEAD` | Yes (real 7-char git SHA at runtime) | FLOWING |
| infra/config.py IMAGE_TAG | imageTag config value | `cfg.get("imageTag")` ← Pulumi config-map from CI | Yes (when CI passes it); falls back to ENV locally | FLOWING |
| infra/components/app.py container image | IMAGE_TAG | `from config import IMAGE_TAG` | Yes (bound to config.py export) | FLOWING |

No HOLLOW or DISCONNECTED artifacts. The full chain SHA → build output → needs.build.outputs.sha → config-map → cfg.get("imageTag") → IMAGE_TAG → registry_url concat is wired end-to-end.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Static test suite passes | `uv run pytest tests/infra/test_cd_workflows_static.py tests/infra/test_cd_static.py -v` | 23 passed in 0.07s | PASS |
| config.py IMAGE_TAG grep | `grep IMAGE_TAG infra/config.py` | Match at line 13 | PASS |
| app.py IMAGE_TAG grep | `grep IMAGE_TAG infra/components/app.py` | Matches at lines 11, 124 | PASS |
| cd-staging.yml has no pull_request | grep `pull_request` cd-staging.yml | Zero matches | PASS |
| Plan commits exist in git log | `git log --oneline` for 50914d5, 450e6cf, c25e3e3, 1c3451a | All four commits present | PASS |
| End-to-end CI run (push → deploy) | Requires merge to main | N/A | SKIP (human) |
| GitHub Environment approval gate runtime | Requires workflow_dispatch and human reviewer | N/A | SKIP (human) |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| CD-01 | 05.1-02 | GitHub Actions CD job builds Docker image, pushes to Artifact Registry, and runs pulumi up --stack dev on every push to main | SATISFIED | cd-dev.yml push-main trigger → cd-base.yml build job builds+pushes image, deploy job runs pulumi up with stack: dev |
| CD-02 | 05.1-02 | GitHub Actions runs pulumi preview on every PR; pulumi up --stack staging runs on workflow dispatch | SATISFIED (with locked deviation) | cd-staging.yml workflow_dispatch only; PR preview was deliberately dropped per D-05 / CONTEXT.md (locked decision: "No PR preview, no auto-deploy"). The staging side of CD-02 (dispatch-triggered up) is satisfied; PR preview is a deferred enhancement. |
| CD-03 | 05.1-02 | pulumi up --stack prod requires manual workflow dispatch with environment approval gate | NEEDS HUMAN | cd-prod.yml workflow_dispatch + environment: prod passed through to cd-base.yml deploy job. Approval gate behavior requires GitHub Environment 'prod' to be configured in repo settings (user_setup task). |
| CD-04 | 05.1-02 | Pulumi state access uses Workload Identity (GitHub Actions OIDC → GCP) — no static keys | SATISFIED | cd-base.yml uses google-github-actions/auth@v3 in both jobs with WIF_PROVIDER / WIF_SERVICE_ACCOUNT secrets; both jobs have id-token: write; no GOOGLE_APPLICATION_CREDENTIALS or gcloud auth activate-service-account references |
| CD-05 | 05.1-01 | CI test job is unchanged: pytest with SQLite, no GCP dependency | SATISFIED | ci.yml was not modified; test_cd_workflows_static.py::TestCD05CIUnchanged::test_ci_yml_has_no_gcp_auth_steps passes |

**Orphaned requirements check:** REQUIREMENTS.md maps CD-01..CD-05 to "Phase 5" broadly; all five IDs are claimed by plans in 05.1 (CD-01..CD-04 by 05.1-02, CD-05 by 05.1-01). No orphans.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| cd-base.yml | 91 | String-interpolated config-map JSON (per IN-02 in REVIEW.md) | Info | Safe today since sha source is `[0-9a-f]{7}`; flagged as non-blocker by REVIEW |
| cd-base.yml | 31-61 vs 63-66 | Prod environment gate only on deploy job, not build (per WR-01) | Warning | Does not falsify success criteria (criterion #4 says "pass environment: prod from cd-prod.yml to cd-base.yml for the deploy gate" which is met); REVIEW.md flags as non-blocker per instructions |
| All cd-*.yml callers | top-level `permissions:` | Dead permissions block (per IN-01) | Info | No functional impact; cosmetic audit concern |
| cd-base.yml | 95-108 | Health check 60s budget may be too short (per WR-03) | Warning | Runtime concern only — success criterion is "post-deploy health check exists", which is met |
| infra/config.py | 13 | ENV fallback may mask CI misconfig (per IN-06) | Info | Local-dev ergonomics vs CI safety tradeoff; not a phase goal violation |

Per instructions, all WR-level findings are already flagged in REVIEW.md and should not be re-raised as verification gaps unless they falsify a success criterion. None do — the phase goal explicitly says "staging dispatch-only" and "prod environment approval gate" which are both present in the workflow files at the surface level the success criteria specify.

### Human Verification Required

1. **GitHub Environment 'prod' configured**
   - **Test:** In GitHub repo Settings → Environments, confirm a `prod` environment exists with at least one required reviewer.
   - **Expected:** The `prod` environment appears in the environments list with "Required reviewers" set. When cd-prod.yml runs via workflow_dispatch, the deploy job shows "Waiting for review" until a reviewer approves.
   - **Why human:** GitHub Environment configuration is a dashboard task and cannot be verified from the codebase. The workflow file correctly passes `environment: prod` but the approval gate itself lives in repo settings.

2. **GitHub repo secrets populated**
   - **Test:** In GitHub repo Settings → Secrets and variables → Actions, confirm `GCP_WIF_PROVIDER`, `GCP_CI_SA_EMAIL`, and `PULUMI_CONFIG_PASSPHRASE` are set as repository secrets.
   - **Expected:** All three secrets listed under "Repository secrets" with non-empty values sourced from `pulumi stack output wif_provider_name`, `pulumi stack output ci_sa_email`, and the Pulumi encryption passphrase respectively.
   - **Why human:** Secret values are runtime-only and come from Pulumi outputs after the GKE cluster is provisioned; they cannot be placed in source control. Workflow files reference the correct secret names but cannot verify the secrets exist.

3. **End-to-end cd-dev runtime validation**
   - **Test:** Merge a trivial commit to `main` and watch the CD Dev workflow run in the Actions tab.
   - **Expected:** Build job completes in a reasonable time (with cache hit after first run), pushes image tagged with 7-char git SHA to `us-central1-docker.pkg.dev/vici-app-dev/vici-images/vici:<sha>`. Deploy job runs `pulumi up --stack dev` successfully and the health check step gets HTTP 200 from `https://dev.usevici.com/health`.
   - **Why human:** Runtime validation of WIF handshake, Docker push to Artifact Registry, Pulumi state write to GCS, and cluster rollout + Ingress health cannot be simulated statically.

4. **cd-prod approval gate runtime validation**
   - **Test:** Dispatch cd-prod.yml from the Actions UI and verify the deploy job blocks pending reviewer approval.
   - **Expected:** Build job runs to completion; deploy job status shows "Waiting for review" in the GitHub Actions UI; after an authorized reviewer approves, deploy job proceeds with `pulumi up --stack prod`.
   - **Why human:** GitHub Environment approval gate behavior only manifests in the Actions UI with a live reviewer, and exercising it is the only way to confirm the protection rule is actually enforcing approval on this workflow.

### Gaps Summary

No code-level gaps block the phase goal. All six success criteria are satisfied in the committed workflow and config files, the full static test suite passes (23/23), and all key wiring is in place end-to-end (caller → cd-base.yml → config-map → config.py → app.py container image).

Four items require human verification before Phase 05.1 can be declared operationally complete:
1. GitHub Environment `prod` with required reviewers (dashboard task)
2. GitHub repo secrets (`GCP_WIF_PROVIDER`, `GCP_CI_SA_EMAIL`, `PULUMI_CONFIG_PASSPHRASE`) populated from Pulumi outputs
3. Live CD Dev run on push to main (validates WIF, Docker push, Pulumi up, health check)
4. Live CD Prod dispatch validates approval gate actually pauses deploy job

Per the project's gate policy, WR-level findings from the REVIEW report (prod build bypass, concurrency, health check timing) are known non-blockers and do not reopen as gaps here — they do not falsify any of the six success criteria as written. Review those separately in a follow-up refinement pass if desired.

---

_Verified: 2026-04-11T16:00:00Z_
_Verifier: Claude (gsd-verifier)_
