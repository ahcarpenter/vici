# Phase 5.1: GitHub Actions CI/CD - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-09
**Phase:** 05.1-github-actions-ci-cd
**Areas discussed:** Workflow structure, Deploy gating, Image tagging, Secrets & config

---

## Workflow structure

| Option | Description | Selected |
|--------|-------------|----------|
| Single job | One job: checkout, WIF auth, docker build+push, pulumi up/preview | |
| Two jobs: build + deploy | Separate build job and deploy job. Enables retrying deploy without rebuilding. | ✓ |
| Three jobs: build + preview + deploy | Build, then pulumi preview (always), then pulumi up (conditional) | |

**User's choice:** Two jobs: build + deploy
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| Base handles everything | cd-base.yml installs Pulumi, runs pulumi login, executes command | ✓ |
| Caller handles Pulumi | Each cd-*.yml installs Pulumi and logs in separately | |

**User's choice:** Base handles everything (Recommended)
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| Always build | Build every time. Docker layer cache via actions/cache. | ✓ |
| Skip if exists | Check Artifact Registry before building. | |

**User's choice:** Always build (Recommended)
**Notes:** None

---

## Deploy gating

| Option | Description | Selected |
|--------|-------------|----------|
| Preview on PR, deploy on dispatch | PRs get pulumi preview comment. Staging deploy requires manual workflow_dispatch. | |
| Preview on PR, auto-deploy on merge | PRs get pulumi preview. Merging to main auto-deploys staging too. | |
| Manual only | No PR preview. Staging deploy only via manual workflow_dispatch. | ✓ |

**User's choice:** Manual only — no staging deploy preview on PRs
**Notes:** User selected option 1 but clarified "no staging deploy" — wants manual dispatch only.

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, curl /health | After pulumi up, curl dev Ingress /health. Fail if not 200 within 60s. | ✓ |
| No health check | Trust Pulumi completion. | |
| Full smoke test | Health check plus test SMS webhook POST. | |

**User's choice:** curl /health for now
**Notes:** User wants to come back to full smoke test (option 3) later.

| Option | Description | Selected |
|--------|-------------|----------|
| GitHub Environment | Create 'prod' GitHub Environment with required reviewers. Native, auditable. | ✓ |
| Manual workflow input | Boolean input confirm_prod: true. No reviewer gate. | |

**User's choice:** GitHub Environment (Recommended)
**Notes:** None

---

## Image tagging

| Option | Description | Selected |
|--------|-------------|----------|
| Git SHA short | Tag with 7-char git SHA. Immutable, traceable to exact commit. | ✓ |
| SHA + latest | Tag with git SHA AND update :latest. | |
| Branch-based | Tag with branch name. Mutable tags. | |

**User's choice:** Git SHA short (Recommended)
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| CLI flag | Pass --config app:imageTag=<sha> to pulumi up. | ✓ |
| Stack config update | Update Pulumi.<env>.yaml with new tag. | |
| Environment variable | Set IMAGE_TAG env var that config.py reads. | |

**User's choice:** CLI flag (Recommended)
**Notes:** None

---

## Secrets & config

| Option | Description | Selected |
|--------|-------------|----------|
| GitHub repo secrets | Store WIF provider full name as GitHub secret. | ✓ |
| Hardcoded in workflow | Construct WIF provider path from known IDs in YAML. | |
| Pulumi stack output | Run pulumi stack output first, then use. | |

**User's choice:** GitHub repo secrets (Recommended)
**Notes:** None

| Option | Description | Selected |
|--------|-------------|----------|
| GitHub secret (PULUMI_ACCESS_TOKEN) | Store token as GitHub repo secret. | |
| Not needed — GCS backend | Pulumi state in GCS, WIF auth covers access. No token needed. | ✓ |

**User's choice:** Not needed — GCS backend
**Notes:** Since Pulumi state is in GCS (not Pulumi Cloud), no access token required.

---

## Claude's Discretion

- GitHub Actions step naming and ordering
- Docker build context and caching strategy details
- Pulumi install method
- Health check retry logic

## Deferred Ideas

- Full smoke test (test SMS webhook POST) — revisit after basic health check
- Staging PR preview (pulumi preview as PR comment) — deliberate v1 simplification
- Image promotion strategy (promote dev image instead of rebuilding)
