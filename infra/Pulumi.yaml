name: vici-infra
description: Vici GKE infrastructure — all environments
runtime:
  name: python
  options:
    virtualenv: .venv
backend:
  url: gs://vici-app-pulumi-state-dev   # default for local dev; CI overrides via PULUMI_BACKEND_URL
