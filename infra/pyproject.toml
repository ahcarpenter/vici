[project]
name = "vici-infra"
version = "0.1.0"
description = "Vici GKE infrastructure — Pulumi Python program"
requires-python = ">=3.12"
dependencies = [
    "pulumi>=3.229.0,<4",
    "pulumi-gcp>=9.18.0,<10",
    "pulumi-kubernetes>=4.28.0,<5",
]

[tool.uv]
package = false
